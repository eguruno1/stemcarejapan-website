# Phase 4: 실시간 채팅(Socket.IO) 개발계획서

## Phase 2·3 완료 후 인계 (2026-09-18)

Phase 2·3 검토 완료 구현을 출발점으로 사용한다. useRoomStream의 appendNote, 방 ID/요청 세대 검사, 쓰기 이후 오래된 폴링 응답 무효화, 중앙 401 처리, IME 보호와 같은 clientMessageId 재시도를 보존한다. 단순히 초안의 훅 전체 코드로 교체하지 않는다. 현재 목록은 5초, 상세·고객 위젯은 3초 폴링이다.
이 Phase의 구현·완료 상태는 이번 검토에서 변경하지 않는다.


## Phase 0·1 완료 후 인계 (2026-09-18)

이 Phase는 **미완료 / 후속 개발 대상**이다. Phase 0·1 완료 문서와 실제 소스를 선행 기준으로 사용한다. 아래 인계 사항은 본문의 과거 예시보다 우선한다.

현재 createMessage(input, tx?)는 DTO를 반환하며 행 잠금·종료 검사·발신자 ID 충돌 검사를 수행한다. createMessageRow 분리 시 이 보장을 그대로 옮기고 DB commit 후 emit한다. 인증 시 활성 계정을 재확인한다. Phase 1의 별도 markRoomRead 호출은 getRoomDetailAndMarkRead로 통합되었다. Socket 읽음 이벤트를 만들 때도 같은 방 잠금과 읽음 시각 규칙을 적용한 전용 서비스를 추가해야 한다.


> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

작성일: 2026-09-04

**Goal:** 고객 위젯과 운영자 화면이 폴링 없이 실시간으로 메시지를 주고받고, 신규 상담·상태 변경·입력 중 표시가 즉시 반영되게 한다.

**Architecture:** Express HTTP 서버에 Socket.IO를 얹는다. 고객은 `visitorToken`, 운영자는 로그인 쿠키의 JWT로 소켓 인증한다. 소켓은 **저장 로직을 새로 만들지 않고** Phase 1의 `messageService.createMessage` / `chatRoomService` 를 그대로 호출한다. 저장에 성공하면 서버가 `room:<roomId>` 룸에 브로드캐스트하고, 운영자 목록 갱신은 `operators` 룸에 따로 보낸다. 클라이언트 쪽은 Phase 2의 `poller.js` 와 Phase 3의 `useRoomStream` 내부만 교체하고, 두 곳 모두 **소켓이 끊기면 폴링으로 자동 대체(fallback)** 한다.

**Tech Stack:** Socket.IO 4.7(서버/클라이언트), Express 4, Vitest + socket.io-client(테스트)

**Spec:** `docs/20260904_상담채팅시스템구축방향.md` 3절·11절 3단계, `docs/plans/20260904_phase4_상담채팅시스템구축계획서.md`

---

## Global Constraints

Phase 0 문서의 **Global Constraints 전체가 그대로 적용된다.** 추가 제약:

- **저장 로직을 소켓 핸들러에 복사하지 않는다.** 메시지 저장은 오직 `messageService.createMessage` 한 곳에서만 일어난다. HTTP와 Socket이 각자 저장하면 반드시 어긋난다.
- REST API를 **삭제하지 않는다.** 소켓이 막힌 네트워크(사내 방화벽 등)에서 폴백으로 계속 쓴다.
- 모든 소켓 이벤트 이름은 `chat:` 접두어를 쓴다. 목록 갱신용은 `rooms:` 접두어.
- 클라이언트가 보낸 값은 **소켓에서도 Zod로 검증**한다. HTTP만 검증하고 소켓은 믿는 실수를 하지 않는다.
- 소켓 연결 시 인증에 실패하면 **연결 자체를 거부**한다. 연결한 뒤에 권한을 확인하지 않는다.
- 브로드캐스트는 `room:<roomId>` 룸에만 보낸다. 실수로 전체(`io.emit`)에 보내면 남의 상담 내용이 새어 나간다.
- 고객에게 나가는 페이로드는 `toMessageDTO(row, 'customer')` 를, 운영자에게는 `'operator'` 를 거친다. **같은 객체를 양쪽에 보내지 않는다.**

---

## 이 Phase를 시작하기 전에 알아야 할 개념

**WebSocket과 Socket.IO의 차이**
WebSocket은 브라우저와 서버가 연결을 계속 열어두고 양방향으로 말할 수 있게 하는 표준 기술이다. Socket.IO는 그 위에 자동 재연결, 방(room) 개념, 이벤트 이름, 폴백 전송 방식을 얹은 라이브러리다. 직접 WebSocket을 쓰면 "끊겼을 때 다시 붙이기"를 전부 손으로 짜야 한다.

**"방(room)"이란?**
Socket.IO에서 방은 그냥 이름표다. `socket.join('room:abc')` 하면 그 소켓에 `room:abc` 라벨이 붙고, `io.to('room:abc').emit(...)` 하면 그 라벨을 가진 소켓들에게만 간다. 우리는 상담방마다 하나씩(`room:<roomId>`), 그리고 운영자 전용으로 하나(`operators`) 만든다.

**왜 소켓에서도 인증을 하나?**
소켓 연결은 HTTP 라우터를 거치지 않는다. `requireOperator` 미들웨어가 적용되지 않는다는 뜻이다. 인증을 안 하면 누구나 `chat:join` 으로 아무 `roomId`나 넣어 남의 상담을 훔쳐볼 수 있다.

**폴백(fallback)이 왜 필요한가?**
회사 방화벽이나 일부 프록시는 WebSocket을 막는다. 그러면 고객 화면이 아예 동작하지 않는다. 그래서 "소켓이 5초 안에 연결되지 않으면 폴링으로 돌아간다"는 안전장치를 넣는다. Phase 2/3에서 폴링 코드를 지우지 않고 남겨두는 이유가 이것이다.

---

## File Structure

```text
apps/api/src/
├── server.ts                    수정 (http.Server 를 만들고 소켓 부착)
├── realtime/
│   ├── socketServer.ts       ★ Socket.IO 서버 생성과 조립
│   ├── authSocket.ts         ★ 연결 인증 미들웨어
│   ├── rooms.ts              ★ 룸 이름 규칙 (한 곳에서만 만든다)
│   ├── chatEvents.ts         ★ chat:* 이벤트 핸들러
│   └── emitters.ts           ★ 서버 → 클라이언트 전송 함수 모음
├── chatRooms/
│   ├── chatRoomService.ts       수정 (requestHandoff 추가)
│   └── adminChatRoutes.ts       수정 (HTTP 변경도 브로드캐스트)
└── messages/
    └── messageService.ts        수정 (저장 후 훅 호출)

apps/api/tests/
└── realtime.test.ts          ★

js/chat/
├── socket.js                 ★ Socket.IO 클라이언트 (poller.js 대체)
├── poller.js                    유지 (폴백용)
├── stream.js                 ★ socket 우선 → 실패 시 poller 로 전환
└── widget.js                    수정 (stream.js 사용)

apps/admin/
├── lib/socket.ts             ★ 소켓 싱글턴
├── hooks/useRoomStream.ts       수정 (내부만 소켓으로)
├── hooks/useRoomList.ts         수정 (rooms:updated 수신)
└── components/chat/ChatComposer.tsx  수정 (입력 중 전송)

index.html / stemcell/index.html / korea-travel/index.html  수정 (socket.io CDN 추가)
```

---

## Task 1: Socket.IO 서버 부착과 연결 인증

**Files:**
- Create: `apps/api/src/realtime/rooms.ts`
- Create: `apps/api/src/realtime/authSocket.ts`
- Create: `apps/api/src/realtime/socketServer.ts`
- Modify: `apps/api/src/server.ts`
- Modify: `apps/api/src/app.ts`
- Test: `apps/api/tests/realtime.test.ts`

**Interfaces:**
- Consumes: `authorizeVisitor` (Phase 1 Task 6), `verifyOperatorToken` (Phase 1 Task 4), `config`
- Produces:
  - `rooms.ts` → `roomChannel(roomId: string): string` (= `room:${roomId}`), `OPERATORS_CHANNEL = 'operators'`
  - `authSocket.ts` → `authenticateSocket(socket, next)`; 성공 시 `socket.data.identity` 를 채운다
    - `SocketIdentity = { kind: 'customer'; roomId: string; customerId: string } | { kind: 'operator'; operatorId: string; role: OperatorRole }`
  - `socketServer.ts` → `createSocketServer(httpServer: http.Server): Server`, `getIo(): Server | null`

---

- [ ] **Step 1: 패키지 설치**

```bash
npm install -w apps/api socket.io
npm install -w apps/api -D socket.io-client
```

- [ ] **Step 2: 실패하는 테스트 작성**

`apps/api/tests/realtime.test.ts`

```ts
import { createServer, type Server as HttpServer } from 'node:http';
import type { AddressInfo } from 'node:net';
import { io as ioClient, type Socket as ClientSocket } from 'socket.io-client';
import { afterAll, afterEach, beforeAll, beforeEach, describe, expect, it } from 'vitest';
import { createApp } from '../src/app';
import { signOperatorToken } from '../src/auth/token';
import { config } from '../src/config';
import { createSocketServer } from '../src/realtime/socketServer';
import { createCustomerWithRoom, createOperator } from './helpers/factories';
import { disconnectDatabase, resetDatabase } from './helpers/testDb';

let httpServer: HttpServer;
let baseUrl: string;
const openSockets: ClientSocket[] = [];

/** 테스트용 소켓을 열고, 연결 성공/실패를 기다린다. */
function connect(auth: Record<string, unknown>): Promise<ClientSocket> {
  return new Promise((resolve, reject) => {
    const socket = ioClient(baseUrl, { auth, transports: ['websocket'], reconnection: false });
    openSockets.push(socket);
    socket.on('connect', () => resolve(socket));
    socket.on('connect_error', (err) => reject(err));
  });
}

/** 특정 이벤트가 올 때까지 기다린다. */
function waitFor<T>(socket: ClientSocket, event: string, timeoutMs = 4000): Promise<T> {
  return new Promise((resolve, reject) => {
    const timer = setTimeout(() => reject(new Error(`${event} 이벤트가 오지 않았습니다.`)), timeoutMs);
    socket.once(event, (payload: T) => {
      clearTimeout(timer);
      resolve(payload);
    });
  });
}

beforeAll(async () => {
  httpServer = createServer(createApp());
  createSocketServer(httpServer);
  await new Promise<void>((resolve) => httpServer.listen(0, resolve));
  const { port } = httpServer.address() as AddressInfo;
  baseUrl = `http://localhost:${port}`;
});

afterAll(async () => {
  await new Promise<void>((resolve) => httpServer.close(() => resolve()));
  await disconnectDatabase();
});

beforeEach(async () => {
  await resetDatabase();
});

afterEach(() => {
  while (openSockets.length > 0) openSockets.pop()?.disconnect();
});

describe('소켓 연결 인증', () => {
  it('토큰이 없으면 연결이 거부된다', async () => {
    await expect(connect({})).rejects.toThrow();
  });

  it('잘못된 visitorToken 은 연결이 거부된다', async () => {
    const { room } = await createCustomerWithRoom();

    await expect(connect({ roomId: room.id, visitorToken: '틀린토큰' })).rejects.toThrow();
  });

  it('올바른 visitorToken 이면 고객으로 연결된다', async () => {
    const { room, visitorToken } = await createCustomerWithRoom();

    const socket = await connect({ roomId: room.id, visitorToken });

    expect(socket.connected).toBe(true);
  });

  it('조작된 운영자 JWT 는 연결이 거부된다', async () => {
    await expect(connect({ operatorToken: 'not-a-real-jwt' })).rejects.toThrow();
  });

  it('올바른 운영자 JWT 면 운영자로 연결된다', async () => {
    const { operator } = await createOperator();
    const token = signOperatorToken({ operatorId: operator.id, role: 'operator' });

    const socket = await connect({ operatorToken: token });

    expect(socket.connected).toBe(true);
  });
});

describe('chat:join', () => {
  it('고객은 자기 상담방에 입장하고 기존 메시지를 받는다', async () => {
    const { room, visitorToken } = await createCustomerWithRoom();
    const socket = await connect({ roomId: room.id, visitorToken });

    socket.emit('chat:join', { roomId: room.id });
    const payload = await waitFor<{ roomId: string; status: string; messages: unknown[] }>(
      socket,
      'chat:joined'
    );

    expect(payload.roomId).toBe(room.id);
    expect(payload.status).toBe('bot');
    expect(Array.isArray(payload.messages)).toBe(true);
  });

  it('고객은 남의 상담방에 입장할 수 없다', async () => {
    const mine = await createCustomerWithRoom();
    const other = await createCustomerWithRoom();
    const socket = await connect({ roomId: mine.room.id, visitorToken: mine.visitorToken });

    socket.emit('chat:join', { roomId: other.room.id });
    const error = await waitFor<{ code: string }>(socket, 'chat:error');

    expect(error.code).toBe('FORBIDDEN');
  });

  it('운영자는 아무 상담방에나 입장할 수 있다', async () => {
    const { operator } = await createOperator();
    const { room } = await createCustomerWithRoom();
    const socket = await connect({
      operatorToken: signOperatorToken({ operatorId: operator.id, role: 'operator' })
    });

    socket.emit('chat:join', { roomId: room.id });
    const payload = await waitFor<{ roomId: string }>(socket, 'chat:joined');

    expect(payload.roomId).toBe(room.id);
  });
});
```

- [ ] **Step 3: 실패 확인**

```bash
npx vitest run tests/realtime.test.ts --root apps/api
```

기대: `Cannot find module '../src/realtime/socketServer'`.

- [ ] **Step 4: `apps/api/src/realtime/rooms.ts` 작성**

```ts
/**
 * 룸 이름은 반드시 이 함수로만 만든다.
 * 문자열을 손으로 조합하면 오타 하나로 브로드캐스트가 조용히 사라진다.
 */
export function roomChannel(roomId: string): string {
  return `room:${roomId}`;
}

/** 로그인한 운영자 전원이 들어가는 방. 신규 상담 알림과 목록 갱신에 쓴다. */
export const OPERATORS_CHANNEL = 'operators';
```

- [ ] **Step 5: `apps/api/src/realtime/authSocket.ts` 작성**

```ts
import type { Socket } from 'socket.io';
import type { OperatorRole } from '@stemcare/shared';
import { verifyOperatorToken } from '../auth/token';
import { hashVisitorToken } from '../auth/token';
import { config } from '../config';
import { prisma } from '../db';

export type SocketIdentity =
  | { kind: 'customer'; roomId: string; customerId: string }
  | { kind: 'operator'; operatorId: string; role: OperatorRole };

declare module 'socket.io' {
  interface Socket {
    data: { identity?: SocketIdentity };
  }
}

/** 쿠키 헤더 문자열에서 값 하나를 꺼낸다. (소켓 핸드셰이크에는 cookie-parser 가 없다) */
function readCookie(cookieHeader: string | undefined, name: string): string | null {
  if (!cookieHeader) return null;

  for (const part of cookieHeader.split(';')) {
    const [key, ...rest] = part.trim().split('=');
    if (key === name) return decodeURIComponent(rest.join('='));
  }
  return null;
}

/**
 * 연결 시점에 단 한 번 신원을 확인한다.
 * next(new Error(...)) 를 호출하면 연결 자체가 거부되고
 * 클라이언트는 'connect_error' 를 받는다.
 */
export async function authenticateSocket(
  socket: Socket,
  next: (err?: Error) => void
): Promise<void> {
  const auth = socket.handshake.auth ?? {};

  // 1) 운영자: auth.operatorToken 또는 쿠키
  const operatorToken =
    (typeof auth.operatorToken === 'string' ? auth.operatorToken : null) ??
    readCookie(socket.handshake.headers.cookie, config.adminCookieName);

  if (operatorToken) {
    const payload = verifyOperatorToken(operatorToken);
    if (!payload) {
      next(new Error('UNAUTHORIZED'));
      return;
    }

    const operator = await prisma.operator.findUnique({ where: { id: payload.operatorId } });
    if (!operator || !operator.isActive) {
      next(new Error('UNAUTHORIZED'));
      return;
    }

    socket.data.identity = { kind: 'operator', operatorId: operator.id, role: payload.role };
    next();
    return;
  }

  // 2) 고객: auth.roomId + auth.visitorToken
  const roomId = typeof auth.roomId === 'string' ? auth.roomId : null;
  const visitorToken = typeof auth.visitorToken === 'string' ? auth.visitorToken : null;

  if (!roomId || !visitorToken) {
    next(new Error('UNAUTHORIZED'));
    return;
  }

  const room = await prisma.chatRoom.findUnique({ where: { id: roomId } });
  if (!room || room.visitorTokenHash !== hashVisitorToken(visitorToken)) {
    next(new Error('UNAUTHORIZED'));
    return;
  }

  socket.data.identity = { kind: 'customer', roomId: room.id, customerId: room.customerId };
  next();
}
```

- [ ] **Step 6: `apps/api/src/realtime/socketServer.ts` 작성**

```ts
import type { Server as HttpServer } from 'node:http';
import { Server } from 'socket.io';
import { config } from '../config';
import { authenticateSocket } from './authSocket';
import { registerChatEvents } from './chatEvents';
import { OPERATORS_CHANNEL } from './rooms';

let io: Server | null = null;

export function createSocketServer(httpServer: HttpServer): Server {
  io = new Server(httpServer, {
    cors: {
      origin: config.corsOrigins,
      credentials: true
    },
    // 연결이 끊긴 걸 25초 안에 알아채도록 한다.
    pingInterval: 20_000,
    pingTimeout: 25_000
  });

  io.use((socket, next) => {
    void authenticateSocket(socket, next);
  });

  io.on('connection', (socket) => {
    const identity = socket.data.identity;
    if (!identity) {
      socket.disconnect(true);
      return;
    }

    // 운영자는 자동으로 공용 알림 방에 들어간다.
    if (identity.kind === 'operator') {
      void socket.join(OPERATORS_CHANNEL);
    }

    registerChatEvents(io!, socket);
  });

  return io;
}

/**
 * 라우터(HTTP) 쪽에서도 브로드캐스트해야 할 때 쓴다.
 * 소켓 서버가 아직 없으면 null 을 반환하므로, 호출부는 반드시 null 을 확인한다.
 * (테스트에서 소켓 없이 앱만 띄우는 경우가 있다)
 */
export function getIo(): Server | null {
  return io;
}
```

- [ ] **Step 7: `apps/api/src/realtime/chatEvents.ts` 최소 버전 작성**

Task 2에서 메시지 이벤트를 추가한다. 지금은 `chat:join` 만 만든다.

```ts
import type { Server, Socket } from 'socket.io';
import { z } from 'zod';
import { prisma } from '../db';
import { listMessages } from '../messages/messageService';
import { roomChannel } from './rooms';

const JoinSchema = z.object({ roomId: z.string().uuid() });

function emitError(socket: Socket, code: string, message: string): void {
  socket.emit('chat:error', { code, message });
}

export function registerChatEvents(io: Server, socket: Socket): void {
  socket.on('chat:join', async (raw: unknown) => {
    const parsed = JoinSchema.safeParse(raw);
    if (!parsed.success) {
      emitError(socket, 'VALIDATION_ERROR', '잘못된 요청입니다.');
      return;
    }

    const identity = socket.data.identity!;
    const { roomId } = parsed.data;

    // 고객은 연결할 때 확인한 자기 방에만 들어갈 수 있다.
    if (identity.kind === 'customer' && identity.roomId !== roomId) {
      emitError(socket, 'FORBIDDEN', '이 상담방에 접근할 권한이 없습니다.');
      return;
    }

    const room = await prisma.chatRoom.findUnique({ where: { id: roomId } });
    if (!room) {
      emitError(socket, 'NOT_FOUND', '상담방을 찾을 수 없습니다.');
      return;
    }

    await socket.join(roomChannel(roomId));

    const viewer = identity.kind === 'customer' ? 'customer' : 'operator';
    const messages = await listMessages(roomId, viewer);

    socket.emit('chat:joined', {
      roomId: room.id,
      status: room.status,
      assignedOperatorId: room.assignedOperatorId,
      messages
    });
  });
}
```

- [ ] **Step 8: `apps/api/src/server.ts` 수정**

Express 앱을 직접 `listen` 하는 대신, `http.Server` 를 만들고 거기에 Express와 Socket.IO를 함께 얹는다.

```ts
import { createServer } from 'node:http';
import { createApp } from './app';
import { config } from './config';
import { createSocketServer } from './realtime/socketServer';

const app = createApp();
const httpServer = createServer(app);

createSocketServer(httpServer);

httpServer.listen(config.port, () => {
  console.log(`[api] http + socket.io listening on http://localhost:${config.port} (${config.nodeEnv})`);
});
```

- [ ] **Step 9: 테스트 통과 확인**

```bash
npx vitest run tests/realtime.test.ts --root apps/api
```

기대: 8 passed.

- [ ] **Step 10: 커밋**

```bash
git add apps/api/src/realtime apps/api/src/server.ts apps/api/tests/realtime.test.ts apps/api/package.json package-lock.json
git commit -m "feat: attach socket.io server with connection auth and room join"
```

---

## Task 2: 메시지 실시간 송수신과 중복 방지

**Files:**
- Modify: `apps/api/src/realtime/chatEvents.ts`
- Create: `apps/api/src/realtime/emitters.ts`
- Modify: `apps/api/tests/realtime.test.ts`

**Interfaces:**
- Consumes: `createMessage`, `listMessages` (Phase 1), `authorizeVisitor`, `assertRoomOpen`
- Produces:
  - `emitters.ts` → `broadcastMessage(io, roomId, row)`, `broadcastStatus(io, roomId, status, assignedOperatorId)`, `notifyOperators(io, payload)`
    (Task 6 에서 `broadcastMessage` 는 읽음 처리를 위해 `Promise<void>` 를 반환하도록 바뀐다)
  - 소켓 이벤트 (클라 → 서버): `chat:message`, `chat:typing`, `chat:handoff-request`
  - 소켓 이벤트 (서버 → 클라): `chat:message`, `chat:message:ack`, `chat:typing`, `chat:status`, `chat:error`, `rooms:updated`

---

- [ ] **Step 1: 테스트 추가**

`apps/api/tests/realtime.test.ts` 파일 끝에 덧붙인다.

```ts
describe('chat:message', () => {
  it('고객 메시지가 저장되고 같은 방의 운영자에게 전달된다', async () => {
    const { operator } = await createOperator();
    const { room, visitorToken } = await createCustomerWithRoom();

    const customerSocket = await connect({ roomId: room.id, visitorToken });
    const operatorSocket = await connect({
      operatorToken: signOperatorToken({ operatorId: operator.id, role: 'operator' })
    });

    customerSocket.emit('chat:join', { roomId: room.id });
    await waitFor(customerSocket, 'chat:joined');
    operatorSocket.emit('chat:join', { roomId: room.id });
    await waitFor(operatorSocket, 'chat:joined');

    const received = waitFor<{ message: { visibleText: string; senderType: string } }>(
      operatorSocket,
      'chat:message'
    );
    customerSocket.emit('chat:message', {
      roomId: room.id,
      text: 'ホテルの予約もお願いできますか。',
      clientMessageId: 'c-1'
    });

    const payload = await received;
    expect(payload.message.senderType).toBe('customer');
    expect(payload.message.visibleText).toBe('ホテルの予約もお願いできますか。');
  });

  it('보낸 사람은 ack 를 받는다', async () => {
    const { room, visitorToken } = await createCustomerWithRoom();
    const socket = await connect({ roomId: room.id, visitorToken });
    socket.emit('chat:join', { roomId: room.id });
    await waitFor(socket, 'chat:joined');

    const ack = waitFor<{ clientMessageId: string; message: { id: string } }>(
      socket,
      'chat:message:ack'
    );
    socket.emit('chat:message', { roomId: room.id, text: '안녕하세요', clientMessageId: 'c-2' });

    const payload = await ack;
    expect(payload.clientMessageId).toBe('c-2');
    expect(payload.message.id).toEqual(expect.any(String));
  });

  it('같은 clientMessageId 를 두 번 보내도 메시지는 하나만 저장된다', async () => {
    const { prisma } = await import('../src/db');
    const { room, visitorToken } = await createCustomerWithRoom();
    const socket = await connect({ roomId: room.id, visitorToken });
    socket.emit('chat:join', { roomId: room.id });
    await waitFor(socket, 'chat:joined');

    socket.emit('chat:message', { roomId: room.id, text: '중복', clientMessageId: 'dup' });
    await waitFor(socket, 'chat:message:ack');
    socket.emit('chat:message', { roomId: room.id, text: '중복', clientMessageId: 'dup' });
    await waitFor(socket, 'chat:message:ack');

    expect(await prisma.message.count({ where: { chatRoomId: room.id } })).toBe(1);
  });

  it('빈 메시지는 오류를 돌려준다', async () => {
    const { room, visitorToken } = await createCustomerWithRoom();
    const socket = await connect({ roomId: room.id, visitorToken });
    socket.emit('chat:join', { roomId: room.id });
    await waitFor(socket, 'chat:joined');

    socket.emit('chat:message', { roomId: room.id, text: '   ', clientMessageId: 'e-1' });
    const error = await waitFor<{ code: string }>(socket, 'chat:error');

    expect(error.code).toBe('VALIDATION_ERROR');
  });

  it('종료된 상담에는 메시지를 보낼 수 없다', async () => {
    const { room, visitorToken } = await createCustomerWithRoom({ status: 'closed' });
    const socket = await connect({ roomId: room.id, visitorToken });
    socket.emit('chat:join', { roomId: room.id });
    await waitFor(socket, 'chat:joined');

    socket.emit('chat:message', { roomId: room.id, text: '종료 후', clientMessageId: 'c-3' });
    const error = await waitFor<{ code: string }>(socket, 'chat:error');

    expect(error.code).toBe('ROOM_CLOSED');
  });

  it('다른 상담방으로는 메시지를 보낼 수 없다', async () => {
    const mine = await createCustomerWithRoom();
    const other = await createCustomerWithRoom();
    const socket = await connect({ roomId: mine.room.id, visitorToken: mine.visitorToken });
    socket.emit('chat:join', { roomId: mine.room.id });
    await waitFor(socket, 'chat:joined');

    socket.emit('chat:message', { roomId: other.room.id, text: '침입', clientMessageId: 'x-1' });
    const error = await waitFor<{ code: string }>(socket, 'chat:error');

    expect(error.code).toBe('FORBIDDEN');
  });

  it('고객에게 가는 메시지에는 운영자 내부 번역 정보가 없다', async () => {
    const { operator } = await createOperator();
    const { room, visitorToken } = await createCustomerWithRoom();

    const customerSocket = await connect({ roomId: room.id, visitorToken });
    const operatorSocket = await connect({
      operatorToken: signOperatorToken({ operatorId: operator.id, role: 'operator' })
    });
    customerSocket.emit('chat:join', { roomId: room.id });
    await waitFor(customerSocket, 'chat:joined');
    operatorSocket.emit('chat:join', { roomId: room.id });
    await waitFor(operatorSocket, 'chat:joined');

    const customerReceives = waitFor<{ message: { translatedText: string | null } }>(
      customerSocket,
      'chat:message'
    );
    operatorSocket.emit('chat:message', {
      roomId: room.id,
      text: '방문 예정일을 알려주세요.',
      clientMessageId: 'op-1'
    });

    const payload = await customerReceives;
    expect(payload.message.translatedText).toBeNull();
  });
});

describe('chat:handoff-request', () => {
  it('고객이 운영자 연결을 요청하면 상태가 waiting 이 된다', async () => {
    const { prisma } = await import('../src/db');
    const { room, visitorToken } = await createCustomerWithRoom({ status: 'bot' });
    const socket = await connect({ roomId: room.id, visitorToken });
    socket.emit('chat:join', { roomId: room.id });
    await waitFor(socket, 'chat:joined');

    const statusEvent = waitFor<{ status: string }>(socket, 'chat:status');
    socket.emit('chat:handoff-request', { roomId: room.id, reason: 'customer_requested' });

    expect((await statusEvent).status).toBe('waiting');
    const updated = await prisma.chatRoom.findUniqueOrThrow({ where: { id: room.id } });
    expect(updated.status).toBe('waiting');
  });
});

describe('chat:typing', () => {
  it('입력 중 표시가 상대에게만 전달된다', async () => {
    const { operator } = await createOperator();
    const { room, visitorToken } = await createCustomerWithRoom();

    const customerSocket = await connect({ roomId: room.id, visitorToken });
    const operatorSocket = await connect({
      operatorToken: signOperatorToken({ operatorId: operator.id, role: 'operator' })
    });
    customerSocket.emit('chat:join', { roomId: room.id });
    await waitFor(customerSocket, 'chat:joined');
    operatorSocket.emit('chat:join', { roomId: room.id });
    await waitFor(operatorSocket, 'chat:joined');

    const typing = waitFor<{ from: string; isTyping: boolean }>(operatorSocket, 'chat:typing');
    customerSocket.emit('chat:typing', { roomId: room.id, isTyping: true });

    const payload = await typing;
    expect(payload.from).toBe('customer');
    expect(payload.isTyping).toBe(true);
  });
});
```

- [ ] **Step 2: 실패 확인**

```bash
npx vitest run tests/realtime.test.ts --root apps/api
```

- [ ] **Step 3: `apps/api/src/realtime/emitters.ts` 작성**

```ts
import type { Message } from '@prisma/client';
import type { ChatRoomStatus } from '@stemcare/shared';
import type { Server } from 'socket.io';
import { toMessageDTO } from '../messages/messageMapper';
import { OPERATORS_CHANNEL, roomChannel } from './rooms';

/**
 * 같은 메시지를 고객과 운영자에게 "다른 모양으로" 보낸다.
 *
 * 고객에게 운영자용 DTO 를 그대로 보내면 내부 번역 정보와
 * 운영자가 수정하기 전의 원문이 노출된다. 반드시 나눠 보낸다.
 */
export function broadcastMessage(io: Server, roomId: string, row: Message): void {
  const channel = roomChannel(roomId);

  for (const [socketId, socket] of io.sockets.sockets) {
    if (!socket.rooms.has(channel)) continue;

    const identity = socket.data.identity;
    if (!identity) continue;

    const viewer = identity.kind === 'customer' ? 'customer' : 'operator';
    socket.emit('chat:message', { message: toMessageDTO(row, viewer) });
    void socketId;
  }

  // 운영자 목록(사이드바)도 갱신이 필요하다.
  io.to(OPERATORS_CHANNEL).emit('rooms:updated', { roomId });
}

export function broadcastStatus(
  io: Server,
  roomId: string,
  status: ChatRoomStatus,
  assignedOperatorId: string | null
): void {
  io.to(roomChannel(roomId)).emit('chat:status', { roomId, status, assignedOperatorId });
  io.to(OPERATORS_CHANNEL).emit('rooms:updated', { roomId });
}

/** 신규 상담 접수 등, 방에 들어가 있지 않은 운영자 전원에게 알린다. */
export function notifyOperators(io: Server, payload: { roomId: string; kind: string }): void {
  io.to(OPERATORS_CHANNEL).emit('rooms:new', payload);
  io.to(OPERATORS_CHANNEL).emit('rooms:updated', { roomId: payload.roomId });
}
```

- [ ] **Step 4: `messageService.ts` 가 저장된 원본 row 도 돌려주도록 수정**

`broadcastMessage`는 `Message` row가 필요한데 현재 `createMessage`는 DTO만 반환한다. 함수를 하나 추가한다.

`apps/api/src/messages/messageService.ts` 에 다음을 추가한다.

```ts
import type { Message } from '@prisma/client';

/**
 * createMessage 와 같은 저장 로직을 쓰되, DB row 를 그대로 돌려준다.
 * 소켓 브로드캐스트는 고객용/운영자용 DTO 를 각각 만들어야 하므로 row 가 필요하다.
 */
export async function createMessageRow(input: CreateMessageInput): Promise<Message> {
  const visibleText = input.visibleText ?? input.text;

  try {
    const [message] = await prisma.$transaction([
      prisma.message.create({
        data: {
          chatRoomId: input.chatRoomId,
          senderType: input.senderType,
          senderId: input.senderId ?? null,
          messageType: 'text',
          originalLanguage: input.originalLanguage ?? 'unknown',
          originalText: input.text,
          translatedLanguage: input.translatedLanguage ?? null,
          translatedText: input.translatedText ?? null,
          visibleText,
          translationStatus: input.translationStatus ?? 'none',
          clientMessageId: input.clientMessageId ?? null
        }
      }),
      prisma.chatRoom.update({
        where: { id: input.chatRoomId },
        data: { lastMessageAt: new Date() }
      })
    ]);

    return message;
  } catch (err) {
    if (err instanceof Prisma.PrismaClientKnownRequestError && err.code === 'P2002' && input.clientMessageId) {
      return prisma.message.findFirstOrThrow({
        where: { chatRoomId: input.chatRoomId, clientMessageId: input.clientMessageId }
      });
    }
    throw err;
  }
}
```

그리고 기존 `createMessage` 를 이 함수를 쓰도록 간단히 바꾼다.

```ts
export async function createMessage(input: CreateMessageInput): Promise<MessageDTO> {
  const row = await createMessageRow(input);
  return toMessageDTO(row, input.viewer ?? 'operator');
}
```

이렇게 하면 저장 로직이 여전히 한 곳(`createMessageRow`)에만 있다.

- [ ] **Step 5: `apps/api/src/realtime/chatEvents.ts` 확장**

기존 `chat:join` 아래에 이벤트들을 추가한다. 파일 전체는 다음과 같다.

```ts
import type { Server, Socket } from 'socket.io';
import { z } from 'zod';
import { AppError } from '../common/errors';
import { prisma } from '../db';
import { createMessageRow, listMessages } from '../messages/messageService';
import { broadcastMessage, broadcastStatus } from './emitters';
import { roomChannel } from './rooms';

const JoinSchema = z.object({ roomId: z.string().uuid() });

const MessageSchema = z.object({
  roomId: z.string().uuid(),
  text: z.string().trim().min(1).max(2000),
  clientMessageId: z.string().min(1).max(100)
});

const TypingSchema = z.object({ roomId: z.string().uuid(), isTyping: z.boolean() });

const HandoffSchema = z.object({
  roomId: z.string().uuid(),
  reason: z.string().max(100).optional()
});

function emitError(socket: Socket, code: string, message: string): void {
  socket.emit('chat:error', { code, message });
}

/** 이 소켓이 해당 상담방을 다룰 권한이 있는지 확인한다. */
function assertRoomAccess(socket: Socket, roomId: string): void {
  const identity = socket.data.identity!;
  if (identity.kind === 'customer' && identity.roomId !== roomId) {
    throw new AppError(403, 'FORBIDDEN', '이 상담방에 접근할 권한이 없습니다.');
  }
}

export function registerChatEvents(io: Server, socket: Socket): void {
  const identity = socket.data.identity!;

  /** 모든 핸들러를 감싸 오류를 chat:error 로 바꿔준다. */
  function handle<T>(schema: z.ZodSchema<T>, fn: (input: T) => Promise<void>) {
    return async (raw: unknown) => {
      const parsed = schema.safeParse(raw);
      if (!parsed.success) {
        emitError(socket, 'VALIDATION_ERROR', '잘못된 요청입니다.');
        return;
      }

      try {
        await fn(parsed.data);
      } catch (err) {
        if (err instanceof AppError) {
          emitError(socket, err.code, err.message);
          return;
        }
        console.error('[socket] unhandled', err);
        emitError(socket, 'INTERNAL_ERROR', '처리 중 문제가 발생했습니다.');
      }
    };
  }

  socket.on(
    'chat:join',
    handle(JoinSchema, async ({ roomId }) => {
      assertRoomAccess(socket, roomId);

      const room = await prisma.chatRoom.findUnique({ where: { id: roomId } });
      if (!room) throw new AppError(404, 'NOT_FOUND', '상담방을 찾을 수 없습니다.');

      await socket.join(roomChannel(roomId));

      const viewer = identity.kind === 'customer' ? 'customer' : 'operator';
      socket.emit('chat:joined', {
        roomId: room.id,
        status: room.status,
        assignedOperatorId: room.assignedOperatorId,
        messages: await listMessages(roomId, viewer)
      });
    })
  );

  socket.on(
    'chat:message',
    handle(MessageSchema, async ({ roomId, text, clientMessageId }) => {
      assertRoomAccess(socket, roomId);

      const room = await prisma.chatRoom.findUnique({ where: { id: roomId } });
      if (!room) throw new AppError(404, 'NOT_FOUND', '상담방을 찾을 수 없습니다.');
      if (room.status === 'closed') {
        throw new AppError(409, 'ROOM_CLOSED', '종료된 상담입니다.');
      }

      let originalLanguage = 'unknown';
      let senderId: string | null = null;

      if (identity.kind === 'customer') {
        const customer = await prisma.customer.findUniqueOrThrow({ where: { id: identity.customerId } });
        originalLanguage = customer.preferredLanguage;
        senderId = customer.id;
      } else {
        // 운영자 기본 작성 언어는 한국어다. (Phase 5 에서 자동 감지로 바뀐다)
        originalLanguage = 'ko';
        senderId = identity.operatorId;
      }

      const row = await createMessageRow({
        chatRoomId: roomId,
        senderType: identity.kind === 'customer' ? 'customer' : 'operator',
        senderId,
        text,
        originalLanguage: originalLanguage as 'ko' | 'ja' | 'unknown',
        clientMessageId
      });

      // 방 전체에 알린다. (보낸 사람도 포함 — 다른 기기/탭에서 보고 있을 수 있다)
      broadcastMessage(io, roomId, row);

      // 보낸 사람에게는 "네가 보낸 그 메시지가 이거다"를 따로 알려준다.
      const viewer = identity.kind === 'customer' ? 'customer' : 'operator';
      const { toMessageDTO } = await import('../messages/messageMapper');
      socket.emit('chat:message:ack', {
        clientMessageId,
        message: toMessageDTO(row, viewer)
      });
    })
  );

  socket.on(
    'chat:typing',
    handle(TypingSchema, async ({ roomId, isTyping }) => {
      assertRoomAccess(socket, roomId);

      // socket.to(...) 는 "나를 제외한" 방 참가자에게 보낸다.
      socket.to(roomChannel(roomId)).emit('chat:typing', {
        roomId,
        from: identity.kind,
        isTyping
      });
    })
  );

  socket.on(
    'chat:handoff-request',
    handle(HandoffSchema, async ({ roomId }) => {
      assertRoomAccess(socket, roomId);

      const room = await prisma.chatRoom.findUnique({ where: { id: roomId } });
      if (!room) throw new AppError(404, 'NOT_FOUND', '상담방을 찾을 수 없습니다.');
      if (room.status === 'closed') throw new AppError(409, 'ROOM_CLOSED', '종료된 상담입니다.');

      // 이미 운영자가 붙어 있으면 아무것도 하지 않는다.
      if (room.status === 'active') return;

      await prisma.chatRoom.update({ where: { id: roomId }, data: { status: 'waiting' } });

      const notice = await createMessageRow({
        chatRoomId: roomId,
        senderType: 'system',
        text: '담당자 연결을 요청했습니다. 잠시만 기다려주세요.',
        visibleText: '담당자 연결을 요청했습니다. 잠시만 기다려주세요.',
        originalLanguage: 'ko'
      });

      broadcastMessage(io, roomId, notice);
      broadcastStatus(io, roomId, 'waiting', room.assignedOperatorId);
    })
  );

  socket.on('disconnect', () => {
    // Socket.IO 가 룸에서 자동으로 빼주므로 별도 정리는 필요 없다.
  });
}
```

- [ ] **Step 6: 테스트 통과 확인**

```bash
npx vitest run tests/realtime.test.ts --root apps/api
```

기대: 18 passed.

- [ ] **Step 7: 커밋**

```bash
git add apps/api/src apps/api/tests/realtime.test.ts
git commit -m "feat: add realtime message, typing and handoff socket events"
```

---

## Task 3: HTTP 변경도 실시간으로 알리기

**Files:**
- Modify: `apps/api/src/chatRooms/adminChatRoutes.ts`
- Modify: `apps/api/src/chatRooms/publicChatRoutes.ts`
- Modify: `apps/api/src/chatRooms/chatRoomService.ts`
- Test: `apps/api/tests/realtimeBridge.test.ts`

**Interfaces:**
- Consumes: `getIo()` (Task 1), `emitters.ts` (Task 2)
- Produces: HTTP로 일어난 변경(배정·상태변경·운영자 메시지·신규 상담)이 소켓으로도 전파된다

---

- [ ] **Step 1: 문제 이해**

운영자가 관리자 화면에서 `내가 상담하기` 를 누르면 HTTP `PATCH` 가 간다. 이건 소켓을 거치지 않으므로, 고객 화면은 폴링 없이는 상태 변화를 모른다. 그래서 **HTTP 라우터에서도 브로드캐스트를 호출**해야 한다.

- [ ] **Step 2: 테스트 작성**

`apps/api/tests/realtimeBridge.test.ts`

```ts
import { createServer, type Server as HttpServer } from 'node:http';
import type { AddressInfo } from 'node:net';
import request from 'supertest';
import { io as ioClient, type Socket as ClientSocket } from 'socket.io-client';
import { afterAll, afterEach, beforeAll, beforeEach, describe, expect, it } from 'vitest';
import { createApp } from '../src/app';
import { createSocketServer } from '../src/realtime/socketServer';
import { createCustomerWithRoom, createOperator } from './helpers/factories';
import { disconnectDatabase, resetDatabase } from './helpers/testDb';

let httpServer: HttpServer;
let baseUrl: string;
const app = createApp();
const openSockets: ClientSocket[] = [];

function connect(auth: Record<string, unknown>): Promise<ClientSocket> {
  return new Promise((resolve, reject) => {
    const socket = ioClient(baseUrl, { auth, transports: ['websocket'], reconnection: false });
    openSockets.push(socket);
    socket.on('connect', () => resolve(socket));
    socket.on('connect_error', reject);
  });
}

function waitFor<T>(socket: ClientSocket, event: string, timeoutMs = 4000): Promise<T> {
  return new Promise((resolve, reject) => {
    const timer = setTimeout(() => reject(new Error(`${event} 없음`)), timeoutMs);
    socket.once(event, (payload: T) => {
      clearTimeout(timer);
      resolve(payload);
    });
  });
}

beforeAll(async () => {
  httpServer = createServer(app);
  createSocketServer(httpServer);
  await new Promise<void>((resolve) => httpServer.listen(0, resolve));
  baseUrl = `http://localhost:${(httpServer.address() as AddressInfo).port}`;
});

afterAll(async () => {
  await new Promise<void>((resolve) => httpServer.close(() => resolve()));
  await disconnectDatabase();
});

beforeEach(async () => {
  await resetDatabase();
});

afterEach(() => {
  while (openSockets.length > 0) openSockets.pop()?.disconnect();
});

describe('HTTP → 소켓 전파', () => {
  it('운영자가 HTTP 로 배정하면 고객 소켓이 상태 변경을 받는다', async () => {
    const { plainPassword } = await createOperator({ email: 'bridge@stemcare.test' });
    const { room, visitorToken } = await createCustomerWithRoom({ status: 'waiting' });

    const customerSocket = await connect({ roomId: room.id, visitorToken });
    customerSocket.emit('chat:join', { roomId: room.id });
    await waitFor(customerSocket, 'chat:joined');

    const agent = request.agent(baseUrl);
    await agent
      .post('/api/admin/auth/login')
      .send({ email: 'bridge@stemcare.test', password: plainPassword });

    const statusEvent = waitFor<{ status: string }>(customerSocket, 'chat:status');
    await agent.patch(`/api/admin/chat-rooms/${room.id}/assign`).expect(200);

    expect((await statusEvent).status).toBe('active');
  });

  it('운영자가 HTTP 로 보낸 메시지가 고객 소켓에 도착한다', async () => {
    const { plainPassword } = await createOperator({ email: 'msg@stemcare.test' });
    const { room, visitorToken } = await createCustomerWithRoom({ status: 'active' });

    const customerSocket = await connect({ roomId: room.id, visitorToken });
    customerSocket.emit('chat:join', { roomId: room.id });
    await waitFor(customerSocket, 'chat:joined');

    const agent = request.agent(baseUrl);
    await agent.post('/api/admin/auth/login').send({ email: 'msg@stemcare.test', password: plainPassword });

    const received = waitFor<{ message: { visibleText: string } }>(customerSocket, 'chat:message');
    await agent
      .post(`/api/admin/chat-rooms/${room.id}/messages`)
      .send({ originalText: '방문 예정일을 알려주세요.', originalLanguage: 'ko' })
      .expect(201);

    expect((await received).message.visibleText).toBe('방문 예정일을 알려주세요.');
  });

  it('새 상담이 접수되면 운영자에게 rooms:new 가 간다', async () => {
    const { operator } = await createOperator();
    const { signOperatorToken } = await import('../src/auth/token');
    const operatorSocket = await connect({
      operatorToken: signOperatorToken({ operatorId: operator.id, role: 'operator' })
    });

    const notified = waitFor<{ roomId: string }>(operatorSocket, 'rooms:new');
    await request(baseUrl)
      .post('/api/public/chat/start')
      .send({
        name: '신규 고객',
        phone: '010-1111-2222',
        preferredLanguage: 'ko',
        serviceType: 'korea_travel',
        privacyAgreed: true
      })
      .expect(201);

    expect((await notified).roomId).toEqual(expect.any(String));
  });

  it('고객이 HTTP 로 보낸 메시지도 운영자 소켓에 도착한다', async () => {
    const { operator } = await createOperator();
    const { signOperatorToken } = await import('../src/auth/token');
    const { room, visitorToken } = await createCustomerWithRoom();

    const operatorSocket = await connect({
      operatorToken: signOperatorToken({ operatorId: operator.id, role: 'operator' })
    });
    operatorSocket.emit('chat:join', { roomId: room.id });
    await waitFor(operatorSocket, 'chat:joined');

    const received = waitFor<{ message: { visibleText: string } }>(operatorSocket, 'chat:message');
    await request(baseUrl)
      .post(`/api/public/chat/${room.id}/messages`)
      .set('X-Visitor-Token', visitorToken)
      .send({ text: 'HTTP 폴백 메시지' })
      .expect(201);

    expect((await received).message.visibleText).toBe('HTTP 폴백 메시지');
  });
});
```

- [ ] **Step 3: 실패 확인**

```bash
npx vitest run tests/realtimeBridge.test.ts --root apps/api
```

- [ ] **Step 4: `chatRoomService.ts` 에 브로드캐스트 연결**

파일 상단에 import를 추가한다.

```ts
import { broadcastMessage, broadcastStatus, notifyOperators } from '../realtime/emitters';
import { getIo } from '../realtime/socketServer';
```

`startChat` 의 마지막 `return` 직전에 추가한다.

```ts
  const io = getIo();
  if (io) {
    notifyOperators(io, { roomId: room.id, kind: 'chat_started' });
  }
```

`assignRoom` 의 `return getRoomDetail(roomId);` 직전에 추가한다.

```ts
  const io = getIo();
  if (io) {
    if (isNewAssignment) {
      const notice = await prisma.message.findFirstOrThrow({
        where: { chatRoomId: roomId, senderType: 'system' },
        orderBy: { createdAt: 'desc' }
      });
      broadcastMessage(io, roomId, notice);
    }
    broadcastStatus(io, roomId, 'active', operatorId);
  }
```

`updateRoomStatus` 의 `return getRoomDetail(roomId);` 직전에 추가한다.

```ts
  const io = getIo();
  if (io) {
    if (status === 'closed') {
      const notice = await prisma.message.findFirstOrThrow({
        where: { chatRoomId: roomId, senderType: 'system' },
        orderBy: { createdAt: 'desc' }
      });
      broadcastMessage(io, roomId, notice);
    }
    broadcastStatus(io, roomId, status, room.assignedOperatorId);
  }
```

`getIo()` 가 `null` 일 수 있으므로 항상 확인한다. 소켓 없이 앱만 띄우는 테스트가 있기 때문이다.

- [ ] **Step 5: 메시지 라우터에 브로드캐스트 연결**

`publicChatRoutes.ts` 의 `POST /:roomId/messages` 핸들러에서 `createMessage` 대신 `createMessageRow` 를 쓰고 브로드캐스트한다.

```ts
    const row = await createMessageRow({
      chatRoomId: room.id,
      senderType: 'customer',
      senderId: customer.id,
      text: body.text,
      originalLanguage: customer.preferredLanguage as 'ko' | 'ja',
      clientMessageId: body.clientMessageId ?? null
    });

    const io = getIo();
    if (io) broadcastMessage(io, room.id, row);

    res.status(201).json({ message: toMessageDTO(row, 'customer') });
```

import를 추가한다.
```ts
import { createMessageRow } from '../messages/messageService';
import { toMessageDTO } from '../messages/messageMapper';
import { broadcastMessage } from '../realtime/emitters';
import { getIo } from '../realtime/socketServer';
```

`adminChatRoutes.ts` 의 `POST /:roomId/messages` 도 같은 방식으로 바꾼다.

```ts
    const row = await createMessageRow({
      chatRoomId: room.id,
      senderType: 'operator',
      senderId: req.operator!.operatorId,
      text: body.originalText,
      originalLanguage: body.originalLanguage,
      translatedText: body.translatedText ?? null,
      translatedLanguage: body.translatedLanguage ?? null,
      visibleText: hasTranslation ? body.translatedText : body.originalText,
      translationStatus: hasTranslation ? 'done' : 'none',
      clientMessageId: body.clientMessageId ?? null
    });

    const io = getIo();
    if (io) broadcastMessage(io, room.id, row);

    res.status(201).json({ message: toMessageDTO(row, 'operator') });
```

- [ ] **Step 6: 테스트 통과 확인**

```bash
npm run test -w apps/api
```

기대: 모든 파일 통과. 특히 `realtimeBridge.test.ts` 4 passed.

- [ ] **Step 7: 커밋**

```bash
git add apps/api/src apps/api/tests/realtimeBridge.test.ts
git commit -m "feat: broadcast http-triggered chat changes over socket.io"
```

---

## Task 4: 고객 위젯을 실시간으로 전환

**Files:**
- Create: `js/chat/socket.js`
- Create: `js/chat/stream.js`
- Modify: `js/chat/widget.js`
- Modify: `js/chat/state.js`
- Modify: `index.html`, `stemcell/index.html`, `korea-travel/index.html`

**Interfaces:**
- Consumes: Task 1~3 서버 이벤트, `state.js` (Phase 2)
- Produces:
  - `socket.js` → `connectSocket({ onFallback }): Promise<Socket>`, `disconnectSocket()`, `sendViaSocket({ text, clientMessageId }): boolean`, `sendTyping(isTyping)`, `requestHandoff()`
  - `stream.js` → `startStream({ onFatalError })`, `stopStream()`, `sendMessage({ text, clientMessageId })`, `isRealtime(): boolean`

---

- [ ] **Step 1: 세 HTML 페이지에 Socket.IO CDN 추가**

`js/chat/widget.js` 스크립트 태그 **앞에** 넣는다.

```html
  <script src="https://cdn.socket.io/4.7.5/socket.io.min.js"></script>
```

명령으로 처리한다.

```bash
sed -i '' 's#<script type="module" src="js/chat/widget.js"></script>#<script src="https://cdn.socket.io/4.7.5/socket.io.min.js"></script>\n  <script type="module" src="js/chat/widget.js"></script>#' index.html
sed -i '' 's#<script type="module" src="../js/chat/widget.js"></script>#<script src="https://cdn.socket.io/4.7.5/socket.io.min.js"></script>\n  <script type="module" src="../js/chat/widget.js"></script>#' stemcell/index.html korea-travel/index.html
grep -n "socket.io.min.js" index.html stemcell/index.html korea-travel/index.html
```

CDN 스크립트는 전역 `window.io` 를 만든다. ES module 안에서 `import` 하지 않고 `window.io` 로 접근한다.

- [ ] **Step 2: `js/chat/state.js` 에 상태 추가**

기존 `state` 객체에 다음 필드를 추가한다.

```js
  transport: 'none',    // 'none' | 'socket' | 'polling'
  peerTyping: false
```

그리고 파일 끝에 setter를 추가한다.

```js
export function setTransport(transport) {
  if (state.transport === transport) return;
  state.transport = transport;
  notify();
}

export function setPeerTyping(isTyping) {
  if (state.peerTyping === isTyping) return;
  state.peerTyping = isTyping;
  notify();
}

export function setRoomStatus(status) {
  if (!state.room) return;
  state.room = { ...state.room, status };
  notify();
}
```

- [ ] **Step 3: `js/chat/socket.js` 작성**

```js
import { API_BASE } from './config.js';
import {
  getSession,
  resolvePendingMessage,
  setConnection,
  setPeerTyping,
  setRoomStatus,
  setTransport,
  upsertMessages
} from './state.js';

let socket = null;
let typingTimer = null;

/** 소켓 연결이 이 시간 안에 안 되면 폴링으로 돌아간다. */
const CONNECT_TIMEOUT_MS = 5000;

/**
 * Socket.IO 로 상담방에 연결한다.
 * 실패하면 reject 하므로, 호출한 쪽에서 폴링으로 대체한다.
 */
export function connectSocket() {
  const session = getSession();
  if (!session) return Promise.reject(new Error('세션이 없습니다.'));

  // CDN 스크립트가 로드되지 않은 경우 (차단, 오프라인 등)
  if (typeof window.io !== 'function') {
    return Promise.reject(new Error('socket.io 를 불러오지 못했습니다.'));
  }

  return new Promise((resolve, reject) => {
    const timer = window.setTimeout(() => {
      socket?.disconnect();
      socket = null;
      reject(new Error('소켓 연결 시간 초과'));
    }, CONNECT_TIMEOUT_MS);

    socket = window.io(API_BASE, {
      auth: { roomId: session.roomId, visitorToken: session.visitorToken },
      // websocket 이 막힌 환경을 위해 polling transport 도 허용한다.
      transports: ['websocket', 'polling'],
      reconnectionAttempts: Infinity,
      reconnectionDelay: 1000,
      reconnectionDelayMax: 8000
    });

    socket.on('connect', () => {
      window.clearTimeout(timer);
      setConnection('ok');
      setTransport('socket');
      socket.emit('chat:join', { roomId: session.roomId });
    });

    socket.on('connect_error', (err) => {
      window.clearTimeout(timer);
      // 이미 한 번 연결된 뒤의 오류는 자동 재연결에 맡긴다.
      if (socket?.active) {
        setConnection('reconnecting');
        return;
      }
      socket?.disconnect();
      socket = null;
      reject(err);
    });

    socket.on('chat:joined', (payload) => {
      setRoomStatus(payload.status);
      upsertMessages(payload.messages);
      resolve(socket);
    });

    socket.on('chat:message', (payload) => {
      upsertMessages([payload.message]);
    });

    socket.on('chat:message:ack', (payload) => {
      resolvePendingMessage(payload.clientMessageId, payload.message);
    });

    socket.on('chat:status', (payload) => {
      setRoomStatus(payload.status);
    });

    socket.on('chat:typing', (payload) => {
      // 운영자가 입력 중일 때만 고객에게 보여준다.
      if (payload.from === 'operator') setPeerTyping(payload.isTyping);
    });

    socket.on('chat:error', (payload) => {
      console.warn('[chat] 서버 오류:', payload.code);
    });

    socket.on('disconnect', (reason) => {
      setConnection('reconnecting');
      // 서버가 의도적으로 끊은 경우는 자동 재연결하지 않는다.
      if (reason === 'io server disconnect') socket?.connect();
    });

    socket.io.on('reconnect', () => {
      setConnection('ok');
      // 끊긴 동안 놓친 메시지를 받기 위해 다시 join 한다.
      socket.emit('chat:join', { roomId: session.roomId });
    });
  });
}

export function disconnectSocket() {
  socket?.disconnect();
  socket = null;
  setTransport('none');
}

export function isSocketConnected() {
  return Boolean(socket?.connected);
}

export function sendViaSocket({ text, clientMessageId }) {
  const session = getSession();
  if (!session || !socket?.connected) return false;

  socket.emit('chat:message', { roomId: session.roomId, text, clientMessageId });
  return true;
}

/** 입력 중 신호. 너무 자주 보내지 않도록 1.5초 뒤 자동으로 끈다. */
export function sendTyping(isTyping) {
  const session = getSession();
  if (!session || !socket?.connected) return;

  socket.emit('chat:typing', { roomId: session.roomId, isTyping });

  window.clearTimeout(typingTimer);
  if (isTyping) {
    typingTimer = window.setTimeout(() => {
      socket?.emit('chat:typing', { roomId: session.roomId, isTyping: false });
    }, 1500);
  }
}

export function requestHandoffViaSocket() {
  const session = getSession();
  if (!session || !socket?.connected) return false;

  socket.emit('chat:handoff-request', { roomId: session.roomId, reason: 'customer_requested' });
  return true;
}
```

- [ ] **Step 4: `js/chat/stream.js` 작성 (소켓 우선, 폴링 폴백)**

```js
import { sendMessage as sendMessageHttp } from './api.js';
import { pollOnce, startPolling, stopPolling } from './poller.js';
import {
  connectSocket,
  disconnectSocket,
  isSocketConnected,
  requestHandoffViaSocket,
  sendTyping as sendTypingSocket,
  sendViaSocket
} from './socket.js';
import { getSession, resolvePendingMessage, setTransport } from './state.js';

let mode = 'none'; // 'none' | 'socket' | 'polling'

/**
 * 실시간 연결을 시작한다.
 * 소켓을 먼저 시도하고, 실패하면 조용히 폴링으로 대체한다.
 * 고객은 어느 쪽으로 동작하는지 알 필요가 없다.
 */
export async function startStream({ onFatalError }) {
  stopStream();

  try {
    await connectSocket();
    mode = 'socket';
    setTransport('socket');
  } catch (err) {
    console.info('[chat] 실시간 연결 실패 → 주기 조회로 전환합니다.');
    mode = 'polling';
    setTransport('polling');
    startPolling({ onError: onFatalError });
  }
}

export function stopStream() {
  disconnectSocket();
  stopPolling();
  mode = 'none';
  setTransport('none');
}

export function isRealtime() {
  return mode === 'socket' && isSocketConnected();
}

/**
 * 메시지를 보낸다.
 * 소켓이 살아 있으면 소켓으로, 아니면 HTTP 로 보낸다.
 * 두 경로 모두 clientMessageId 를 쓰므로 중복 저장되지 않는다.
 */
export async function sendMessage({ text, clientMessageId }) {
  if (isRealtime() && sendViaSocket({ text, clientMessageId })) {
    // 확정은 chat:message:ack 이벤트가 해준다.
    return;
  }

  const session = getSession();
  if (!session) throw new Error('세션이 없습니다.');

  const { message } = await sendMessageHttp(session.roomId, session.visitorToken, {
    text,
    clientMessageId
  });
  resolvePendingMessage(clientMessageId, message);

  // 폴링 모드에서는 답변이 와 있을 수 있으니 즉시 한 번 확인한다.
  if (mode === 'polling') void pollOnce();
}

export function sendTyping(isTyping) {
  if (isRealtime()) sendTypingSocket(isTyping);
}

export function requestHandoff() {
  return requestHandoffViaSocket();
}
```

- [ ] **Step 5: `js/chat/widget.js` 를 `stream.js` 사용하도록 수정**

import 문을 교체한다.

```js
// 삭제:
// import { pollOnce, startPolling, stopPolling } from './poller.js';

// 추가:
import {
  isRealtime,
  requestHandoff,
  sendMessage as sendMessageStream,
  sendTyping,
  startStream,
  stopStream
} from './stream.js';
```

`restoreSession` 을 교체한다.

```js
function restoreSession(elements) {
  const session = loadSession();
  if (!session) return;

  setPhase('chat');
  void startStream({
    onFatalError: (error) => {
      clearSession();
      setPhase('form', errorKeyOf(error));
    }
  });
}
```

`bindStartForm` 의 성공 처리 부분에서 `startPolling(...)` 을 `startStream(...)` 으로 바꾼다.

```js
      saveSession({ roomId: result.roomId, visitorToken: result.visitorToken });
      setPhase('chat');
      void startStream({
        onFatalError: (error) => {
          clearSession();
          setPhase('form', errorKeyOf(error));
        }
      });
```

`submitMessage` 를 교체한다.

```js
async function submitMessage(text, clientMessageId) {
  try {
    await sendMessageStream({ text, clientMessageId });
  } catch {
    failPendingMessage(clientMessageId);
  }
}
```

`bindComposer` 의 `input` 이벤트에 typing 전송을 추가한다.

```js
  input.addEventListener('input', () => {
    input.style.height = 'auto';
    input.style.height = `${input.scrollHeight}px`;
    elements.composerButton.disabled = input.value.trim() === '';
    sendTyping(input.value.trim().length > 0);
  });
```

`bindPanelToggle` 에서 `pollOnce()` 호출을 지운다. (소켓이 알아서 최신 상태를 준다)

```js
    if (isOpen && getState().phase === 'chat') {
      scrollToBottom(elements.thread);
    }
```

`handoffButton` 핸들러를 교체한다.

```js
  elements.handoffButton.addEventListener('click', async () => {
    const session = getSession();
    if (!session) return;

    // 소켓이 있으면 전용 이벤트로, 없으면 일반 메시지로 요청한다.
    if (requestHandoff()) return;

    const clientMessageId = newClientMessageId();
    const text = getLang() === 'ja' ? '担当者と相談したいです。' : '담당자와 상담하고 싶습니다.';
    addPendingMessage({ clientMessageId, text, createdAt: new Date().toISOString() });
    await submitMessage(text, clientMessageId);
  });
```

`newChatButton` 핸들러의 `stopPolling()` 을 `stopStream()` 으로 바꾼다.

- [ ] **Step 6: `render.js` 에 "운영자 입력 중" 표시 추가**

`renderState` 안 채팅 화면 부분에 다음을 추가한다.

```js
  // 운영자가 입력 중이면 상태 표시줄 옆에 알린다.
  elements.statusLabel.textContent = state.peerTyping
    ? `${t(`status.${status}`)} · ${getLang() === 'ja' ? '入力中…' : '입력 중…'}`
    : t(`status.${status}`);
```

`render.js` 상단 import에 `getLang` 을 추가한다.
```js
import { getLang, t } from './i18n.js';
```

- [ ] **Step 7: 두 브라우저 창으로 실시간 확인**

세 서비스를 모두 띄운다.

창 A: `http://localhost:8080/korea-travel/` (고객)
창 B: `http://localhost:3100/chats` (운영자, 로그인)

1. 창 A에서 상담을 시작한다.
2. **창 B의 목록에 즉시 나타난다.** (5초 폴링 없이 바로 — Task 5 이후)
3. 창 A에서 메시지를 보낸다 → 창 B 상세 화면에 **즉시** 나타난다.
4. 창 B에서 답변한다 → 창 A에 **즉시** 나타난다.
5. 창 A의 `담당자 연결 요청` → 창 A 상태가 `운영자 연결 대기 중` 으로 바뀐다.
6. 창 B에서 `내가 상담하기` → 창 A 상태가 `운영자 상담 중` 으로 바뀐다.
7. 창 A 개발자도구 Network → WS 탭에서 `socket.io` 연결이 `101 Switching Protocols` 인지 확인한다.

- [ ] **Step 8: 폴백 확인**

브라우저 콘솔에서 CDN을 강제로 막고 새로고침한다.

```js
// 개발자도구 → Network → socket.io.min.js 를 Block request URL 로 차단 후 새로고침
```

기대: 콘솔에 `[chat] 실시간 연결 실패 → 주기 조회로 전환합니다.` 가 뜨고, **채팅은 3초 간격으로 계속 동작한다.**

- [ ] **Step 9: 재연결 확인**

1. 창 A에서 채팅 중에 API 서버를 종료한다 (`Ctrl+C`).
2. 상태 표시줄에 재연결 안내가 뜬다.
3. API 서버를 다시 켠다.
4. 몇 초 안에 자동으로 다시 연결되고, 끊긴 동안 온 메시지가 채워진다.

- [ ] **Step 10: 커밋**

```bash
git add js/chat index.html stemcell/index.html korea-travel/index.html
git commit -m "feat: switch customer widget to socket.io with polling fallback"
```

---

## Task 5: 관리자 화면을 실시간으로 전환

**Files:**
- Create: `apps/admin/lib/socket.ts`
- Modify: `apps/admin/hooks/useRoomStream.ts`
- Modify: `apps/admin/hooks/useRoomList.ts`
- Modify: `apps/admin/components/chat/ChatComposer.tsx`
- Modify: `apps/admin/app/chats/[roomId]/page.tsx`

**Interfaces:**
- Consumes: Task 1~3 서버 이벤트
- Produces:
  - `lib/socket.ts` → `getSocket(): Socket`, `closeSocket(): void`
  - `useRoomStream` 반환값에 `realtime: boolean`, `peerTyping: boolean` 추가 (기존 필드는 그대로)

---

- [ ] **Step 1: 패키지 설치**

```bash
npm install -w apps/admin socket.io-client
```

- [ ] **Step 2: `apps/admin/lib/socket.ts` 작성**

```ts
'use client';

import { io, type Socket } from 'socket.io-client';
import { API_URL } from './env';

let socket: Socket | null = null;

/**
 * 앱 전체에서 소켓 하나만 쓴다.
 * 컴포넌트마다 연결하면 상담방을 옮길 때마다 연결이 쌓인다.
 *
 * 인증은 httpOnly 쿠키로 한다. withCredentials 를 켜야 쿠키가 핸드셰이크에 실린다.
 */
export function getSocket(): Socket {
  if (socket) return socket;

  socket = io(API_URL, {
    withCredentials: true,
    transports: ['websocket', 'polling'],
    reconnectionAttempts: Infinity,
    reconnectionDelay: 1000,
    reconnectionDelayMax: 8000
  });

  return socket;
}

export function closeSocket(): void {
  socket?.disconnect();
  socket = null;
}
```

- [ ] **Step 3: `apps/admin/hooks/useRoomStream.ts` 를 소켓 기반으로 교체**

```ts
'use client';

import { useCallback, useEffect, useRef, useState } from 'react';
import type { ChatRoomDetail, ChatRoomStatus, MessageDTO } from '@stemcare/shared';
import { fetchRoom } from '@/lib/api';
import { getSocket } from '@/lib/socket';

const FALLBACK_POLL_MS = 5000;

export function useRoomStream(roomId: string) {
  const [room, setRoom] = useState<ChatRoomDetail | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);
  const [realtime, setRealtime] = useState(false);
  const [peerTyping, setPeerTyping] = useState(false);
  const inFlightRef = useRef(false);

  const refresh = useCallback(async () => {
    if (inFlightRef.current) return;
    inFlightRef.current = true;

    try {
      const next = await fetchRoom(roomId);
      setRoom(next);
      setError(null);
    } catch (err) {
      setError(err instanceof Error ? err.message : '상담을 불러오지 못했습니다.');
    } finally {
      inFlightRef.current = false;
      setLoading(false);
    }
  }, [roomId]);

  // 상담방이 바뀌면 상세를 다시 불러온다.
  // (메시지 목록·고객 정보·메모는 REST 로 한 번에 받는 게 간단하다)
  useEffect(() => {
    setLoading(true);
    setRoom(null);
    setPeerTyping(false);
    void refresh();
  }, [roomId, refresh]);

  // 소켓 구독
  useEffect(() => {
    const socket = getSocket();

    function handleConnect() {
      setRealtime(true);
      socket.emit('chat:join', { roomId });
    }

    function handleDisconnect() {
      setRealtime(false);
    }

    function handleMessage(payload: { message: MessageDTO }) {
      if (payload.message.chatRoomId !== roomId) return;
      setRoom((prev) => {
        if (!prev) return prev;
        if (prev.messages.some((m) => m.id === payload.message.id)) return prev;
        return { ...prev, messages: [...prev.messages, payload.message] };
      });
    }

    function handleStatus(payload: {
      roomId: string;
      status: ChatRoomStatus;
      assignedOperatorId: string | null;
    }) {
      if (payload.roomId !== roomId) return;
      setRoom((prev) =>
        prev
          ? { ...prev, status: payload.status, assignedOperatorId: payload.assignedOperatorId }
          : prev
      );
    }

    function handleTyping(payload: { roomId: string; from: string; isTyping: boolean }) {
      if (payload.roomId !== roomId || payload.from !== 'customer') return;
      setPeerTyping(payload.isTyping);
    }

    socket.on('connect', handleConnect);
    socket.on('disconnect', handleDisconnect);
    socket.on('chat:message', handleMessage);
    socket.on('chat:status', handleStatus);
    socket.on('chat:typing', handleTyping);

    // 이미 연결돼 있으면 connect 이벤트가 안 오므로 직접 join 한다.
    if (socket.connected) handleConnect();

    return () => {
      socket.off('connect', handleConnect);
      socket.off('disconnect', handleDisconnect);
      socket.off('chat:message', handleMessage);
      socket.off('chat:status', handleStatus);
      socket.off('chat:typing', handleTyping);
    };
  }, [roomId]);

  // 소켓이 끊긴 동안에만 폴링한다.
  useEffect(() => {
    if (realtime) return undefined;

    const id = window.setInterval(() => {
      if (document.visibilityState === 'visible') void refresh();
    }, FALLBACK_POLL_MS);

    return () => window.clearInterval(id);
  }, [realtime, refresh]);

  const applyRoom = useCallback((next: ChatRoomDetail) => setRoom(next), []);

  const appendMessage = useCallback((message: MessageDTO) => {
    setRoom((prev) => {
      if (!prev) return prev;
      if (prev.messages.some((m) => m.id === message.id)) return prev;
      return { ...prev, messages: [...prev.messages, message] };
    });
  }, []);

  return { room, loading, error, realtime, peerTyping, refresh, applyRoom, appendMessage };
}
```

- [ ] **Step 4: `apps/admin/hooks/useRoomList.ts` 에 소켓 갱신 추가**

`useRoomList` 안, 기존 폴링 `useEffect` **아래에** 추가한다.

```ts
  // 소켓이 알려주면 즉시 목록을 갱신한다.
  useEffect(() => {
    const socket = getSocket();

    function handleUpdate() {
      void load();
    }

    socket.on('rooms:updated', handleUpdate);
    socket.on('rooms:new', handleUpdate);

    return () => {
      socket.off('rooms:updated', handleUpdate);
      socket.off('rooms:new', handleUpdate);
    };
  }, [load]);
```

상단 import에 추가한다.
```ts
import { getSocket } from '@/lib/socket';
```

기존 5초 폴링은 그대로 둔다. 소켓이 막힌 환경에서 목록이 멈추면 안 되기 때문이다.

- [ ] **Step 5: `ChatComposer.tsx` 에 입력 중 전송 추가**

`textarea` 의 `onChange` 를 교체한다.

```tsx
        <textarea
          value={text}
          onChange={(e) => {
            setText(e.target.value);
            const socket = getSocket();
            if (socket.connected) {
              socket.emit('chat:typing', { roomId, isTyping: e.target.value.trim().length > 0 });
            }
          }}
```

상단 import에 추가한다.
```tsx
import { getSocket } from '@/lib/socket';
```

- [ ] **Step 6: 상세 페이지에 실시간/입력 중 표시 추가**

`apps/admin/app/chats/[roomId]/page.tsx` 의 `useRoomStream` 사용부와 헤더를 수정한다.

```tsx
  const { room, loading, error, realtime, peerTyping, applyRoom, appendMessage } = useRoomStream(roomId);
```

헤더의 `{error && ...}` 부분을 다음으로 교체한다.

```tsx
          {peerTyping && (
            <span style={{ fontSize: 11, color: 'var(--text-muted)' }}>고객이 입력 중…</span>
          )}
          <span
            style={{
              marginLeft: 'auto',
              fontSize: 11,
              color: realtime ? '#1a7f37' : '#c07a2b'
            }}
          >
            {realtime ? '실시간 연결됨' : '재연결 중 · 주기 조회로 동작'}
          </span>
```

- [ ] **Step 7: 로그아웃 시 소켓 정리**

`apps/admin/components/AuthProvider.tsx` 의 `signOut` 을 수정한다.

```tsx
  const signOut = useCallback(async () => {
    closeSocket();
    await api.logout().catch(() => undefined);
    setOperator(null);
    setStatus('anonymous');
  }, []);
```

import를 추가한다.
```tsx
import { closeSocket } from '@/lib/socket';
```

로그아웃 후에도 소켓이 살아 있으면, 서버가 아직 그 소켓을 운영자로 인식해 남의 상담 이벤트를 계속 보낸다.

- [ ] **Step 8: 두 브라우저 창으로 최종 확인**

1. 창 A(고객)에서 새 상담 시작 → 창 B(운영자) 목록에 **즉시** 나타난다.
2. 창 A에서 타이핑 → 창 B 헤더에 `고객이 입력 중…` 이 뜬다.
3. 창 A에서 메시지 전송 → 창 B에 즉시 표시, 목록의 unread 배지도 갱신된다.
4. 창 B에서 답변 → 창 A에 즉시 표시.
5. 창 B 헤더에 `실시간 연결됨` 이 초록색으로 보인다.
6. API 서버를 끄면 `재연결 중 · 주기 조회로 동작` 으로 바뀐다.
7. 창 B에서 위로 스크롤한 상태로 창 A가 메시지를 보내면 → 화면이 안 움직이고 `새 메시지 1개 ↓` 만 뜬다.

- [ ] **Step 9: 빌드와 테스트 확인**

```bash
npm run test -w apps/api
npm run test -w apps/admin
npm run build -w apps/admin
```

- [ ] **Step 10: 커밋**

```bash
git add apps/admin package-lock.json
git commit -m "feat: switch admin screen to socket.io realtime with polling fallback"
```

---

## Task 6: unread 실시간 반영과 읽음 처리 정리

**Files:**
- Modify: `apps/api/src/chatRooms/chatRoomService.ts`
- Modify: `apps/api/src/realtime/chatEvents.ts`
- Test: `apps/api/tests/unread.test.ts`

**Interfaces:**
- Consumes: Phase 1 `getRoomDetailAndMarkRead` 및 `roomLock.ts` (Socket 읽음 전용 서비스는 동일 잠금 규칙으로 추가)
- Produces: 운영자가 방에 들어와 있는 동안 도착한 메시지는 unread 로 세지 않는다

---

- [ ] **Step 1: 문제 이해**

현재는 상세 API(`GET /:roomId`)를 부를 때만 읽음 처리가 된다. 그런데 소켓을 쓰면 운영자가 방을 **열어둔 채로** 새 메시지를 받는다. 이때 unread가 계속 올라가면 목록의 배지가 이상해진다.

해결: 소켓으로 `chat:message` 를 방에 뿌릴 때, **그 방에 접속해 있는 운영자가 한 명이라도 있으면** `operatorLastReadAt` 을 갱신한다.

- [ ] **Step 2: 테스트 작성**

`apps/api/tests/unread.test.ts`

```ts
import { createServer, type Server as HttpServer } from 'node:http';
import type { AddressInfo } from 'node:net';
import request from 'supertest';
import { io as ioClient, type Socket as ClientSocket } from 'socket.io-client';
import { afterAll, afterEach, beforeAll, beforeEach, describe, expect, it } from 'vitest';
import { createApp } from '../src/app';
import { signOperatorToken } from '../src/auth/token';
import { createSocketServer } from '../src/realtime/socketServer';
import { createCustomerWithRoom, createOperator } from './helpers/factories';
import { disconnectDatabase, resetDatabase } from './helpers/testDb';

let httpServer: HttpServer;
let baseUrl: string;
const app = createApp();
const openSockets: ClientSocket[] = [];

function connect(auth: Record<string, unknown>): Promise<ClientSocket> {
  return new Promise((resolve, reject) => {
    const socket = ioClient(baseUrl, { auth, transports: ['websocket'], reconnection: false });
    openSockets.push(socket);
    socket.on('connect', () => resolve(socket));
    socket.on('connect_error', reject);
  });
}

function waitFor<T>(socket: ClientSocket, event: string, timeoutMs = 4000): Promise<T> {
  return new Promise((resolve, reject) => {
    const timer = setTimeout(() => reject(new Error(`${event} 없음`)), timeoutMs);
    socket.once(event, (payload: T) => {
      clearTimeout(timer);
      resolve(payload);
    });
  });
}

beforeAll(async () => {
  httpServer = createServer(app);
  createSocketServer(httpServer);
  await new Promise<void>((resolve) => httpServer.listen(0, resolve));
  baseUrl = `http://localhost:${(httpServer.address() as AddressInfo).port}`;
});

afterAll(async () => {
  await new Promise<void>((resolve) => httpServer.close(() => resolve()));
  await disconnectDatabase();
});

beforeEach(async () => {
  await resetDatabase();
});

afterEach(() => {
  while (openSockets.length > 0) openSockets.pop()?.disconnect();
});

describe('unread 실시간 처리', () => {
  it('운영자가 방에 들어와 있으면 새 고객 메시지가 unread 로 쌓이지 않는다', async () => {
    const { operator, plainPassword } = await createOperator({ email: 'unread@stemcare.test' });
    const { room, visitorToken } = await createCustomerWithRoom();

    const operatorSocket = await connect({
      operatorToken: signOperatorToken({ operatorId: operator.id, role: 'operator' })
    });
    operatorSocket.emit('chat:join', { roomId: room.id });
    await waitFor(operatorSocket, 'chat:joined');

    const customerSocket = await connect({ roomId: room.id, visitorToken });
    customerSocket.emit('chat:join', { roomId: room.id });
    await waitFor(customerSocket, 'chat:joined');

    const delivered = waitFor(operatorSocket, 'chat:message');
    customerSocket.emit('chat:message', { roomId: room.id, text: '읽음 처리 확인', clientMessageId: 'u-1' });
    await delivered;

    const agent = request.agent(baseUrl);
    await agent.post('/api/admin/auth/login').send({ email: 'unread@stemcare.test', password: plainPassword });
    const list = await agent.get('/api/admin/chat-rooms');

    expect(list.body.rooms[0].unreadCount).toBe(0);
  });

  it('운영자가 방에 없으면 unread 가 쌓인다', async () => {
    const { plainPassword } = await createOperator({ email: 'away@stemcare.test' });
    const { room, visitorToken } = await createCustomerWithRoom();

    const customerSocket = await connect({ roomId: room.id, visitorToken });
    customerSocket.emit('chat:join', { roomId: room.id });
    await waitFor(customerSocket, 'chat:joined');

    const ack = waitFor(customerSocket, 'chat:message:ack');
    customerSocket.emit('chat:message', { roomId: room.id, text: '아무도 없음', clientMessageId: 'u-2' });
    await ack;

    const agent = request.agent(baseUrl);
    await agent.post('/api/admin/auth/login').send({ email: 'away@stemcare.test', password: plainPassword });
    const list = await agent.get('/api/admin/chat-rooms');

    expect(list.body.rooms[0].unreadCount).toBe(1);
  });
});
```

- [ ] **Step 3: `emitters.ts` 에 읽음 처리 추가**

`broadcastMessage` 를 수정한다.

```ts
export async function broadcastMessage(io: Server, roomId: string, row: Message): Promise<void> {
  const channel = roomChannel(roomId);
  let operatorPresent = false;

  for (const [, socket] of io.sockets.sockets) {
    if (!socket.rooms.has(channel)) continue;

    const identity = socket.data.identity;
    if (!identity) continue;

    if (identity.kind === 'operator') operatorPresent = true;

    const viewer = identity.kind === 'customer' ? 'customer' : 'operator';
    socket.emit('chat:message', { message: toMessageDTO(row, viewer) });
  }

  // 운영자가 그 방을 보고 있었다면 방금 온 메시지는 이미 읽은 것이다.
  if (operatorPresent && row.senderType === 'customer') {
    await prisma.chatRoom.update({
      where: { id: roomId },
      data: { operatorLastReadAt: new Date() }
    });
  }

  io.to(OPERATORS_CHANNEL).emit('rooms:updated', { roomId });
}
```

`prisma` import를 추가한다.
```ts
import { prisma } from '../db';
```

- [ ] **Step 4: 호출부를 `await` 로 바꾸기**

`broadcastMessage` 가 이제 `Promise` 를 반환하므로, 모든 호출부에 `await` 를 붙인다.

- `chatEvents.ts` 의 `chat:message`, `chat:handoff-request` 핸들러
- `chatRoomService.ts` 의 `assignRoom`, `updateRoomStatus`
- `publicChatRoutes.ts`, `adminChatRoutes.ts` 의 메시지 라우트

예:
```ts
      if (io) await broadcastMessage(io, room.id, row);
```

호출부를 빠짐없이 찾으려면 grep 을 쓴다. `tsc` 는 기본 설정에서 무시된 Promise
(floating promise)를 오류로 잡아주지 않으므로, 타입 검사만 믿으면 안 된다.

```bash
grep -rn "broadcastMessage(" apps/api/src | grep -v "export async function"
```

출력된 모든 줄에 `await` 가 붙어 있어야 한다. 붙이지 않으면 컴파일은 통과하지만
읽음 처리가 끝나기 전에 응답이 나가서, unread 카운트가 간헐적으로 틀리게 된다.
이런 버그는 재현이 어려우므로 지금 확실히 잡는다.

```bash
npx tsc --noEmit -p apps/api/tsconfig.json
```

- [ ] **Step 5: 테스트 통과 확인**

```bash
npm run test -w apps/api
```

기대: unread 2 passed, 나머지도 전부 통과.

- [ ] **Step 6: 커밋**

```bash
git add apps/api
git commit -m "feat: mark room read when operator is present during broadcast"
```

---

## Task 7: 운영자 접속 상태 표시

**Files:**
- Create: `apps/api/src/realtime/presence.ts`
- Modify: `apps/api/src/realtime/socketServer.ts`
- Modify: `apps/api/src/realtime/chatEvents.ts`
- Modify: `js/chat/socket.js`
- Modify: `js/chat/state.js`
- Modify: `js/chat/render.js`
- Modify: `js/chat/i18n.js`
- Test: `apps/api/tests/presence.test.ts`

**Interfaces:**
- Consumes: `rooms.ts`, `emitters.ts` (Task 1~2)
- Produces:
  - `presence.ts` → `countOnlineOperators(io: Server): number`, `isOperatorInRoom(io: Server, roomId: string): boolean`, `broadcastPresence(io: Server, roomId: string): void`
  - 소켓 이벤트 `chat:presence` → `{ roomId: string; operatorOnline: boolean; anyOperatorOnline: boolean }`

---

### 왜 이 기능이 필요한가

스펙 11절 3단계는 실시간 채팅의 완료 항목으로 "운영자 접속 상태 표시"를 요구한다.

고객 입장에서 생각하면 이유가 분명하다. 밤 11시에 상담을 시작한 고객이
"운영자 연결 대기 중"만 30분 보고 있으면, 지금 아무도 없는 건지 곧 답이 올 건지
알 수가 없다. 그러면 고객은 떠나거나, 같은 질문을 여러 번 보낸다.

"현재 담당자가 접속해 있습니다" 또는 "현재 담당자가 부재중입니다. 남겨주시면
확인 후 연락드립니다"를 보여주면 고객이 기다릴지 메시지를 남길지 판단할 수 있다.

- [ ] **Step 1: 실패하는 테스트 작성**

`apps/api/tests/presence.test.ts`

```ts
import { createServer, type Server as HttpServer } from 'node:http';
import type { AddressInfo } from 'node:net';
import { io as ioClient, type Socket as ClientSocket } from 'socket.io-client';
import { afterAll, afterEach, beforeAll, beforeEach, describe, expect, it } from 'vitest';
import { createApp } from '../src/app';
import { signOperatorToken } from '../src/auth/token';
import { createSocketServer } from '../src/realtime/socketServer';
import { createCustomerWithRoom, createOperator } from './helpers/factories';
import { disconnectDatabase, resetDatabase } from './helpers/testDb';

let httpServer: HttpServer;
let baseUrl: string;
const openSockets: ClientSocket[] = [];

function connect(auth: Record<string, unknown>): Promise<ClientSocket> {
  return new Promise((resolve, reject) => {
    const socket = ioClient(baseUrl, { auth, transports: ['websocket'], reconnection: false });
    openSockets.push(socket);
    socket.on('connect', () => resolve(socket));
    socket.on('connect_error', reject);
  });
}

function waitFor<T>(socket: ClientSocket, event: string, timeoutMs = 4000): Promise<T> {
  return new Promise((resolve, reject) => {
    const timer = setTimeout(() => reject(new Error(`${event} 없음`)), timeoutMs);
    socket.once(event, (payload: T) => {
      clearTimeout(timer);
      resolve(payload);
    });
  });
}

/** 어떤 이벤트가 주어진 시간 안에 "오지 않았음"을 확인한다. */
function expectNoEvent(socket: ClientSocket, event: string, ms = 500): Promise<boolean> {
  return new Promise((resolve) => {
    let fired = false;
    const handler = () => {
      fired = true;
    };
    socket.once(event, handler);
    setTimeout(() => {
      socket.off(event, handler);
      resolve(fired);
    }, ms);
  });
}

beforeAll(async () => {
  httpServer = createServer(createApp());
  createSocketServer(httpServer);
  await new Promise<void>((resolve) => httpServer.listen(0, resolve));
  baseUrl = `http://localhost:${(httpServer.address() as AddressInfo).port}`;
});

afterAll(async () => {
  await new Promise<void>((resolve) => httpServer.close(() => resolve()));
  await disconnectDatabase();
});

beforeEach(async () => {
  await resetDatabase();
});

afterEach(() => {
  while (openSockets.length > 0) openSockets.pop()?.disconnect();
});

describe('운영자 접속 상태', () => {
  it('고객이 입장하면 현재 접속 상태를 함께 받는다', async () => {
    const { room, visitorToken } = await createCustomerWithRoom();
    const socket = await connect({ roomId: room.id, visitorToken });

    socket.emit('chat:join', { roomId: room.id });
    const payload = await waitFor<{ anyOperatorOnline: boolean }>(socket, 'chat:presence');

    expect(payload.anyOperatorOnline).toBe(false);
  });

  it('운영자가 접속하면 고객에게 알림이 간다', async () => {
    const { operator } = await createOperator();
    const { room, visitorToken } = await createCustomerWithRoom();

    const customerSocket = await connect({ roomId: room.id, visitorToken });
    customerSocket.emit('chat:join', { roomId: room.id });
    await waitFor(customerSocket, 'chat:joined');
    await waitFor(customerSocket, 'chat:presence');

    const presenceUpdate = waitFor<{ anyOperatorOnline: boolean }>(customerSocket, 'chat:presence');
    await connect({ operatorToken: signOperatorToken({ operatorId: operator.id, role: 'operator' }) });

    expect((await presenceUpdate).anyOperatorOnline).toBe(true);
  });

  it('운영자가 그 방에 들어오면 operatorOnline 이 true 가 된다', async () => {
    const { operator } = await createOperator();
    const { room, visitorToken } = await createCustomerWithRoom();

    const customerSocket = await connect({ roomId: room.id, visitorToken });
    customerSocket.emit('chat:join', { roomId: room.id });
    await waitFor(customerSocket, 'chat:joined');

    const operatorSocket = await connect({
      operatorToken: signOperatorToken({ operatorId: operator.id, role: 'operator' })
    });

    const inRoom = new Promise<boolean>((resolve) => {
      const timer = setTimeout(() => resolve(false), 4000);
      customerSocket.on('chat:presence', (payload: { operatorOnline: boolean }) => {
        if (payload.operatorOnline) {
          clearTimeout(timer);
          resolve(true);
        }
      });
    });

    operatorSocket.emit('chat:join', { roomId: room.id });

    expect(await inRoom).toBe(true);
  });

  it('운영자가 나가면 다시 false 가 된다', async () => {
    const { operator } = await createOperator();
    const { room, visitorToken } = await createCustomerWithRoom();

    const customerSocket = await connect({ roomId: room.id, visitorToken });
    customerSocket.emit('chat:join', { roomId: room.id });
    await waitFor(customerSocket, 'chat:joined');

    const operatorSocket = await connect({
      operatorToken: signOperatorToken({ operatorId: operator.id, role: 'operator' })
    });
    operatorSocket.emit('chat:join', { roomId: room.id });
    await waitFor(customerSocket, 'chat:presence');

    const wentOffline = new Promise<boolean>((resolve) => {
      const timer = setTimeout(() => resolve(false), 4000);
      customerSocket.on('chat:presence', (payload: { anyOperatorOnline: boolean }) => {
        if (!payload.anyOperatorOnline) {
          clearTimeout(timer);
          resolve(true);
        }
      });
    });

    operatorSocket.disconnect();

    expect(await wentOffline).toBe(true);
  });

  it('다른 상담방 고객에게는 그 방의 운영자 입장이 전달되지 않는다', async () => {
    const { operator } = await createOperator();
    const watched = await createCustomerWithRoom();
    const other = await createCustomerWithRoom();

    const otherCustomer = await connect({
      roomId: other.room.id,
      visitorToken: other.visitorToken
    });
    otherCustomer.emit('chat:join', { roomId: other.room.id });
    await waitFor(otherCustomer, 'chat:joined');
    await waitFor(otherCustomer, 'chat:presence');

    const operatorSocket = await connect({
      operatorToken: signOperatorToken({ operatorId: operator.id, role: 'operator' })
    });
    // 감시 대상이 아닌 방에만 들어간다.
    operatorSocket.emit('chat:join', { roomId: watched.room.id });

    // other 방의 고객은 operatorOnline=true 를 받으면 안 된다.
    const gotRoomPresence = await new Promise<boolean>((resolve) => {
      let sawInRoom = false;
      const handler = (payload: { operatorOnline: boolean }) => {
        if (payload.operatorOnline) sawInRoom = true;
      };
      otherCustomer.on('chat:presence', handler);
      setTimeout(() => {
        otherCustomer.off('chat:presence', handler);
        resolve(sawInRoom);
      }, 800);
    });

    expect(gotRoomPresence).toBe(false);
  });
});
```

- [ ] **Step 2: 실패 확인**

```bash
npx vitest run tests/presence.test.ts --root apps/api
```

기대: `chat:presence 없음` 으로 실패.

- [ ] **Step 3: `apps/api/src/realtime/presence.ts` 작성**

```ts
import type { Server } from 'socket.io';
import { OPERATORS_CHANNEL, roomChannel } from './rooms';

/**
 * 접속 상태는 DB 에 저장하지 않는다.
 *
 * 이유: 소켓 연결은 언제든 끊긴다. DB 에 "접속 중" 을 기록하면
 * 서버가 갑자기 죽었을 때 영원히 "접속 중" 으로 남는다.
 * Socket.IO 의 현재 연결 목록을 그때그때 세는 것이 항상 정확하다.
 */

/** 지금 로그인해 소켓에 붙어 있는 운영자 수 */
export function countOnlineOperators(io: Server): number {
  return io.sockets.adapter.rooms.get(OPERATORS_CHANNEL)?.size ?? 0;
}

/** 이 상담방을 열어놓고 보고 있는 운영자가 있는가 */
export function isOperatorInRoom(io: Server, roomId: string): boolean {
  const channel = roomChannel(roomId);

  for (const [, socket] of io.sockets.sockets) {
    if (!socket.rooms.has(channel)) continue;
    if (socket.data.identity?.kind === 'operator') return true;
  }

  return false;
}

/** 한 상담방의 고객에게 현재 접속 상태를 알린다. */
export function broadcastPresence(io: Server, roomId: string): void {
  io.to(roomChannel(roomId)).emit('chat:presence', {
    roomId,
    operatorOnline: isOperatorInRoom(io, roomId),
    anyOperatorOnline: countOnlineOperators(io) > 0
  });
}

/**
 * 운영자가 접속하거나 끊겼을 때, 지금 고객이 보고 있는 모든 상담방에 알린다.
 * 상담방이 아주 많아도 "고객 소켓이 붙어 있는 방"만 대상이므로 부담이 작다.
 */
export function broadcastPresenceToAllCustomerRooms(io: Server): void {
  const roomIds = new Set<string>();

  for (const [, socket] of io.sockets.sockets) {
    const identity = socket.data.identity;
    if (identity?.kind === 'customer') roomIds.add(identity.roomId);
  }

  for (const roomId of roomIds) {
    broadcastPresence(io, roomId);
  }
}
```

- [ ] **Step 4: `socketServer.ts` 에서 접속/해제 시 알리기**

`io.on('connection', ...)` 안, `registerChatEvents(io!, socket);` 아래에 추가한다.

```ts
    if (identity.kind === 'operator') {
      // 운영자가 들어왔다. 대기 중인 고객들에게 알린다.
      broadcastPresenceToAllCustomerRooms(io!);

      socket.on('disconnect', () => {
        // disconnect 핸들러가 먼저 실행되면 아직 목록에 남아 있을 수 있으므로
        // 다음 이벤트 루프에서 센다.
        setImmediate(() => broadcastPresenceToAllCustomerRooms(io!));
      });
    }
```

import를 추가한다.

```ts
import { broadcastPresenceToAllCustomerRooms } from './presence';
```

`setImmediate` 가 필요한 이유는, Socket.IO가 `disconnect` 이벤트를 먼저 발생시키고
그 다음에 룸에서 소켓을 제거하기 때문이다. 바로 세면 방금 나간 운영자가 아직
집계에 포함된다.

- [ ] **Step 5: `chatEvents.ts` 의 `chat:join` 에서 현재 상태 전달**

`chat:join` 핸들러의 `socket.emit('chat:joined', {...})` 다음에 추가한다.

```ts
      // 방 참가자가 바뀌었으므로 접속 상태를 다시 알린다.
      broadcastPresence(io, roomId);
```

그리고 소켓이 방에서 나갈 때도 갱신한다. `registerChatEvents` 의 `disconnect` 핸들러를 교체한다.

```ts
  socket.on('disconnect', () => {
    // 이 소켓이 보고 있던 상담방들의 접속 상태를 갱신한다.
    const watchedRooms = [...socket.rooms].filter((name) => name.startsWith('room:'));

    setImmediate(() => {
      for (const channel of watchedRooms) {
        broadcastPresence(io, channel.slice('room:'.length));
      }
    });
  });
```

import를 추가한다.

```ts
import { broadcastPresence } from './presence';
```

- [ ] **Step 6: 테스트 통과 확인**

```bash
npx vitest run tests/presence.test.ts --root apps/api
```

기대: 5 passed.

- [ ] **Step 7: `js/chat/i18n.js` 에 문구 추가**

두 언어 사전에 각각 추가한다. (키 개수가 같아야 한다)

`ko`:
```js
    'presence.online': '담당자 접속 중',
    'presence.inRoom': '담당자가 상담방에 있습니다',
    'presence.offline': '현재 담당자가 부재중입니다. 메시지를 남겨주시면 확인 후 연락드립니다.',
```

`ja`:
```js
    'presence.online': '担当者がオンラインです',
    'presence.inRoom': '担当者が相談ルームにいます',
    'presence.offline': '現在担当者が不在です。メッセージを残していただければ、確認後ご連絡いたします。',
```

Phase 2 Task 1 Step 3의 키 개수 검사 명령을 다시 실행해 두 사전이 같은지 확인한다.

```bash
node -e "
const src = require('fs').readFileSync('js/chat/i18n.js','utf8');
const ko = [...src.matchAll(/'([a-zA-Z.]+)':/g)].map(m=>m[1]);
const half = ko.length/2;
const a = ko.slice(0,half).sort(), b = ko.slice(half).sort();
console.log('키 개수:', a.length, b.length);
console.log('불일치:', a.filter(k=>!b.includes(k)).concat(b.filter(k=>!a.includes(k))));
"
```

기대: `키 개수: 43 43`, `불일치: []`

- [ ] **Step 8: `js/chat/state.js` 에 상태 추가**

`state` 객체에 추가한다.

```js
  presence: { operatorOnline: false, anyOperatorOnline: false },
```

파일 끝에 setter를 추가한다.

```js
export function setPresence(presence) {
  if (
    state.presence.operatorOnline === presence.operatorOnline &&
    state.presence.anyOperatorOnline === presence.anyOperatorOnline
  ) {
    return;
  }
  state.presence = presence;
  notify();
}
```

`getState()` 에서도 복사해 돌려주도록 한 줄 고친다.

```js
export function getState() {
  return {
    ...state,
    messages: [...state.messages],
    pending: [...state.pending],
    presence: { ...state.presence }
  };
}
```

- [ ] **Step 9: `js/chat/socket.js` 에 수신 핸들러 추가**

`chat:status` 핸들러 아래에 추가한다.

```js
    socket.on('chat:presence', (payload) => {
      setPresence({
        operatorOnline: Boolean(payload.operatorOnline),
        anyOperatorOnline: Boolean(payload.anyOperatorOnline)
      });
    });
```

import에 `setPresence` 를 추가한다.

- [ ] **Step 10: `js/chat/render.js` 에 표시 추가**

`renderState` 의 채팅 화면 부분에서, 상태 표시줄 문구를 만드는 곳을 다음으로 교체한다.

```js
  // 상태 표시줄: 상담 상태 + (입력 중) + 담당자 접속 여부
  const parts = [t(`status.${status}`)];

  if (state.peerTyping) {
    parts.push(getLang() === 'ja' ? '入力中…' : '입력 중…');
  } else if (state.presence.operatorOnline) {
    parts.push(t('presence.inRoom'));
  } else if (state.presence.anyOperatorOnline) {
    parts.push(t('presence.online'));
  }

  elements.statusLabel.textContent = parts.join(' · ');

  // 운영자를 기다리는 중인데 아무도 접속해 있지 않으면 안내 문구를 띄운다.
  const showOfflineNotice =
    (status === 'waiting' || status === 'bot') && !state.presence.anyOperatorOnline;
  elements.offlineNotice.hidden = !showOfflineNotice;
  if (showOfflineNotice) elements.offlineNotice.textContent = t('presence.offline');
```

`mountWidget` 의 채팅 화면 HTML에서 상태 표시줄 바로 아래에 요소를 추가한다.

```html
        <p class="consult-chat-offline" data-role="offline-notice" hidden></p>
```

`elements` 객체에도 추가한다.

```js
    offlineNotice: root.querySelector('[data-role="offline-notice"]'),
```

- [ ] **Step 11: `css/chat-widget.css` 에 스타일 추가**

```css
.consult-chat-offline {
  margin: 0;
  padding: 8px 16px;
  font-size: 12px;
  line-height: 1.5;
  color: #8a5f10;
  background: #fdf6e4;
  border-bottom: 1px solid rgba(0, 0, 0, 0.06);
}

.consult-chat-offline[hidden] { display: none; }
```

- [ ] **Step 12: 두 브라우저 창으로 확인**

1. 운영자 창을 **모두 닫은 상태**에서 고객이 상담을 시작한다.
   → 상태 표시줄 아래에 노란 띠로 "현재 담당자가 부재중입니다…" 가 보인다.
2. 운영자가 관리자 화면에 로그인한다.
   → 고객 화면의 노란 띠가 **사라지고** 상태 표시줄에 `담당자 접속 중` 이 붙는다.
3. 운영자가 그 상담방을 연다.
   → `담당자가 상담방에 있습니다` 로 바뀐다.
4. 운영자가 브라우저 탭을 닫는다.
   → 몇 초 안에 다시 부재중 안내가 나타난다.
5. 다른 고객 창을 하나 더 열어, 운영자가 **다른 방**에 들어가도
   이 고객에게는 `담당자 접속 중` 까지만 보이고 `상담방에 있습니다` 는 안 보인다.

- [ ] **Step 13: 커밋**

```bash
git add apps/api js/chat css/chat-widget.css
git commit -m "feat: show operator online presence to customers in realtime"
```

---

## Phase 4 완료 기준 (Definition of Done)

- [ ] 고객과 운영자가 같은 상담방에서 새로고침 없이 실시간으로 대화한다.
- [ ] 모든 메시지가 DB에 저장된다. (소켓으로 보내도 저장 로직은 하나뿐)
- [ ] 같은 `clientMessageId` 를 두 번 보내도 메시지는 하나만 저장된다.
- [ ] 소켓 인증이 실패하면 연결 자체가 거부된다. (`connect_error`)
- [ ] 고객이 남의 `roomId` 로 join 하거나 메시지를 보내면 `FORBIDDEN` 이 온다.
- [ ] 고객에게 가는 메시지에 운영자용 번역 정보가 섞이지 않는다.
- [ ] 신규 상담이 접수되면 운영자 목록에 즉시 나타난다.
- [ ] 운영자가 배정하거나 상태를 바꾸면 고객 화면에 즉시 반영된다.
- [ ] `담당자 연결 요청` 을 누르면 상태가 `waiting` 이 되고 양쪽에 반영된다.
- [ ] 입력 중 표시가 상대에게 보인다.
- [ ] 운영자가 방을 열어둔 동안 온 메시지는 unread 로 쌓이지 않는다.
- [ ] 운영자가 아무도 접속해 있지 않으면 고객 화면에 부재중 안내가 뜬다.
- [ ] 운영자가 로그인하면 고객 화면의 부재중 안내가 사라지고 `담당자 접속 중` 이 표시된다.
- [ ] 운영자가 그 상담방을 열면 `담당자가 상담방에 있습니다` 로 바뀐다.
- [ ] 소켓이 막힌 환경(CDN 차단)에서도 폴링으로 채팅이 계속 동작한다.
- [ ] API 서버를 껐다 켜면 자동으로 재연결되고 놓친 메시지가 채워진다.
- [ ] `npm run test -w apps/api` 가 전부 통과한다.

## 이 Phase에서 하지 않는 것

- 번역 → Phase 5
- AI 자동 응답 → Phase 5
- 브라우저 알림/알림음 → Phase 6 이후 (운영 안정화)
- 메시지별 읽음 시각 (누가 언제 읽었는지) → MVP 이후
- 여러 서버로 확장(Redis adapter) → 운영 규모가 커질 때

## 초보자가 자주 막히는 지점

**"소켓이 연결은 되는데 메시지가 안 온다"**
`chat:join` 을 안 보냈거나, `roomChannel()` 이 아닌 문자열로 방 이름을 만들었다. 서버에서 `io.sockets.adapter.rooms` 를 찍어보면 실제 방 이름을 확인할 수 있다.

**"관리자 화면 소켓이 계속 UNAUTHORIZED 다"**
`io(API_URL, { withCredentials: true })` 가 빠졌다. 이게 없으면 핸드셰이크에 쿠키가 실리지 않는다.

**"메시지가 두 번 보인다"**
`broadcastMessage` 로 방 전체에 보내면서, 동시에 HTTP 응답으로도 화면에 추가하고 있다. `useRoomStream` 의 `appendMessage` 에서 `id` 중복을 걸러야 한다.

**"React에서 소켓 이벤트가 여러 번 실행된다"**
`useEffect` 의 cleanup에서 `socket.off(...)` 를 하지 않았다. StrictMode에서는 개발 중 effect가 두 번 실행되므로 반드시 정리해야 한다.

**"소켓 연결이 계속 늘어난다"**
컴포넌트마다 `io(...)` 를 호출했다. `lib/socket.ts` 처럼 모듈 수준 싱글턴으로 하나만 만들어야 한다.

**"테스트에서 소켓이 열린 채로 남아 프로세스가 안 끝난다"**
`afterEach` 에서 열었던 클라이언트 소켓을 전부 `disconnect()` 하고, `afterAll` 에서 `httpServer.close()` 를 해야 한다.
