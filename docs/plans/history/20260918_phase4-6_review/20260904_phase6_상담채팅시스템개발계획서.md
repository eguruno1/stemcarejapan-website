# Phase 6: 테스트·배포·운영 안정화 개발계획서

## Phase 2·3 완료 후 인계 (2026-09-18)

현재 E2E는 tests/e2e/playwright.config.ts가 전용 127.0.0.1:8081/4001/3101 서버를 자동 기동하며 TEST_DATABASE_URL을 사용한다. API 테스트와 같은 DB로 동시에 실행하지 않는다. SCJ_E2E=1일 때 관리자 산출물은 .next-e2e에 분리한다. 실제 배포에는 사이트에서 접근 가능한 API와 CORS 설정이 필요하며 정적 Sites 배포만으로 Express/PostgreSQL이 배포되지 않는다.
이 Phase의 구현·완료 상태는 이번 검토에서 변경하지 않는다.


## Phase 0·1 완료 후 인계 (2026-09-18)

이 Phase는 **미완료 / 후속 개발 대상**이다. Phase 0·1 완료 문서와 실제 소스를 선행 기준으로 사용한다. 아래 인계 사항은 본문의 과거 예시보다 우선한다.

현재 관리자 start 포트는 3100이다. Docker EXPOSE와 Nginx upstream도 3100에 맞춘다. production JWT_SECRET은 필수다. 테스트 DB URL은 필수이며 DB 이름은 _test로 끝나고 개발 DB와 달라야 한다. 기본 seed는 기존 계정 비밀번호를 변경하지 않으며 비밀번호 변경 화면은 아직 없다.


> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

작성일: 2026-09-04

**Goal:** Phase 0~5에서 만든 상담채팅시스템을 실제 고객이 쓸 수 있는 상태로 만든다. E2E 테스트, 보안 하드닝, 개인정보 보관 정책, HTTPS 배포, 로그·모니터링, DB 백업까지 마친다.

**Architecture:** 개발용 `docker-compose.yml` 과 별개로 `docker-compose.prod.yml` 을 만든다. Nginx가 HTTPS를 종료하고 `/`(정적 홈페이지), `/admin`(Next.js), `/api`·`/socket.io`(API 서버)로 나눠 보낸다. 이렇게 하면 브라우저 입장에서 **모든 것이 같은 출처**가 되어 CORS 문제와 쿠키 문제가 함께 사라진다. 로그는 pino로 구조화하고 개인정보 필드를 자동 마스킹한다. 개인정보 보관 기간은 스케줄 작업으로 강제한다.

**Tech Stack:** Playwright(E2E), helmet, express-rate-limit, pino + pino-http, node-cron, Nginx, Docker Compose, pg_dump

**Spec:** `docs/20260904_상담채팅시스템구축방향.md` 12절, `docs/plans/20260904_phase6_상담채팅시스템구축계획서.md`

---

## Global Constraints

Phase 0 문서의 **Global Constraints 전체가 그대로 적용된다.** 추가 제약:

- **운영 로그에 고객 이름·전화·이메일·상담 본문을 남기지 않는다.** 상담 본문은 DB에만 있다.
- 운영 환경에서 쿠키는 반드시 `secure: true`, `httpOnly: true`, `sameSite: 'lax'` 다.
- 운영 환경에서 `JWT_SECRET` 이 기본값이면 **서버가 시작되지 않아야 한다.**
- 상담 종료 후 **180일**이 지나면 개인정보(이름·전화·이메일)를 비식별화한다. 상담 본문은 **365일** 뒤 삭제한다. (운영 정책은 바꿀 수 있으나 코드에 상수로 명시한다)
- 배포 전 체크리스트를 **전부** 통과하지 않으면 배포하지 않는다.
- 백업은 "설정했다"가 아니라 **"복구해봤다"** 로 확인한다.

---

## 이 Phase를 시작하기 전에 알아야 할 개념

**리버스 프록시(Nginx)란?**
브라우저의 모든 요청을 먼저 받아서, 경로에 따라 뒤에 있는 서버로 넘겨주는 중계자다. 우리는 하나의 도메인(`chat.example.com`)으로 세 개의 서버(정적 홈페이지·관리자 앱·API)를 서비스한다. Nginx가 없으면 고객은 8080, 운영자는 3100, API는 4000 포트를 각각 열어야 하고, 브라우저가 이를 서로 다른 출처로 보기 때문에 CORS와 쿠키 문제가 계속 생긴다.

**HTTPS가 왜 필수인가?**
이 시스템은 이름·전화·건강 관심사를 주고받는다. HTTP는 이 내용이 중간 네트워크에서 그대로 읽힌다. 그리고 `secure` 쿠키는 HTTPS에서만 동작하므로, HTTP로는 안전한 로그인 자체가 불가능하다.

**개인정보 비식별화란?**
행을 통째로 지우면 "몇 건의 상담이 있었는지" 같은 통계도 사라진다. 대신 이름을 `고객_a1b2`, 전화를 `***`로 바꾸면 **누구인지 알 수 없으면서 건수는 남는다.** 이걸 비식별화(pseudonymization)라고 한다.

**구조화 로그란?**
`console.log('사용자 로그인 실패')` 대신 `{"level":"warn","msg":"login_failed","operatorId":"...","reqId":"..."}` 처럼 JSON으로 남기는 것이다. 나중에 "특정 상담방에서 무슨 일이 있었나"를 `reqId`나 `roomId`로 검색할 수 있다.

---

## File Structure

```text
apps/api/src/
├── config.ts                    수정 (운영 환경 필수값 검증)
├── app.ts                       수정 (helmet, rate limit, 로거)
├── common/
│   ├── logger.ts             ★ pino + 개인정보 마스킹
│   └── security.ts           ★ helmet/rate-limit 설정
├── ops/
│   ├── retention.ts          ★ 개인정보 보관 기간 처리
│   ├── retentionJob.ts       ★ node-cron 스케줄
│   └── metricsRoutes.ts      ★ /api/admin/ops/metrics
└── server.ts                    수정 (graceful shutdown)

apps/api/tests/
├── security.test.ts          ★
└── retention.test.ts         ★

tests/e2e/
├── playwright.config.ts         수정 (관리자 프로젝트 추가)
├── chat-widget.spec.ts          기존 (Phase 2)
├── operator-flow.spec.ts     ★ 고객↔운영자 전 과정
└── scroll-quality.spec.ts    ★ 스크롤 품질

deploy/
├── docker-compose.prod.yml   ★
├── Dockerfile.api            ★
├── Dockerfile.admin          ★
├── nginx/
│   ├── nginx.conf            ★
│   └── site.conf             ★
├── backup/
│   ├── backup.sh             ★
│   └── restore.sh            ★
└── DEPLOY.md                 ★ 배포 절차서

.github/workflows/ci.yml      ★ (선택)
CHECKLIST.md                  ★ 배포 전 체크리스트
```

---

## Task 1: 보안 하드닝

**Files:**
- Create: `apps/api/src/common/security.ts`
- Modify: `apps/api/src/config.ts`
- Modify: `apps/api/src/app.ts`
- Modify: `apps/api/src/auth/authRoutes.ts`
- Test: `apps/api/tests/security.test.ts`

**Interfaces:**
- Consumes: `config.ts`, `app.ts`
- Produces:
  - `security.ts` → `applySecurity(app: Express): void`, `publicChatLimiter: RequestHandler`, `loginLimiter: RequestHandler`
  - `config.ts` → `assertProductionConfig(): void` — 운영 환경 필수값 검증

---

- [ ] **Step 1: 패키지 설치**

```bash
npm install -w apps/api helmet express-rate-limit
```

- [ ] **Step 2: 실패하는 테스트 작성**

`apps/api/tests/security.test.ts`

```ts
import request from 'supertest';
import { afterAll, beforeEach, describe, expect, it } from 'vitest';
import { createApp } from '../src/app';
import { createCustomerWithRoom, createOperator } from './helpers/factories';
import { disconnectDatabase, resetDatabase } from './helpers/testDb';

const app = createApp();

beforeEach(async () => {
  await resetDatabase();
});

afterAll(async () => {
  await disconnectDatabase();
});

describe('보안 헤더', () => {
  it('helmet 기본 헤더가 붙는다', async () => {
    const res = await request(app).get('/health');

    expect(res.headers['x-content-type-options']).toBe('nosniff');
    expect(res.headers['x-frame-options']).toBeDefined();
    expect(res.headers['x-powered-by']).toBeUndefined();
  });
});

describe('요청 제한 (rate limit)', () => {
  it('로그인을 짧은 시간에 여러 번 시도하면 429 가 난다', async () => {
    await createOperator({ email: 'rl@stemcare.test' });

    const attempts = [];
    for (let i = 0; i < 12; i += 1) {
      attempts.push(
        request(app)
          .post('/api/admin/auth/login')
          .send({ email: 'rl@stemcare.test', password: 'wrong-password-1' })
      );
    }
    const results = await Promise.all(attempts);

    expect(results.some((res) => res.status === 429)).toBe(true);
  });

  it('상담 시작도 과도하게 호출하면 429 가 난다', async () => {
    const attempts = [];
    for (let i = 0; i < 12; i += 1) {
      attempts.push(
        request(app).post('/api/public/chat/start').send({
          name: `연속 ${i}`,
          phone: '010-0000-0000',
          preferredLanguage: 'ko',
          serviceType: 'undecided',
          privacyAgreed: true
        })
      );
    }
    const results = await Promise.all(attempts);

    expect(results.some((res) => res.status === 429)).toBe(true);
  });
});

describe('민감 정보 노출 방지', () => {
  it('로그인 응답에 비밀번호 해시가 없다', async () => {
    const { plainPassword } = await createOperator({ email: 'leak@stemcare.test' });

    const res = await request(app)
      .post('/api/admin/auth/login')
      .send({ email: 'leak@stemcare.test', password: plainPassword });

    expect(JSON.stringify(res.body)).not.toContain('$2');
    expect(JSON.stringify(res.body)).not.toContain('passwordHash');
  });

  it('서버 오류 응답에 내부 스택이 노출되지 않는다', async () => {
    const res = await request(app)
      .get('/api/public/chat/이건-uuid가-아님')
      .set('X-Visitor-Token', 'x'.repeat(64));

    expect(JSON.stringify(res.body)).not.toContain('prisma');
    expect(JSON.stringify(res.body)).not.toContain('at Object');
  });

  it('고객 응답에 visitorTokenHash 가 없다', async () => {
    const { room, visitorToken } = await createCustomerWithRoom();

    const res = await request(app)
      .get(`/api/public/chat/${room.id}`)
      .set('X-Visitor-Token', visitorToken);

    expect(JSON.stringify(res.body)).not.toContain('visitorTokenHash');
  });
});

describe('운영 환경 설정 검증', () => {
  it('JWT_SECRET 이 기본값이면 오류를 던진다', async () => {
    const { assertProductionConfig } = await import('../src/config');
    const original = { env: process.env.NODE_ENV, secret: process.env.JWT_SECRET };

    process.env.NODE_ENV = 'production';
    process.env.JWT_SECRET = 'change-this-in-production';

    expect(() => assertProductionConfig()).toThrow(/JWT_SECRET/);

    process.env.NODE_ENV = original.env;
    process.env.JWT_SECRET = original.secret;
  });
});
```

- [ ] **Step 3: `apps/api/src/common/security.ts` 작성**

```ts
import type { Express, RequestHandler } from 'express';
import rateLimit from 'express-rate-limit';
import helmet from 'helmet';
import { config } from '../config';

/**
 * 짧은 시간에 같은 IP 에서 오는 과도한 요청을 막는다.
 *
 * 이게 없으면:
 * - 로그인: 비밀번호를 무차별 대입으로 알아낼 수 있다.
 * - 상담 시작: 스팸 상담이 무한정 쌓여 DB 가 가득 찬다.
 */
export const loginLimiter: RequestHandler = rateLimit({
  windowMs: 60_000,
  limit: 10,
  standardHeaders: 'draft-7',
  legacyHeaders: false,
  message: { error: { code: 'TOO_MANY_REQUESTS', message: '잠시 후 다시 시도해주세요.' } }
});

export const publicChatLimiter: RequestHandler = rateLimit({
  windowMs: 60_000,
  limit: 10,
  standardHeaders: 'draft-7',
  legacyHeaders: false,
  message: { error: { code: 'TOO_MANY_REQUESTS', message: '요청이 너무 많습니다. 잠시 후 다시 시도해주세요.' } }
});

/** 메시지 전송은 조금 더 여유롭게 허용한다. 실제 대화가 막히면 안 된다. */
export const messageLimiter: RequestHandler = rateLimit({
  windowMs: 60_000,
  limit: 60,
  standardHeaders: 'draft-7',
  legacyHeaders: false,
  message: { error: { code: 'TOO_MANY_REQUESTS', message: '메시지를 너무 빠르게 보내고 있습니다.' } }
});

export function applySecurity(app: Express): void {
  app.use(
    helmet({
      // 이 서버는 JSON API 만 제공하므로 CSP 는 Nginx 쪽 정적 파일에 건다.
      contentSecurityPolicy: false,
      crossOriginResourcePolicy: { policy: 'cross-origin' }
    })
  );

  // Nginx 뒤에 있으므로 X-Forwarded-For 를 신뢰해야 rate limit 이 실제 IP 로 동작한다.
  if (config.nodeEnv === 'production') {
    app.set('trust proxy', 1);
  }

  // Express 버전 노출 제거
  app.disable('x-powered-by');
}
```

- [ ] **Step 4: `apps/api/src/config.ts` 에 운영 검증 추가**

파일 끝에 추가한다.

```ts
const INSECURE_DEFAULTS = ['change-this-in-production', 'dev-only-secret', 'change-this-too'];

/**
 * 운영 환경에서 위험한 기본값이 남아 있으면 서버를 띄우지 않는다.
 * "설정을 깜빡했다"가 보안 사고로 이어지는 것을 막는 가장 확실한 방법이다.
 */
export function assertProductionConfig(): void {
  if ((process.env.NODE_ENV ?? 'development') !== 'production') return;

  const problems: string[] = [];

  const secret = process.env.JWT_SECRET ?? '';
  if (INSECURE_DEFAULTS.includes(secret) || secret.length < 32) {
    problems.push('JWT_SECRET 이 기본값이거나 32자 미만입니다.');
  }

  if (!process.env.DATABASE_URL) {
    problems.push('DATABASE_URL 이 없습니다.');
  }

  if (!process.env.WEBSITE_ORIGIN || !process.env.ADMIN_ORIGIN) {
    problems.push('WEBSITE_ORIGIN / ADMIN_ORIGIN 이 설정되지 않았습니다.');
  }

  if ((process.env.WEBSITE_ORIGIN ?? '').startsWith('http://')) {
    problems.push('운영 환경에서는 HTTPS 주소를 사용해야 합니다.');
  }

  if (problems.length > 0) {
    throw new Error(`운영 환경 설정 오류:\n- ${problems.join('\n- ')}`);
  }
}
```

- [ ] **Step 5: `apps/api/src/app.ts` 에 적용**

`createApp()` 안, `cors` 앞에 추가한다.

```ts
  applySecurity(app);
```

그리고 라우터 등록부에 rate limiter를 끼운다.

```ts
  app.use('/api/admin/auth/login', loginLimiter);
  app.use('/api/public/chat/start', publicChatLimiter);
  app.use('/api/public/chat', messageLimiter);

  app.use('/api/admin/auth', authRoutes);
  app.use('/api/admin/chat-rooms', adminChatRoutes);
  app.use('/api/admin/translate', translateRoutes);
  app.use('/api/public/chat', publicChatRoutes);
```

미들웨어는 등록 순서대로 실행되므로, limiter를 라우터보다 **먼저** 등록해야 한다.

- [ ] **Step 6: `apps/api/src/server.ts` 에 설정 검증 추가**

```ts
import { assertProductionConfig } from './config';

assertProductionConfig();

const app = createApp();
// ... 이하 동일
```

- [ ] **Step 7: 테스트 통과 확인**

```bash
npx vitest run tests/security.test.ts --root apps/api
```

기대: 7 passed.

rate limit 테스트가 다른 테스트에 영향을 주면 `vitest.config.ts` 의 `fileParallelism: false` 덕분에 순차 실행되므로 괜찮다. 그래도 실패하면 `beforeEach` 에서 limiter를 초기화하거나 테스트 환경에서 limit을 크게 잡는 방법도 있다.

- [ ] **Step 8: 커밋**

```bash
git add apps/api package-lock.json
git commit -m "feat: add security headers, rate limiting and production config validation"
```

---

## Task 2: 구조화 로그와 개인정보 마스킹

**Files:**
- Create: `apps/api/src/common/logger.ts`
- Modify: `apps/api/src/app.ts`
- Modify: `apps/api/src/realtime/chatEvents.ts`
- Test: `apps/api/tests/logger.test.ts`

**Interfaces:**
- Consumes: `config.ts`
- Produces:
  - `logger: pino.Logger`
  - `httpLogger: RequestHandler`
  - `maskPersonalData(value: unknown): unknown` — 테스트로 검증 가능한 순수 함수

---

- [ ] **Step 1: 패키지 설치**

```bash
npm install -w apps/api pino pino-http
npm install -w apps/api -D pino-pretty
```

- [ ] **Step 2: 실패하는 테스트 작성**

`apps/api/tests/logger.test.ts`

```ts
import { describe, expect, it } from 'vitest';
import { maskPersonalData } from '../src/common/logger';

describe('maskPersonalData', () => {
  it('이름을 마스킹한다', () => {
    const masked = maskPersonalData({ name: '야마다 타로', roomId: 'r1' }) as Record<string, unknown>;

    expect(masked.name).toBe('[REDACTED]');
    expect(masked.roomId).toBe('r1');
  });

  it('전화번호와 이메일을 마스킹한다', () => {
    const masked = maskPersonalData({
      phone: '+81 90-1234-5678',
      email: 'yamada@example.com'
    }) as Record<string, unknown>;

    expect(masked.phone).toBe('[REDACTED]');
    expect(masked.email).toBe('[REDACTED]');
  });

  it('상담 본문을 마스킹한다', () => {
    const masked = maskPersonalData({
      originalText: '来月、母と2人で韓国に行きたいです。',
      translatedText: '다음 달에 어머니와 갑니다',
      visibleText: '来月'
    }) as Record<string, unknown>;

    expect(masked.originalText).toBe('[REDACTED]');
    expect(masked.translatedText).toBe('[REDACTED]');
    expect(masked.visibleText).toBe('[REDACTED]');
  });

  it('비밀번호와 토큰을 마스킹한다', () => {
    const masked = maskPersonalData({
      password: 'secret',
      passwordHash: '$2a$10$abc',
      visitorToken: 'a'.repeat(64),
      authorization: 'Bearer xyz'
    }) as Record<string, unknown>;

    expect(masked.password).toBe('[REDACTED]');
    expect(masked.passwordHash).toBe('[REDACTED]');
    expect(masked.visitorToken).toBe('[REDACTED]');
    expect(masked.authorization).toBe('[REDACTED]');
  });

  it('중첩된 객체도 마스킹한다', () => {
    const masked = maskPersonalData({
      room: { id: 'r1', customer: { name: '홍길동', phone: '010-1111-2222' } }
    }) as any;

    expect(masked.room.id).toBe('r1');
    expect(masked.room.customer.name).toBe('[REDACTED]');
  });

  it('배열도 마스킹한다', () => {
    const masked = maskPersonalData([{ name: '홍길동' }, { name: '김철수' }]) as any[];

    expect(masked[0].name).toBe('[REDACTED]');
    expect(masked[1].name).toBe('[REDACTED]');
  });

  it('안전한 필드는 그대로 둔다', () => {
    const masked = maskPersonalData({
      roomId: 'r1',
      status: 'active',
      durationMs: 120,
      translationStatus: 'done'
    }) as Record<string, unknown>;

    expect(masked).toEqual({
      roomId: 'r1',
      status: 'active',
      durationMs: 120,
      translationStatus: 'done'
    });
  });
});
```

- [ ] **Step 3: `apps/api/src/common/logger.ts` 작성**

```ts
import pino from 'pino';
import pinoHttp from 'pino-http';
import { randomUUID } from 'node:crypto';
import { config } from '../config';

/**
 * 로그에 절대 남기면 안 되는 필드 이름들.
 *
 * 상담 본문(originalText 등)까지 포함하는 이유:
 * 이 시스템의 대화에는 건강 상태나 시술 관심사가 들어갈 수 있다.
 * 그런 내용은 접근 통제가 되는 DB 에만 있어야 하고,
 * 서버 로그 파일에 흩어지면 안 된다.
 */
const SENSITIVE_KEYS = new Set([
  'name',
  'customerName',
  'phone',
  'email',
  'memo',
  'note',
  'password',
  'passwordHash',
  'visitorToken',
  'visitorTokenHash',
  'operatorToken',
  'authorization',
  'cookie',
  'set-cookie',
  'originalText',
  'translatedText',
  'visibleText',
  'text',
  'message',
  'summary',
  'replyText'
]);

const REDACTED = '[REDACTED]';

export function maskPersonalData(value: unknown, depth = 0): unknown {
  if (depth > 6) return value;
  if (value === null || typeof value !== 'object') return value;

  if (Array.isArray(value)) {
    return value.map((item) => maskPersonalData(item, depth + 1));
  }

  const result: Record<string, unknown> = {};
  for (const [key, item] of Object.entries(value)) {
    if (SENSITIVE_KEYS.has(key) || SENSITIVE_KEYS.has(key.toLowerCase())) {
      result[key] = REDACTED;
      continue;
    }
    result[key] = maskPersonalData(item, depth + 1);
  }
  return result;
}

export const logger = pino({
  level: config.nodeEnv === 'production' ? 'info' : 'debug',
  // 개발 중에는 사람이 읽기 좋게, 운영에서는 JSON 한 줄로.
  transport:
    config.nodeEnv === 'development'
      ? { target: 'pino-pretty', options: { colorize: true, translateTime: 'HH:MM:ss' } }
      : undefined,
  // pino 자체 redact 도 함께 건다. (헤더처럼 우리가 손대지 않는 경로용)
  redact: {
    paths: [
      'req.headers.cookie',
      'req.headers.authorization',
      'req.headers["x-visitor-token"]',
      'res.headers["set-cookie"]',
      'req.body.password',
      'req.body.name',
      'req.body.phone',
      'req.body.email',
      'req.body.message',
      'req.body.text',
      'req.body.originalText'
    ],
    censor: REDACTED
  }
});

export const httpLogger = pinoHttp({
  logger,
  genReqId: (req) => (req.headers['x-request-id'] as string) ?? randomUUID(),
  // 본문은 아예 로그에 넣지 않는다. 경로와 상태만으로 충분히 디버깅할 수 있다.
  serializers: {
    req: (req) => ({ id: req.id, method: req.method, url: req.url }),
    res: (res) => ({ statusCode: res.statusCode })
  },
  customLogLevel: (_req, res, err) => {
    if (err || res.statusCode >= 500) return 'error';
    if (res.statusCode >= 400) return 'warn';
    return 'info';
  }
});
```

- [ ] **Step 4: `apps/api/src/app.ts` 에 로거 연결**

`applySecurity(app)` 다음 줄에 추가한다.

```ts
  app.use(httpLogger);
```

그리고 `errors.ts` 의 `errorHandler` 에서 `console.error` 를 로거로 바꾼다.

```ts
import { logger, maskPersonalData } from './logger';

// errorHandler 안:
  logger.error(
    { err: err instanceof Error ? { message: err.message, stack: err.stack } : maskPersonalData(err) },
    'unhandled_error'
  );
```

- [ ] **Step 5: 로그에 개인정보가 안 나오는지 실제로 확인**

```bash
npm run dev:api > /tmp/api.log 2>&1 &
sleep 3

curl -s -X POST http://localhost:4000/api/public/chat/start \
  -H 'Content-Type: application/json' \
  -d '{"name":"로그검사고객","phone":"010-9999-8888","email":"leak@test.com","preferredLanguage":"ko","serviceType":"undecided","message":"민감한 상담 내용입니다","privacyAgreed":true}' > /dev/null

sleep 1
grep -c "로그검사고객\|010-9999-8888\|leak@test.com\|민감한 상담 내용" /tmp/api.log
```

기대 결과: **`0`**

`0` 이 아니면 어느 로그가 새는지 확인한다.
```bash
grep -n "로그검사고객\|010-9999-8888" /tmp/api.log
```

- [ ] **Step 6: 테스트 통과 확인**

```bash
npx vitest run tests/logger.test.ts --root apps/api
```

기대: 7 passed.

- [ ] **Step 7: 커밋**

```bash
git add apps/api package-lock.json
git commit -m "feat: add structured logging with personal data masking"
```

---

## Task 3: 개인정보 보관 기간 처리

**Files:**
- Create: `apps/api/src/ops/retention.ts`
- Create: `apps/api/src/ops/retentionJob.ts`
- Modify: `apps/api/src/server.ts`
- Create: `apps/api/prisma/migrations/.../` (컬럼 추가)
- Test: `apps/api/tests/retention.test.ts`

**Interfaces:**
- Consumes: `db.ts`, `logger.ts`
- Produces:
  - `PSEUDONYMIZE_AFTER_DAYS = 180`, `DELETE_MESSAGES_AFTER_DAYS = 365`
  - `pseudonymizeOldCustomers(now?: Date): Promise<number>`
  - `deleteOldMessages(now?: Date): Promise<number>`
  - `runRetention(now?: Date): Promise<{ pseudonymized: number; deleted: number }>`
  - `startRetentionJob(): void`

---

- [ ] **Step 1: Prisma 스키마에 비식별화 표시 컬럼 추가**

`apps/api/prisma/schema.prisma` 의 `Customer` 모델에 추가한다.

```prisma
  pseudonymizedAt DateTime? @map("pseudonymized_at")
```

마이그레이션을 만든다.

```bash
npm run db:migrate -w apps/api -- --name add_customer_pseudonymized_at
```

- [ ] **Step 2: 실패하는 테스트 작성**

`apps/api/tests/retention.test.ts`

```ts
import { afterAll, beforeEach, describe, expect, it } from 'vitest';
import { prisma } from '../src/db';
import { createMessageRow } from '../src/messages/messageService';
import {
  DELETE_MESSAGES_AFTER_DAYS,
  PSEUDONYMIZE_AFTER_DAYS,
  deleteOldMessages,
  pseudonymizeOldCustomers,
  runRetention
} from '../src/ops/retention';
import { createCustomerWithRoom } from './helpers/factories';
import { disconnectDatabase, resetDatabase } from './helpers/testDb';

function daysAgo(days: number): Date {
  return new Date(Date.now() - days * 24 * 60 * 60 * 1000);
}

beforeEach(async () => {
  await resetDatabase();
});

afterAll(async () => {
  await disconnectDatabase();
});

describe('pseudonymizeOldCustomers', () => {
  it('보관 기간이 지난 종료 상담의 고객 정보를 비식별화한다', async () => {
    const { customer, room } = await createCustomerWithRoom({ name: '야마다 타로' });
    await prisma.chatRoom.update({
      where: { id: room.id },
      data: { status: 'closed', closedAt: daysAgo(PSEUDONYMIZE_AFTER_DAYS + 1) }
    });

    const count = await pseudonymizeOldCustomers();

    expect(count).toBe(1);
    const updated = await prisma.customer.findUniqueOrThrow({ where: { id: customer.id } });
    expect(updated.name).not.toBe('야마다 타로');
    expect(updated.name).toMatch(/^고객_/);
    expect(updated.phone).toBe('[삭제됨]');
    expect(updated.email).toBeNull();
    expect(updated.pseudonymizedAt).not.toBeNull();
  });

  it('아직 기간이 안 된 상담은 건드리지 않는다', async () => {
    const { customer, room } = await createCustomerWithRoom({ name: '최근 고객' });
    await prisma.chatRoom.update({
      where: { id: room.id },
      data: { status: 'closed', closedAt: daysAgo(10) }
    });

    const count = await pseudonymizeOldCustomers();

    expect(count).toBe(0);
    const unchanged = await prisma.customer.findUniqueOrThrow({ where: { id: customer.id } });
    expect(unchanged.name).toBe('최근 고객');
  });

  it('진행 중인 상담의 고객은 건드리지 않는다', async () => {
    const { customer, room } = await createCustomerWithRoom({ name: '진행 중 고객' });
    // 오래됐지만 아직 종료되지 않은 상담
    await prisma.chatRoom.update({
      where: { id: room.id },
      data: { status: 'active', createdAt: daysAgo(400) }
    });

    const count = await pseudonymizeOldCustomers();

    expect(count).toBe(0);
    const unchanged = await prisma.customer.findUniqueOrThrow({ where: { id: customer.id } });
    expect(unchanged.name).toBe('진행 중 고객');
  });

  it('이미 비식별화된 고객은 다시 처리하지 않는다', async () => {
    const { customer, room } = await createCustomerWithRoom();
    await prisma.chatRoom.update({
      where: { id: room.id },
      data: { status: 'closed', closedAt: daysAgo(PSEUDONYMIZE_AFTER_DAYS + 1) }
    });

    await pseudonymizeOldCustomers();
    const secondRun = await pseudonymizeOldCustomers();

    expect(secondRun).toBe(0);
    void customer;
  });

  it('상담 건수 통계는 남는다 (행을 지우지 않는다)', async () => {
    const { room } = await createCustomerWithRoom();
    await prisma.chatRoom.update({
      where: { id: room.id },
      data: { status: 'closed', closedAt: daysAgo(PSEUDONYMIZE_AFTER_DAYS + 1) }
    });

    await pseudonymizeOldCustomers();

    expect(await prisma.customer.count()).toBe(1);
    expect(await prisma.chatRoom.count()).toBe(1);
  });
});

describe('deleteOldMessages', () => {
  it('보관 기간이 지난 상담의 메시지를 삭제한다', async () => {
    const { room, customer } = await createCustomerWithRoom();
    await createMessageRow({
      chatRoomId: room.id,
      senderType: 'customer',
      senderId: customer.id,
      text: '오래된 상담 내용',
      originalLanguage: 'ko'
    });
    await prisma.chatRoom.update({
      where: { id: room.id },
      data: { status: 'closed', closedAt: daysAgo(DELETE_MESSAGES_AFTER_DAYS + 1) }
    });

    const deleted = await deleteOldMessages();

    expect(deleted).toBe(1);
    expect(await prisma.message.count({ where: { chatRoomId: room.id } })).toBe(0);
    // 상담방 자체는 남는다.
    expect(await prisma.chatRoom.count()).toBe(1);
  });

  it('기간 내 상담의 메시지는 남긴다', async () => {
    const { room, customer } = await createCustomerWithRoom();
    await createMessageRow({
      chatRoomId: room.id,
      senderType: 'customer',
      senderId: customer.id,
      text: '최근 상담 내용',
      originalLanguage: 'ko'
    });
    await prisma.chatRoom.update({
      where: { id: room.id },
      data: { status: 'closed', closedAt: daysAgo(30) }
    });

    const deleted = await deleteOldMessages();

    expect(deleted).toBe(0);
    expect(await prisma.message.count()).toBe(1);
  });
});

describe('runRetention', () => {
  it('두 작업을 함께 실행하고 결과를 돌려준다', async () => {
    const old = await createCustomerWithRoom();
    await createMessageRow({
      chatRoomId: old.room.id,
      senderType: 'customer',
      senderId: old.customer.id,
      text: '아주 오래된 내용',
      originalLanguage: 'ko'
    });
    await prisma.chatRoom.update({
      where: { id: old.room.id },
      data: { status: 'closed', closedAt: daysAgo(DELETE_MESSAGES_AFTER_DAYS + 1) }
    });

    const result = await runRetention();

    expect(result.pseudonymized).toBe(1);
    expect(result.deleted).toBe(1);
  });
});
```

- [ ] **Step 3: `apps/api/src/ops/retention.ts` 작성**

```ts
import { randomBytes } from 'node:crypto';
import { prisma } from '../db';
import { logger } from '../common/logger';

/**
 * 개인정보 보관 정책.
 *
 * 스펙 12절: "불필요한 정보를 오래 보관하지 않는 정책이 필요하다."
 *
 * - 상담 종료 후 180일: 이름·전화·이메일을 비식별화한다.
 *   (누구인지 알 수 없게 하되, "몇 건의 상담이 있었나" 통계는 남긴다)
 * - 상담 종료 후 365일: 대화 본문을 삭제한다.
 *   (건강 관련 언급이 있을 수 있으므로 오래 두지 않는다)
 *
 * 실제 운영 정책이 정해지면 이 상수만 바꾸면 된다.
 */
export const PSEUDONYMIZE_AFTER_DAYS = 180;
export const DELETE_MESSAGES_AFTER_DAYS = 365;

function daysBefore(now: Date, days: number): Date {
  return new Date(now.getTime() - days * 24 * 60 * 60 * 1000);
}

export async function pseudonymizeOldCustomers(now = new Date()): Promise<number> {
  const cutoff = daysBefore(now, PSEUDONYMIZE_AFTER_DAYS);

  const targets = await prisma.customer.findMany({
    where: {
      pseudonymizedAt: null,
      // 이 고객의 모든 상담이 종료되었고, 가장 최근 종료가 기준일보다 오래됐다.
      chatRooms: {
        every: { status: 'closed', closedAt: { lt: cutoff } },
        some: {}
      }
    },
    select: { id: true }
  });

  let count = 0;
  for (const target of targets) {
    await prisma.customer.update({
      where: { id: target.id },
      data: {
        name: `고객_${randomBytes(2).toString('hex')}`,
        phone: '[삭제됨]',
        email: null,
        memo: null,
        pseudonymizedAt: now
      }
    });
    count += 1;
  }

  if (count > 0) {
    logger.info({ count, policy: 'pseudonymize', afterDays: PSEUDONYMIZE_AFTER_DAYS }, 'retention_done');
  }

  return count;
}

export async function deleteOldMessages(now = new Date()): Promise<number> {
  const cutoff = daysBefore(now, DELETE_MESSAGES_AFTER_DAYS);

  const oldRooms = await prisma.chatRoom.findMany({
    where: { status: 'closed', closedAt: { lt: cutoff } },
    select: { id: true }
  });

  if (oldRooms.length === 0) return 0;

  const roomIds = oldRooms.map((room) => room.id);

  const result = await prisma.message.deleteMany({
    where: { chatRoomId: { in: roomIds } }
  });

  // 요약도 대화 내용을 담고 있으므로 함께 지운다.
  await prisma.chatSummary.deleteMany({ where: { chatRoomId: { in: roomIds } } });
  await prisma.operatorNote.deleteMany({ where: { chatRoomId: { in: roomIds } } });

  if (result.count > 0) {
    logger.info(
      { count: result.count, policy: 'delete_messages', afterDays: DELETE_MESSAGES_AFTER_DAYS },
      'retention_done'
    );
  }

  return result.count;
}

export async function runRetention(now = new Date()): Promise<{ pseudonymized: number; deleted: number }> {
  const deleted = await deleteOldMessages(now);
  const pseudonymized = await pseudonymizeOldCustomers(now);
  return { pseudonymized, deleted };
}
```

- [ ] **Step 4: `apps/api/src/ops/retentionJob.ts` 작성**

```bash
npm install -w apps/api node-cron
npm install -w apps/api -D @types/node-cron
```

```ts
import cron from 'node-cron';
import { logger } from '../common/logger';
import { runRetention } from './retention';

/**
 * 매일 새벽 4시(서버 시각)에 보관 기간 정리를 실행한다.
 *
 * 왜 새벽인가: 상담이 거의 없는 시간대여야 DB 부하가 서비스에 영향을 주지 않는다.
 */
export function startRetentionJob(): void {
  cron.schedule('0 4 * * *', () => {
    void runRetention()
      .then((result) => logger.info(result, 'retention_job_completed'))
      .catch((err) => logger.error({ err: String(err) }, 'retention_job_failed'));
  });

  logger.info({ schedule: '0 4 * * *' }, 'retention_job_scheduled');
}
```

- [ ] **Step 5: `apps/api/src/server.ts` 에서 실행 + graceful shutdown**

```ts
import { createServer } from 'node:http';
import { createApp } from './app';
import { logger } from './common/logger';
import { assertProductionConfig, config } from './config';
import { prisma } from './db';
import { startRetentionJob } from './ops/retentionJob';
import { createSocketServer } from './realtime/socketServer';

assertProductionConfig();

const app = createApp();
const httpServer = createServer(app);
const io = createSocketServer(httpServer);

startRetentionJob();

httpServer.listen(config.port, () => {
  logger.info({ port: config.port, env: config.nodeEnv }, 'api_started');
});

/**
 * 컨테이너가 종료 신호를 받으면 진행 중인 요청을 끝내고 정리한다.
 * 이게 없으면 재배포할 때마다 상담 중이던 연결이 갑자기 끊긴다.
 */
async function shutdown(signal: string): Promise<void> {
  logger.info({ signal }, 'shutdown_started');

  io.close();
  httpServer.close(() => logger.info('http_closed'));

  await prisma.$disconnect();

  // 10초 안에 안 끝나면 강제 종료한다.
  setTimeout(() => process.exit(0), 10_000).unref();
}

process.on('SIGTERM', () => void shutdown('SIGTERM'));
process.on('SIGINT', () => void shutdown('SIGINT'));
```

- [ ] **Step 6: 수동 실행 스크립트 추가**

`apps/api/package.json` 의 `scripts` 에 추가한다.

```json
    "ops:retention": "tsx -e \"import('./src/ops/retention').then(m=>m.runRetention()).then(r=>{console.log(r);process.exit(0)})\""
```

- [ ] **Step 7: 테스트 통과 확인**

```bash
npx vitest run tests/retention.test.ts --root apps/api
```

기대: 8 passed.

- [ ] **Step 8: 커밋**

```bash
git add apps/api package-lock.json
git commit -m "feat: add personal data retention policy with scheduled job"
```

---

## Task 4: 운영 지표 API

**Files:**
- Create: `apps/api/src/ops/metricsRoutes.ts`
- Modify: `apps/api/src/app.ts`
- Modify: `apps/admin/lib/api.ts`
- Create: `apps/admin/app/chats/ops/page.tsx`
- Test: `apps/api/tests/metrics.test.ts`

**Interfaces:**
- Consumes: `requireOperator`, `db.ts`, `getIo()`
- Produces:
  - `GET /api/admin/ops/metrics` → `OpsMetrics`
  - `OpsMetrics = { waitingRooms: number; activeRooms: number; botRooms: number; todayStarted: number; unansweredOver10Min: number; translationFailures24h: number; socketConnections: number; dbOk: boolean }`

---

- [ ] **Step 1: `packages/shared/src/types.ts` 에 타입 추가**

```ts
export interface OpsMetrics {
  waitingRooms: number;
  activeRooms: number;
  botRooms: number;
  todayStarted: number;
  /** 고객 마지막 메시지 이후 10분 넘게 응답이 없는 상담 수 */
  unansweredOver10Min: number;
  translationFailures24h: number;
  socketConnections: number;
  dbOk: boolean;
}
```

- [ ] **Step 2: 테스트 작성**

`apps/api/tests/metrics.test.ts`

```ts
import request from 'supertest';
import { afterAll, beforeEach, describe, expect, it } from 'vitest';
import { createApp } from '../src/app';
import { prisma } from '../src/db';
import { createMessageRow } from '../src/messages/messageService';
import { createCustomerWithRoom, createOperator } from './helpers/factories';
import { disconnectDatabase, resetDatabase } from './helpers/testDb';

const app = createApp();

async function loginAgent() {
  const { plainPassword } = await createOperator({ email: 'ops@stemcare.test' });
  const agent = request.agent(app);
  await agent.post('/api/admin/auth/login').send({ email: 'ops@stemcare.test', password: plainPassword });
  return agent;
}

beforeEach(async () => {
  await resetDatabase();
});

afterAll(async () => {
  await disconnectDatabase();
});

describe('GET /api/admin/ops/metrics', () => {
  it('로그인하지 않으면 401 이다', async () => {
    const res = await request(app).get('/api/admin/ops/metrics');

    expect(res.status).toBe(401);
  });

  it('상태별 상담 수를 센다', async () => {
    const agent = await loginAgent();
    await createCustomerWithRoom({ status: 'waiting' });
    await createCustomerWithRoom({ status: 'waiting' });
    await createCustomerWithRoom({ status: 'active' });
    await createCustomerWithRoom({ status: 'bot' });

    const res = await agent.get('/api/admin/ops/metrics');

    expect(res.status).toBe(200);
    expect(res.body.metrics.waitingRooms).toBe(2);
    expect(res.body.metrics.activeRooms).toBe(1);
    expect(res.body.metrics.botRooms).toBe(1);
  });

  it('오늘 시작된 상담 수를 센다', async () => {
    const agent = await loginAgent();
    await createCustomerWithRoom();
    const old = await createCustomerWithRoom();
    await prisma.chatRoom.update({
      where: { id: old.room.id },
      data: { createdAt: new Date('2020-01-01') }
    });

    const res = await agent.get('/api/admin/ops/metrics');

    expect(res.body.metrics.todayStarted).toBe(1);
  });

  it('10분 넘게 응답 없는 상담을 센다', async () => {
    const agent = await loginAgent();
    const { room, customer } = await createCustomerWithRoom({ status: 'waiting' });
    const message = await createMessageRow({
      chatRoomId: room.id,
      senderType: 'customer',
      senderId: customer.id,
      text: '답변 기다립니다',
      originalLanguage: 'ko'
    });
    await prisma.message.update({
      where: { id: message.id },
      data: { createdAt: new Date(Date.now() - 15 * 60 * 1000) }
    });
    await prisma.chatRoom.update({
      where: { id: room.id },
      data: { lastMessageAt: new Date(Date.now() - 15 * 60 * 1000) }
    });

    const res = await agent.get('/api/admin/ops/metrics');

    expect(res.body.metrics.unansweredOver10Min).toBe(1);
  });

  it('24시간 내 번역 실패 건수를 센다', async () => {
    const agent = await loginAgent();
    const { room, customer } = await createCustomerWithRoom();
    await prisma.message.create({
      data: {
        chatRoomId: room.id,
        senderType: 'customer',
        senderId: customer.id,
        originalLanguage: 'ja',
        originalText: 'テスト',
        visibleText: 'テスト',
        translationStatus: 'failed'
      }
    });

    const res = await agent.get('/api/admin/ops/metrics');

    expect(res.body.metrics.translationFailures24h).toBe(1);
  });

  it('DB 연결 상태를 알려준다', async () => {
    const agent = await loginAgent();

    const res = await agent.get('/api/admin/ops/metrics');

    expect(res.body.metrics.dbOk).toBe(true);
  });
});
```

- [ ] **Step 3: `apps/api/src/ops/metricsRoutes.ts` 작성**

```ts
import { Router } from 'express';
import type { OpsMetrics } from '@stemcare/shared';
import { requireOperator } from '../auth/requireOperator';
import { asyncHandler } from '../common/asyncHandler';
import { prisma } from '../db';
import { getIo } from '../realtime/socketServer';

export const metricsRoutes = Router();

metricsRoutes.use(requireOperator);

metricsRoutes.get(
  '/metrics',
  asyncHandler(async (_req, res) => {
    const now = new Date();
    const startOfToday = new Date(now.getFullYear(), now.getMonth(), now.getDate());
    const tenMinutesAgo = new Date(now.getTime() - 10 * 60 * 1000);
    const dayAgo = new Date(now.getTime() - 24 * 60 * 60 * 1000);

    let dbOk = true;
    try {
      await prisma.$queryRaw`SELECT 1`;
    } catch {
      dbOk = false;
    }

    const [waitingRooms, activeRooms, botRooms, todayStarted, unansweredOver10Min, translationFailures24h] =
      await Promise.all([
        prisma.chatRoom.count({ where: { status: 'waiting' } }),
        prisma.chatRoom.count({ where: { status: 'active' } }),
        prisma.chatRoom.count({ where: { status: 'bot' } }),
        prisma.chatRoom.count({ where: { createdAt: { gte: startOfToday } } }),
        prisma.chatRoom.count({
          where: {
            status: { in: ['waiting', 'active'] },
            lastMessageAt: { lt: tenMinutesAgo }
          }
        }),
        prisma.message.count({
          where: { translationStatus: 'failed', createdAt: { gte: dayAgo } }
        })
      ]);

    const metrics: OpsMetrics = {
      waitingRooms,
      activeRooms,
      botRooms,
      todayStarted,
      unansweredOver10Min,
      translationFailures24h,
      socketConnections: getIo()?.sockets.sockets.size ?? 0,
      dbOk
    };

    res.json({ metrics });
  })
);
```

`apps/api/src/app.ts` 에 등록한다.

```ts
  app.use('/api/admin/ops', metricsRoutes);
```

- [ ] **Step 4: 관리자 화면에 운영 대시보드 추가**

`apps/admin/lib/api.ts` 에 추가한다.

```ts
export async function fetchMetrics(): Promise<OpsMetrics> {
  const res = await request<{ metrics: OpsMetrics }>('/api/admin/ops/metrics');
  return res.metrics;
}
```

`apps/admin/app/chats/ops/page.tsx`:

```tsx
'use client';

import { useEffect, useState } from 'react';
import type { OpsMetrics } from '@stemcare/shared';
import { fetchMetrics } from '@/lib/api';

function Tile({
  label,
  value,
  warn
}: {
  label: string;
  value: string | number;
  warn?: boolean;
}) {
  return (
    <div
      style={{
        background: 'var(--surface)',
        border: `1px solid ${warn ? 'var(--danger)' : 'var(--border)'}`,
        borderRadius: 8,
        padding: 14,
        minWidth: 150
      }}
    >
      <div style={{ fontSize: 11, color: 'var(--text-muted)', marginBottom: 6 }}>{label}</div>
      <div style={{ fontSize: 22, fontWeight: 600, color: warn ? 'var(--danger)' : 'var(--text)' }}>
        {value}
      </div>
    </div>
  );
}

export default function OpsPage() {
  const [metrics, setMetrics] = useState<OpsMetrics | null>(null);
  const [error, setError] = useState<string | null>(null);

  useEffect(() => {
    async function load() {
      try {
        setMetrics(await fetchMetrics());
        setError(null);
      } catch (err) {
        setError(err instanceof Error ? err.message : '지표를 불러오지 못했습니다.');
      }
    }

    void load();
    const id = window.setInterval(load, 15_000);
    return () => window.clearInterval(id);
  }, []);

  return (
    <div style={{ flex: 1, padding: 20, overflowY: 'auto' }}>
      <h1 style={{ fontSize: 15, margin: '0 0 14px' }}>운영 현황</h1>

      {error && <p style={{ color: 'var(--danger)', fontSize: 12 }}>{error}</p>}

      {metrics && (
        <div style={{ display: 'flex', gap: 10, flexWrap: 'wrap' }}>
          <Tile label="운영자 대기" value={metrics.waitingRooms} warn={metrics.waitingRooms > 3} />
          <Tile label="진행 중" value={metrics.activeRooms} />
          <Tile label="AI 상담 중" value={metrics.botRooms} />
          <Tile label="오늘 접수" value={metrics.todayStarted} />
          <Tile
            label="10분 이상 무응답"
            value={metrics.unansweredOver10Min}
            warn={metrics.unansweredOver10Min > 0}
          />
          <Tile
            label="번역 실패 (24h)"
            value={metrics.translationFailures24h}
            warn={metrics.translationFailures24h > 5}
          />
          <Tile label="실시간 연결" value={metrics.socketConnections} />
          <Tile label="DB" value={metrics.dbOk ? '정상' : '장애'} warn={!metrics.dbOk} />
        </div>
      )}

      <p style={{ fontSize: 11, color: 'var(--text-muted)', marginTop: 16 }}>
        15초마다 자동 갱신됩니다.
      </p>
    </div>
  );
}
```

관리자 헤더에 링크를 추가한다. `apps/admin/app/chats/layout.tsx` 의 헤더에서:

```tsx
        <a href="/chats" style={{ fontSize: 12 }}>상담</a>
        <a href="/chats/ops" style={{ fontSize: 12 }}>운영 현황</a>
```

- [ ] **Step 5: 테스트 통과 확인**

```bash
npx vitest run tests/metrics.test.ts --root apps/api
npm run build -w packages/shared
npm run build -w apps/admin
```

기대: 6 passed, 빌드 성공.

- [ ] **Step 6: 커밋**

```bash
git add apps/api apps/admin packages/shared
git commit -m "feat: add operations metrics endpoint and dashboard"
```

---

## Task 5: E2E 테스트 (고객 ↔ 운영자 전 과정)

**Files:**
- Modify: `tests/e2e/playwright.config.ts`
- Create: `tests/e2e/helpers.ts`
- Create: `tests/e2e/operator-flow.spec.ts`
- Create: `tests/e2e/scroll-quality.spec.ts`

**Interfaces:**
- Consumes: Phase 2~5 전체
- Produces: `npm run test:e2e` 로 실행되는 전체 시나리오 검증

---

- [ ] **Step 1: `tests/e2e/playwright.config.ts` 수정**

관리자 화면(3100)과 홈페이지(8080)를 둘 다 쓰므로 `baseURL` 을 지정하지 않고 절대 주소를 쓴다.

```ts
import { defineConfig, devices } from '@playwright/test';

export default defineConfig({
  testDir: '.',
  timeout: 60_000,
  expect: { timeout: 10_000 },
  workers: 1,
  fullyParallel: false,
  reporter: [['list'], ['html', { open: 'never', outputFolder: '../../playwright-report' }]],
  use: {
    trace: 'retain-on-failure',
    screenshot: 'only-on-failure',
    video: 'retain-on-failure'
  },
  projects: [{ name: 'chromium', use: { ...devices['Desktop Chrome'] } }]
});

export const WEBSITE_URL = 'http://localhost:8080';
export const ADMIN_URL = 'http://localhost:3100';
export const API_URL = 'http://localhost:4000';
```

- [ ] **Step 2: `tests/e2e/helpers.ts` 작성**

```ts
import type { Browser, Page } from '@playwright/test';
import { expect } from '@playwright/test';

export const WEBSITE_URL = 'http://localhost:8080';
export const ADMIN_URL = 'http://localhost:3100';

export const ADMIN_EMAIL = process.env.SEED_ADMIN_EMAIL ?? 'admin@stemcarejapan.local';
export const ADMIN_PASSWORD = process.env.SEED_ADMIN_PASSWORD ?? 'change-me-1234';

/** 고객 브라우저 창을 열고 상담을 시작한다. */
export async function startCustomerChat(
  page: Page,
  options: { language?: 'ko' | 'ja'; message?: string; name?: string } = {}
): Promise<void> {
  await page.goto(`${WEBSITE_URL}/korea-travel/`);
  await page.evaluate(() => window.localStorage.clear());
  await page.reload();

  await page.locator('.consult-chat-toggle').click();
  await page.selectOption('select[name="serviceType"]', 'korea_travel');
  await page.fill('input[name="name"]', options.name ?? 'E2E 고객');
  await page.fill('input[name="phone"]', '010-1234-5678');
  await page.selectOption('select[name="preferredLanguage"]', options.language ?? 'ja');
  await page.fill(
    'textarea[name="message"]',
    options.message ?? '来月、母と2人で韓国に行きたいです。'
  );
  await page.check('input[name="privacyAgreed"]');
  await page.locator('.consult-chat-submit').click();

  await expect(page.locator('.consult-chat-thread')).toBeVisible();
}

/** 운영자 브라우저 창을 열고 로그인한다. */
export async function loginOperator(browser: Browser): Promise<Page> {
  const context = await browser.newContext();
  const page = await context.newPage();

  await page.goto(`${ADMIN_URL}/login`);
  await page.fill('input[type="email"]', ADMIN_EMAIL);
  await page.fill('input[type="password"]', ADMIN_PASSWORD);
  await page.locator('button[type="submit"]').click();

  await expect(page).toHaveURL(/\/chats/);
  return page;
}

/** 고객 이름으로 상담을 찾아 연다. */
export async function openRoomByCustomerName(page: Page, name: string): Promise<void> {
  await page.locator('a', { hasText: name }).first().click();
  await expect(page.locator('.consult-chat-thread, [data-testid="message-time"]').first()).toBeVisible({
    timeout: 15_000
  });
}
```

- [ ] **Step 3: `tests/e2e/operator-flow.spec.ts` 작성**

```ts
import { expect, test } from '@playwright/test';
import { loginOperator, startCustomerChat, WEBSITE_URL } from './helpers';

test.describe.configure({ mode: 'serial' });

test('고객 상담 접수부터 종료까지 전 과정', async ({ browser }) => {
  const customerName = `E2E고객_${Date.now()}`;

  // --- 고객 창 ---
  const customerContext = await browser.newContext();
  const customerPage = await customerContext.newPage();
  await startCustomerChat(customerPage, {
    language: 'ja',
    name: customerName,
    message: '来月、母と2人で韓国に行きたいです。'
  });

  // 1) 고객 첫 문의가 보인다
  await expect(
    customerPage.locator('.consult-chat-msg', { hasText: '来月、母と2人で韓国に行きたいです。' })
  ).toBeVisible();

  // 2) AI 인사가 왔다
  await expect(customerPage.locator('.consult-chat-msg[data-sender="ai"]').first()).toBeVisible({
    timeout: 15_000
  });

  // --- 운영자 창 ---
  const operatorPage = await loginOperator(browser);

  // 3) 신규 상담이 목록에 나타난다 (소켓으로 즉시)
  await expect(operatorPage.locator('a', { hasText: customerName })).toBeVisible({ timeout: 15_000 });

  // 4) 상담방에 들어간다
  await operatorPage.locator('a', { hasText: customerName }).first().click();
  await expect(operatorPage.locator('text=来月、母と2人で韓国に行きたいです。')).toBeVisible();

  // 5) 고객 정보 패널에 연락처가 보인다
  await expect(operatorPage.locator('text=010-1234-5678')).toBeVisible();

  // 6) 배정한다
  await operatorPage.locator('button', { hasText: '내가 상담하기' }).click();
  await expect(operatorPage.locator('text=진행 중').first()).toBeVisible();

  // 7) 고객 화면에도 상태가 반영된다
  await expect(customerPage.locator('[data-role="status-label"]')).toContainText('운영자 상담 중', {
    timeout: 15_000
  });

  // 8) 운영자가 한국어로 답변한다 (번역 미리보기 → 전송)
  await operatorPage.locator('.consult-chat-composer textarea, textarea').last().fill('');
  await operatorPage
    .locator('textarea')
    .last()
    .fill('방문 예정일과 인원을 알려주시면 일정을 도와드리겠습니다.');
  await operatorPage.locator('button', { hasText: /번역 확인|전송/ }).click();

  // 번역 미리보기가 뜨면 그대로 전송한다
  const sendAsIs = operatorPage.locator('button', { hasText: '이대로 전송' });
  if (await sendAsIs.isVisible({ timeout: 20_000 }).catch(() => false)) {
    await sendAsIs.click();
  }

  // 9) 고객 화면에 답변이 도착한다
  await expect(customerPage.locator('.consult-chat-msg[data-sender="operator"]')).toBeVisible({
    timeout: 20_000
  });

  // 10) 운영자가 메모를 남긴다
  await operatorPage.locator('textarea[placeholder*="내부 메모"]').fill('항공권 일정 확인 필요');
  await operatorPage.locator('button', { hasText: '메모 저장' }).click();
  await expect(operatorPage.locator('text=항공권 일정 확인 필요')).toBeVisible();

  // 11) 상담을 종료한다
  await operatorPage.locator('button', { hasText: '상담 종료' }).click();
  await expect(operatorPage.locator('text=완료').first()).toBeVisible();

  // 12) 고객 화면 입력창이 잠긴다
  await expect(customerPage.locator('[data-role="status-label"]')).toContainText('상담 종료', {
    timeout: 15_000
  });
  await expect(customerPage.locator('.consult-chat-composer textarea')).toBeDisabled();

  await customerContext.close();
  await operatorPage.context().close();
});

test('운영자 메모는 고객 화면에 절대 보이지 않는다', async ({ browser }) => {
  const customerName = `비밀메모_${Date.now()}`;
  const secretNote = `내부전용_${Date.now()}`;

  const customerContext = await browser.newContext();
  const customerPage = await customerContext.newPage();
  await startCustomerChat(customerPage, { name: customerName, language: 'ko', message: '문의합니다' });

  const operatorPage = await loginOperator(browser);
  await operatorPage.locator('a', { hasText: customerName }).first().click({ timeout: 15_000 });
  await operatorPage.locator('textarea[placeholder*="내부 메모"]').fill(secretNote);
  await operatorPage.locator('button', { hasText: '메모 저장' }).click();
  await expect(operatorPage.locator(`text=${secretNote}`)).toBeVisible();

  // 고객 화면을 새로고침해도 메모가 없어야 한다
  await customerPage.reload();
  await customerPage.locator('.consult-chat-toggle').click();
  await expect(customerPage.locator('.consult-chat-thread')).toBeVisible();
  await expect(customerPage.locator(`text=${secretNote}`)).toHaveCount(0);

  await customerContext.close();
  await operatorPage.context().close();
});

test('로그인 없이 관리자 화면에 접근할 수 없다', async ({ page }) => {
  await page.goto('http://localhost:3100/chats');

  await expect(page).toHaveURL(/\/login/);
  await expect(page.locator('input[type="password"]')).toBeVisible();
});

test('모바일 화면에서도 상담을 시작할 수 있다', async ({ browser }) => {
  const context = await browser.newContext({ viewport: { width: 390, height: 844 } });
  const page = await context.newPage();

  await page.goto(`${WEBSITE_URL}/korea-travel/`);
  await page.evaluate(() => window.localStorage.clear());
  await page.reload();

  await page.locator('.consult-chat-toggle').click();
  await expect(page.locator('.consult-chat-form')).toBeVisible();

  // 가로 스크롤이 생기면 안 된다
  const hasHorizontalScroll = await page.evaluate(
    () => document.documentElement.scrollWidth > document.documentElement.clientWidth
  );
  expect(hasHorizontalScroll).toBe(false);

  await context.close();
});
```

- [ ] **Step 4: `tests/e2e/scroll-quality.spec.ts` 작성**

스펙 3절이 요구하는 스크롤 품질을 검증한다.

```ts
import { expect, test } from '@playwright/test';
import { loginOperator, startCustomerChat } from './helpers';

test.describe.configure({ mode: 'serial' });

test('고객: 예전 메시지를 읽는 중에는 화면이 움직이지 않는다', async ({ browser }) => {
  const customerName = `스크롤고객_${Date.now()}`;

  const customerContext = await browser.newContext();
  const customerPage = await customerContext.newPage();
  await startCustomerChat(customerPage, { name: customerName, language: 'ko', message: '시작' });

  // 메시지를 충분히 쌓는다
  for (let i = 0; i < 15; i += 1) {
    await customerPage.fill('.consult-chat-composer textarea', `고객 메시지 ${i}`);
    await customerPage.locator('.consult-chat-composer button').click();
    await expect(
      customerPage.locator('.consult-chat-msg', { hasText: `고객 메시지 ${i}` })
    ).toBeVisible();
  }

  // 맨 위로 올린다
  await customerPage.locator('.consult-chat-thread').evaluate((el) => {
    el.scrollTop = 0;
  });
  const before = await customerPage
    .locator('.consult-chat-thread')
    .evaluate((el) => el.scrollTop);

  // 운영자가 새 메시지를 보낸다
  const operatorPage = await loginOperator(browser);
  await operatorPage.locator('a', { hasText: customerName }).first().click({ timeout: 15_000 });
  await operatorPage.locator('button', { hasText: '내가 상담하기' }).click();
  await operatorPage.locator('textarea').last().fill('운영자가 보낸 새 메시지');
  await operatorPage.locator('button', { hasText: /번역 확인|전송/ }).click();
  const sendAsIs = operatorPage.locator('button', { hasText: '이대로 전송' });
  if (await sendAsIs.isVisible({ timeout: 20_000 }).catch(() => false)) {
    await sendAsIs.click();
  }

  // 새 메시지 버튼이 뜨고, 스크롤 위치는 그대로여야 한다
  await expect(customerPage.locator('[data-role="newmsg"]')).toBeVisible({ timeout: 20_000 });
  const after = await customerPage.locator('.consult-chat-thread').evaluate((el) => el.scrollTop);
  expect(after).toBe(before);

  // 버튼을 누르면 맨 아래로 간다
  await customerPage.locator('[data-role="newmsg"]').click();
  await customerPage.waitForTimeout(600);
  const atBottom = await customerPage
    .locator('.consult-chat-thread')
    .evaluate((el) => el.scrollHeight - el.scrollTop - el.clientHeight < 20);
  expect(atBottom).toBe(true);

  await customerContext.close();
  await operatorPage.context().close();
});

test('운영자: 번역문이 늦게 붙어 말풍선 높이가 바뀌어도 위치가 유지된다', async ({ browser }) => {
  const customerName = `번역스크롤_${Date.now()}`;

  const customerContext = await browser.newContext();
  const customerPage = await customerContext.newPage();
  await startCustomerChat(customerPage, { name: customerName, language: 'ja', message: 'はじめまして' });

  const operatorPage = await loginOperator(browser);
  await operatorPage.locator('a', { hasText: customerName }).first().click({ timeout: 15_000 });

  // 메시지를 쌓는다
  for (let i = 0; i < 12; i += 1) {
    await customerPage.fill('.consult-chat-composer textarea', `メッセージ ${i}`);
    await customerPage.locator('.consult-chat-composer button').click();
    await customerPage.waitForTimeout(200);
  }

  // 운영자 화면에서 위로 올린다
  const thread = operatorPage.locator('[style*="overflow-y"]').first();
  await operatorPage.waitForTimeout(1500);
  await thread.evaluate((el) => {
    el.scrollTop = 0;
  });
  const before = await thread.evaluate((el) => el.scrollTop);

  // 번역이 붙을 시간을 준다 (말풍선 높이가 커진다)
  await operatorPage.waitForTimeout(6000);
  const after = await thread.evaluate((el) => el.scrollTop);

  // 위치가 크게 흔들리지 않아야 한다 (5px 이내 오차 허용)
  expect(Math.abs(after - before)).toBeLessThanOrEqual(5);

  await customerContext.close();
  await operatorPage.context().close();
});

test('운영자: 처음 여는 상담방은 최신에서 시작하고 다시 여는 상담방은 읽던 위치를 복원한다', async ({ browser }) => {
  const nameA = `방A_${Date.now()}`;
  const nameB = `방B_${Date.now()}`;

  for (const name of [nameA, nameB]) {
    const ctx = await browser.newContext();
    const page = await ctx.newPage();
    await startCustomerChat(page, { name, language: 'ko', message: '안녕하세요' });
    for (let i = 0; i < 12; i += 1) {
      await page.fill('.consult-chat-composer textarea', `${name} 메시지 ${i}`);
      await page.locator('.consult-chat-composer button').click();
      await page.waitForTimeout(150);
    }
    await ctx.close();
  }

  const operatorPage = await loginOperator(browser);
  await operatorPage.locator('a', { hasText: nameA }).first().click({ timeout: 15_000 });
  await operatorPage.waitForTimeout(1200);

  const thread = operatorPage.locator('[style*="overflow-y"]').first();
  await thread.evaluate((el) => {
    el.scrollTop = 120;
  });
  const savedTop = await thread.evaluate((el) => el.scrollTop);

  await operatorPage.locator('a', { hasText: nameB }).first().click();
  await operatorPage.waitForTimeout(1200);

  const firstOpenAtBottom = await thread.evaluate(
    (el) => el.scrollHeight - el.scrollTop - el.clientHeight < 30
  );
  expect(firstOpenAtBottom).toBe(true);

  await operatorPage.locator('a', { hasText: nameA }).first().click();
  await operatorPage.waitForTimeout(1200);

  const restoredTop = await thread.evaluate((el) => el.scrollTop);
  expect(Math.abs(restoredTop - savedTop)).toBeLessThanOrEqual(5);

  await operatorPage.context().close();
});
```

- [ ] **Step 5: E2E 실행**

세 서비스를 모두 띄우고 실행한다.

```bash
docker compose up -d web postgres
npm run db:seed -w apps/api
npm run dev:api &
npm run dev:admin &
sleep 10
npm run test:e2e
```

기대: 모든 스펙 통과.

실패하면 리포트를 본다.
```bash
npx playwright show-report playwright-report
```

- [ ] **Step 6: 커밋**

```bash
git add tests/e2e
git commit -m "test: add end-to-end operator flow and scroll quality tests"
```

---

## Task 6: 운영 배포 구성 (Nginx + HTTPS + Docker)

**Files:**
- Create: `deploy/Dockerfile.api`
- Create: `deploy/Dockerfile.admin`
- Create: `deploy/nginx/site.conf`
- Create: `deploy/docker-compose.prod.yml`
- Create: `deploy/.env.prod.example`
- Create: `deploy/DEPLOY.md`

**Interfaces:**
- Consumes: 전체 애플리케이션
- Produces: 한 도메인에서 `/`(홈페이지), `/admin`(관리자), `/api`·`/socket.io`(API)를 서비스하는 구성

---

- [ ] **Step 1: `deploy/Dockerfile.api` 작성**

```dockerfile
# ---------- 빌드 단계 ----------
FROM node:22-alpine AS builder

WORKDIR /app

# 의존성 설치에 필요한 파일만 먼저 복사한다.
# 소스가 바뀌어도 이 층은 캐시가 유지되어 빌드가 빨라진다.
COPY package.json package-lock.json ./
COPY packages/shared/package.json packages/shared/
COPY apps/api/package.json apps/api/
RUN npm ci --workspaces --include-workspace-root

COPY packages/shared packages/shared
COPY apps/api apps/api

RUN npm run build -w packages/shared
RUN npx prisma generate --schema apps/api/prisma/schema.prisma
RUN npm run build -w apps/api

# ---------- 실행 단계 ----------
FROM node:22-alpine AS runner

WORKDIR /app
ENV NODE_ENV=production

COPY package.json package-lock.json ./
COPY packages/shared/package.json packages/shared/
COPY apps/api/package.json apps/api/
RUN npm ci --workspaces --include-workspace-root --omit=dev

COPY --from=builder /app/packages/shared/dist packages/shared/dist
COPY --from=builder /app/apps/api/dist apps/api/dist
COPY --from=builder /app/apps/api/prisma apps/api/prisma
COPY --from=builder /app/node_modules/.prisma node_modules/.prisma

# root 로 실행하지 않는다.
USER node

EXPOSE 4000

# 컨테이너가 시작될 때 마이그레이션을 적용하고 서버를 띄운다.
CMD ["sh", "-c", "npx prisma migrate deploy --schema apps/api/prisma/schema.prisma && node apps/api/dist/server.js"]
```

- [ ] **Step 2: `deploy/Dockerfile.admin` 작성**

```dockerfile
FROM node:22-alpine AS builder

WORKDIR /app

COPY package.json package-lock.json ./
COPY packages/shared/package.json packages/shared/
COPY apps/admin/package.json apps/admin/
RUN npm ci --workspaces --include-workspace-root

COPY packages/shared packages/shared
COPY apps/admin apps/admin

RUN npm run build -w packages/shared

# Next.js 는 빌드 시점에 NEXT_PUBLIC_* 값을 코드에 박아 넣는다.
# 그래서 이 값은 런타임이 아니라 빌드 인자로 받아야 한다.
ARG NEXT_PUBLIC_API_URL
ENV NEXT_PUBLIC_API_URL=$NEXT_PUBLIC_API_URL
RUN npm run build -w apps/admin

FROM node:22-alpine AS runner

WORKDIR /app
ENV NODE_ENV=production

COPY package.json package-lock.json ./
COPY packages/shared/package.json packages/shared/
COPY apps/admin/package.json apps/admin/
RUN npm ci --workspaces --include-workspace-root --omit=dev

COPY --from=builder /app/packages/shared/dist packages/shared/dist
COPY --from=builder /app/apps/admin/.next apps/admin/.next
COPY --from=builder /app/apps/admin/next.config.ts apps/admin/

USER node
EXPOSE 3100

CMD ["npm", "run", "start", "-w", "apps/admin"]
```

`NEXT_PUBLIC_API_URL` 을 빌드 인자로 받는 이유가 중요하다. Next.js는 `NEXT_PUBLIC_` 변수를 **빌드할 때** JavaScript 파일 안에 문자열로 박아 넣는다. 컨테이너 실행 시점에 환경변수를 바꿔도 이미 만들어진 파일은 안 바뀐다.

- [ ] **Step 3: `deploy/nginx/site.conf` 작성**

```nginx
# HTTP 로 온 요청은 전부 HTTPS 로 보낸다.
server {
    listen 80;
    server_name ${DOMAIN};

    # Let's Encrypt 인증서 갱신용 경로만 예외로 둔다.
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    http2 on;
    server_name ${DOMAIN};

    ssl_certificate     /etc/letsencrypt/live/${DOMAIN}/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/${DOMAIN}/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers off;

    # 브라우저에게 "앞으로 이 도메인은 항상 HTTPS 로만 접속하라"고 알린다.
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    client_max_body_size 5m;

    # ---------- API ----------
    location /api/ {
        proxy_pass http://api:4000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # ---------- Socket.IO ----------
    # WebSocket 은 Upgrade 헤더를 반드시 넘겨야 한다. 이게 빠지면 연결이 계속 실패한다.
    location /socket.io/ {
        proxy_pass http://api:4000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        # 소켓은 오래 열려 있으므로 타임아웃을 늘린다.
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }

    # ---------- 관리자 화면 ----------
    location /admin {
        proxy_pass http://admin:3100;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # ---------- 정적 홈페이지 ----------
    location / {
        proxy_pass http://website:80;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

관리자 앱을 `/admin` 경로에 두려면 `apps/admin/next.config.ts` 에 `basePath` 를 추가해야 한다.

```ts
const nextConfig: NextConfig = {
  reactStrictMode: true,
  transpilePackages: ['@stemcare/shared'],
  basePath: process.env.NEXT_BASE_PATH || undefined
};
```

빌드 시 `NEXT_BASE_PATH=/admin` 을 넘긴다.

- [ ] **Step 4: `deploy/docker-compose.prod.yml` 작성**

```yaml
services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 10
    # 외부에 포트를 노출하지 않는다. 같은 네트워크의 api 만 접근한다.

  api:
    build:
      context: ..
      dockerfile: deploy/Dockerfile.api
    restart: unless-stopped
    environment:
      NODE_ENV: production
      API_PORT: 4000
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
      JWT_SECRET: ${JWT_SECRET}
      ADMIN_COOKIE_NAME: scj_admin_token
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      AI_MODEL: ${AI_MODEL}
      TRANSLATION_MODEL: ${TRANSLATION_MODEL}
      WEBSITE_ORIGIN: https://${DOMAIN}
      ADMIN_ORIGIN: https://${DOMAIN}
    depends_on:
      postgres:
        condition: service_healthy

  admin:
    build:
      context: ..
      dockerfile: deploy/Dockerfile.admin
      args:
        # 같은 도메인이므로 상대 경로를 쓴다. CORS 문제가 사라진다.
        NEXT_PUBLIC_API_URL: https://${DOMAIN}
        NEXT_BASE_PATH: /admin
    restart: unless-stopped
    depends_on:
      - api

  website:
    build:
      context: ..
      dockerfile: Dockerfile
    restart: unless-stopped

  nginx:
    image: nginx:alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    environment:
      DOMAIN: ${DOMAIN}
    volumes:
      - ./nginx/site.conf:/etc/nginx/templates/site.conf.template:ro
      - certbot_conf:/etc/letsencrypt:ro
      - certbot_www:/var/www/certbot:ro
    depends_on:
      - api
      - admin
      - website

  certbot:
    image: certbot/certbot
    volumes:
      - certbot_conf:/etc/letsencrypt
      - certbot_www:/var/www/certbot
    # 12시간마다 인증서 갱신을 시도한다.
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done;'"

  backup:
    image: postgres:16
    restart: unless-stopped
    environment:
      PGPASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - ./backup:/backup
      - backup_data:/backups
    entrypoint: ["/bin/sh", "/backup/backup.sh"]
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  postgres_data:
  certbot_conf:
  certbot_www:
  backup_data:
```

`nginx:alpine` 이미지는 `/etc/nginx/templates/*.template` 안의 `${VAR}` 를 환경변수로 치환한 뒤 `/etc/nginx/conf.d/` 에 넣어준다. 그래서 도메인을 환경변수로 관리할 수 있다.

- [ ] **Step 5: `deploy/.env.prod.example` 작성**

```env
# 이 파일을 .env 로 복사하고 실제 값을 채운다. .env 는 절대 Git 에 올리지 않는다.

DOMAIN=chat.stemcarejapan.example

POSTGRES_USER=stemcare
POSTGRES_PASSWORD=여기에-긴-무작위-비밀번호
POSTGRES_DB=stemcare_chat

# openssl rand -base64 48 로 생성한다. 32자 이상이어야 서버가 뜬다.
JWT_SECRET=여기에-48바이트-무작위-문자열

OPENAI_API_KEY=sk-...
AI_MODEL=gpt-5-mini
TRANSLATION_MODEL=gpt-5-mini

# 초기 관리자 계정 (첫 배포 후 반드시 비밀번호를 변경한다)
SEED_ADMIN_EMAIL=admin@stemcarejapan.example
SEED_ADMIN_PASSWORD=첫-로그인-후-반드시-변경
```

- [ ] **Step 6: `deploy/DEPLOY.md` 작성**

````markdown
# 운영 배포 절차

## 준비물

- 도메인 (예: `chat.stemcarejapan.example`) 이 서버 IP 를 가리키도록 DNS A 레코드 설정
- Docker 와 Docker Compose 가 설치된 리눅스 서버
- OpenAI API 키

## 1. 소스 준비

```bash
git clone <저장소> stemcarejapan-website
cd stemcarejapan-website/deploy
cp .env.prod.example .env
```

`.env` 를 열어 값을 채운다. 특히:

```bash
# 안전한 비밀번호와 시크릿 생성
openssl rand -base64 32   # POSTGRES_PASSWORD 에 사용
openssl rand -base64 48   # JWT_SECRET 에 사용
```

## 2. HTTPS 인증서 발급 (최초 1회)

먼저 HTTP 만으로 nginx 를 띄워 인증서를 받는다.

```bash
docker compose -f docker-compose.prod.yml up -d nginx
docker compose -f docker-compose.prod.yml run --rm certbot certonly \
  --webroot -w /var/www/certbot \
  -d $DOMAIN --email 운영자@이메일 --agree-tos --no-eff-email
docker compose -f docker-compose.prod.yml restart nginx
```

## 3. 전체 배포

```bash
docker compose -f docker-compose.prod.yml up -d --build
docker compose -f docker-compose.prod.yml ps
```

모든 서비스가 `running` 이어야 한다.

## 4. 관리자 계정 생성

```bash
docker compose -f docker-compose.prod.yml exec api \
  npx tsx apps/api/prisma/seed.ts
```

**로그인 후 즉시 비밀번호를 변경한다.**

## 5. 동작 확인

```bash
curl -sI https://$DOMAIN | head -1                # 200
curl -s https://$DOMAIN/api/health                # {"status":"ok",...}
```

브라우저로:
- `https://$DOMAIN/` → 홈페이지, 우하단 상담 위젯
- `https://$DOMAIN/korea-travel/` → 상담 시작
- `https://$DOMAIN/admin` → 운영자 로그인

개발자도구 Network → WS 탭에서 `socket.io` 연결이 `101` 인지 확인한다.

## 6. 업데이트 배포

```bash
git pull
docker compose -f docker-compose.prod.yml up -d --build
```

마이그레이션은 API 컨테이너가 시작될 때 자동 적용된다.

## 로그 보기

```bash
docker compose -f docker-compose.prod.yml logs -f api
docker compose -f docker-compose.prod.yml logs -f nginx
```

## 롤백

```bash
git checkout <이전-커밋>
docker compose -f docker-compose.prod.yml up -d --build
```

**주의**: DB 마이그레이션은 자동으로 되돌아가지 않는다. 컬럼을 삭제하는 마이그레이션을
배포한 뒤 롤백하려면 백업에서 복구해야 한다. 그래서 컬럼 삭제는
"코드에서 사용 중단" → "며칠 관찰" → "다음 배포에서 삭제" 순서로 나눠서 한다.
````

- [ ] **Step 7: 로컬에서 프로덕션 빌드 확인**

실제 서버 없이 이미지가 만들어지는지만 확인한다.

```bash
docker build -f deploy/Dockerfile.api -t scj-api-test .
docker build -f deploy/Dockerfile.admin --build-arg NEXT_PUBLIC_API_URL=https://example.com -t scj-admin-test .
```

기대: 두 이미지 모두 빌드 성공.

- [ ] **Step 8: 커밋**

```bash
git add deploy apps/admin/next.config.ts
git commit -m "feat: add production deployment with nginx, https and docker compose"
```

---

## Task 7: 백업과 복구

**Files:**
- Create: `deploy/backup/backup.sh`
- Create: `deploy/backup/restore.sh`
- Create: `deploy/backup/RESTORE-DRILL.md`

**Interfaces:**
- Consumes: PostgreSQL 컨테이너
- Produces: 매일 자동 백업, 7일 보관, 복구 절차

---

- [ ] **Step 1: `deploy/backup/backup.sh` 작성**

```bash
#!/bin/sh
set -eu

# 매일 새벽 3시에 백업하고 7일치만 보관한다.
# 컨테이너 안에서 무한 루프로 돌며, 다음 3시까지 잠든다.

BACKUP_DIR=/backups
RETENTION_DAYS=7

mkdir -p "$BACKUP_DIR"

run_backup() {
  timestamp=$(date +%Y%m%d_%H%M%S)
  target="$BACKUP_DIR/stemcare_chat_$timestamp.sql.gz"

  echo "[backup] 시작: $target"

  # -Fc 대신 평문 SQL 을 쓰는 이유: 사람이 열어볼 수 있고 복구가 단순하다.
  if pg_dump -h postgres -U "$POSTGRES_USER" -d "$POSTGRES_DB" | gzip > "$target"; then
    echo "[backup] 완료: $(du -h "$target" | cut -f1)"
  else
    echo "[backup] 실패!" >&2
    rm -f "$target"
    return 1
  fi

  # 오래된 백업 삭제
  find "$BACKUP_DIR" -name 'stemcare_chat_*.sql.gz' -mtime +"$RETENTION_DAYS" -delete
  echo "[backup] 보관 중인 백업: $(ls -1 "$BACKUP_DIR" | wc -l) 개"
}

# 컨테이너 시작 시 한 번 백업한다. (설정이 맞는지 바로 알 수 있다)
run_backup || true

while true; do
  # 다음 새벽 3시까지 대기
  now=$(date +%s)
  next=$(date -d 'tomorrow 03:00' +%s 2>/dev/null || date -v+1d -v3H -v0M -v0S +%s)
  sleep $((next - now))

  run_backup || echo "[backup] 이번 회차 실패. 다음 회차에 재시도합니다." >&2
done
```

실행 권한을 준다.

```bash
chmod +x deploy/backup/backup.sh
```

`POSTGRES_USER`, `POSTGRES_DB` 환경변수를 backup 서비스에 넘겨야 하므로 `docker-compose.prod.yml` 의 `backup` 서비스 environment에 추가한다.

```yaml
    environment:
      PGPASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_DB: ${POSTGRES_DB}
```

- [ ] **Step 2: `deploy/backup/restore.sh` 작성**

```bash
#!/bin/sh
set -eu

# 사용법: ./restore.sh <백업파일.sql.gz>
#
# 주의: 이 스크립트는 현재 DB 를 덮어쓴다.
# 운영 DB 에 실행하기 전에 반드시 현재 상태를 먼저 백업한다.

if [ $# -ne 1 ]; then
  echo "사용법: $0 <백업파일.sql.gz>" >&2
  exit 1
fi

BACKUP_FILE=$1

if [ ! -f "$BACKUP_FILE" ]; then
  echo "파일을 찾을 수 없습니다: $BACKUP_FILE" >&2
  exit 1
fi

echo "복구 대상 DB: $POSTGRES_DB"
echo "복구할 백업: $BACKUP_FILE"
printf "정말 진행하시겠습니까? 현재 데이터가 모두 사라집니다. (yes 입력): "
read -r answer

if [ "$answer" != "yes" ]; then
  echo "취소했습니다."
  exit 0
fi

echo "[restore] 기존 스키마 삭제 중…"
psql -h postgres -U "$POSTGRES_USER" -d "$POSTGRES_DB" \
  -c "DROP SCHEMA public CASCADE; CREATE SCHEMA public;"

echo "[restore] 백업 복원 중…"
gunzip -c "$BACKUP_FILE" | psql -h postgres -U "$POSTGRES_USER" -d "$POSTGRES_DB"

echo "[restore] 완료. 테이블 확인:"
psql -h postgres -U "$POSTGRES_USER" -d "$POSTGRES_DB" -c "\dt"
```

```bash
chmod +x deploy/backup/restore.sh
```

- [ ] **Step 3: `deploy/backup/RESTORE-DRILL.md` 작성**

````markdown
# 복구 리허설 (월 1회 실행)

**"백업이 있다"와 "복구할 수 있다"는 다르다.** 실제로 복구해본 적이 없는 백업은
없는 것과 같다. 한 달에 한 번, 아래 절차를 그대로 실행한다.

## 준비: 리허설용 DB 만들기

운영 DB 를 건드리지 않고 별도 DB 에 복구해본다.

```bash
cd deploy

# 1) 리허설용 DB 생성
docker compose -f docker-compose.prod.yml exec postgres \
  psql -U $POSTGRES_USER -d postgres -c "DROP DATABASE IF EXISTS restore_drill;"
docker compose -f docker-compose.prod.yml exec postgres \
  psql -U $POSTGRES_USER -d postgres -c "CREATE DATABASE restore_drill;"
```

## 실행

```bash
# 2) 가장 최근 백업 파일 확인
docker compose -f docker-compose.prod.yml exec backup ls -lt /backups | head -5

# 3) 리허설 DB 에 복구
docker compose -f docker-compose.prod.yml exec backup sh -c \
  'gunzip -c /backups/$(ls -t /backups | head -1) | psql -h postgres -U $POSTGRES_USER -d restore_drill'
```

## 검증 (모두 통과해야 한다)

```bash
docker compose -f docker-compose.prod.yml exec postgres \
  psql -U $POSTGRES_USER -d restore_drill -c "\dt"
# → 6개 테이블 + _prisma_migrations 가 보인다

docker compose -f docker-compose.prod.yml exec postgres \
  psql -U $POSTGRES_USER -d restore_drill -c "SELECT count(*) FROM chat_rooms;"
# → 운영 DB 와 비슷한 건수

docker compose -f docker-compose.prod.yml exec postgres \
  psql -U $POSTGRES_USER -d restore_drill -c "SELECT count(*) FROM messages;"
# → 0 이 아니다

docker compose -f docker-compose.prod.yml exec postgres \
  psql -U $POSTGRES_USER -d restore_drill \
  -c "SELECT id, status, created_at FROM chat_rooms ORDER BY created_at DESC LIMIT 3;"
# → 최근 상담이 실제로 들어 있다
```

## 정리

```bash
docker compose -f docker-compose.prod.yml exec postgres \
  psql -U $POSTGRES_USER -d postgres -c "DROP DATABASE restore_drill;"
```

## 기록

| 날짜 | 실행자 | 백업 파일 | 결과 | 비고 |
|---|---|---|---|---|
| | | | | |

리허설이 실패하면 **배포보다 우선순위가 높은 문제**다. 즉시 원인을 찾는다.
````

- [ ] **Step 4: 로컬에서 백업/복구 리허설**

개발 환경으로 한 번 연습해본다.

```bash
# 1) 백업
docker compose exec postgres pg_dump -U stemcare -d stemcare_chat | gzip > /tmp/drill.sql.gz
ls -lh /tmp/drill.sql.gz

# 2) 리허설 DB 만들고 복구
docker compose exec postgres psql -U stemcare -d postgres -c "DROP DATABASE IF EXISTS drill;"
docker compose exec postgres psql -U stemcare -d postgres -c "CREATE DATABASE drill;"
gunzip -c /tmp/drill.sql.gz | docker compose exec -T postgres psql -U stemcare -d drill

# 3) 검증
docker compose exec postgres psql -U stemcare -d drill -c "\dt"
docker compose exec postgres psql -U stemcare -d drill -c "SELECT count(*) FROM chat_rooms;"

# 4) 정리
docker compose exec postgres psql -U stemcare -d postgres -c "DROP DATABASE drill;"
rm /tmp/drill.sql.gz
```

기대: 3번에서 6개 테이블과 상담 건수가 보인다.

- [ ] **Step 5: 커밋**

```bash
git add deploy/backup deploy/docker-compose.prod.yml
git commit -m "feat: add automated database backup and restore drill procedure"
```

---

## Task 8: 장애 대응 매뉴얼과 배포 전 체크리스트

**Files:**
- Create: `CHECKLIST.md`
- Create: `deploy/RUNBOOK.md`
- Modify: `README-chat.md`

**Interfaces:**
- Consumes: Task 1~7 전부
- Produces: 배포 판단 기준과 장애 대응 절차

---

- [ ] **Step 1: `CHECKLIST.md` 작성**

````markdown
# 배포 전 체크리스트

**모든 항목을 통과해야 배포한다.** 하나라도 미확인이면 배포하지 않는다.

## 1. 자동 테스트

```bash
npm run test -w apps/api          # [ ] 통과
npm run test -w apps/admin        # [ ] 통과
npm run test:e2e                  # [ ] 통과
npm run build                     # [ ] 통과
```

## 2. 고객 위젯 수동 확인

- [ ] `/korea-travel/`, `/stemcell/`, `/` 세 페이지에서 위젯이 열린다
- [ ] 개인정보 동의 없이 시작하면 막힌다 (Network 탭에 요청이 안 나간다)
- [ ] 이름/연락처 누락 시 안내가 나온다
- [ ] 상담 시작 → AI 인사가 고객 언어로 온다
- [ ] 메시지 전송 → 즉시 화면에 보인다
- [ ] 새로고침 → 대화가 유지된다
- [ ] 오프라인 전송 → "전송 실패 · 다시 시도" 가 나오고 재시도가 된다
- [ ] 위로 스크롤 상태에서 새 메시지 → 화면이 안 움직이고 `새 메시지` 버튼만 뜬다
- [ ] 일본어 페이지에서 모든 문구가 일본어다 (한국어가 안 섞인다)

## 3. 운영자 화면 수동 확인

- [ ] 로그인 실패 시 안내가 나온다
- [ ] 로그인 없이 `/chats` 접근 → `/login` 으로 튕긴다
- [ ] 상담 목록에 unread 배지, 상태, 서비스, 언어가 보인다
- [ ] 필터와 정렬이 동작한다
- [ ] 상담 상세에서 원문과 번역이 구분되어 보인다
- [ ] `내가 상담하기` → 상태가 `진행 중` 이 되고 고객 화면에도 반영된다
- [ ] 메모 저장이 되고, 고객 화면에는 절대 안 보인다
- [ ] `상담 종료` → 양쪽 입력창이 잠긴다
- [ ] 운영 현황 대시보드가 열린다

## 4. 번역 확인

- [ ] 일본어 고객 메시지 → 운영자 화면에 한국어 번역이 붙는다
- [ ] 한국어 운영자 답변 → 일본어 미리보기가 뜬다
- [ ] 미리보기를 수정해서 보내면 `직접 수정` 표시가 된다
- [ ] 고객 화면에는 일본어만 보인다
- [ ] 번역 실패 시 원문은 그대로 저장·전달되고 `다시 시도` 버튼이 나온다

## 5. AI 확인

- [ ] 공항 픽업 등 기본 문의에 AI가 답한다
- [ ] 가격 문의 → AI가 답하지 않고 운영자 대기로 전환된다
- [ ] 예약 요청 → 운영자 대기로 전환된다
- [ ] 건강 상태 언급 → 운영자 대기로 전환된다
- [ ] 전환 시 사이드바에 AI 요약과 위험 태그가 보인다
- [ ] 운영자 배정 후에는 AI가 끼어들지 않는다

## 6. 실시간

- [ ] 고객 메시지가 운영자에게 즉시 도착한다
- [ ] 운영자 답변이 고객에게 즉시 도착한다
- [ ] 신규 상담이 운영자 목록에 즉시 나타난다
- [ ] 입력 중 표시가 보인다
- [ ] API 서버를 껐다 켜면 자동 재연결된다
- [ ] socket.io CDN 을 차단해도 폴링으로 동작한다

## 7. 모바일

- [ ] iOS Safari 에서 상담 시작이 된다
- [ ] Android Chrome 에서 상담 시작이 된다
- [ ] 가로 스크롤이 생기지 않는다
- [ ] 키보드가 올라와도 입력창이 가려지지 않는다

## 8. 보안

- [ ] HTTPS 로 접속된다 (자물쇠 아이콘)
- [ ] HTTP 로 접속하면 HTTPS 로 리다이렉트된다
- [ ] 관리자 쿠키가 `HttpOnly; Secure; SameSite=Lax` 다 (개발자도구 Application → Cookies)
- [ ] 로그인 없이 관리자 API 호출 → 401
- [ ] 남의 `visitorToken` 으로 상담방 접근 → 403
- [ ] 로그인 10회 연속 실패 → 429
- [ ] 서버 로그에 이름·전화·이메일·상담 본문이 없다
  ```bash
  docker compose -f deploy/docker-compose.prod.yml logs api | grep -c "010-\|@.*\.com" # → 0
  ```
- [ ] `.env` 가 Git 에 없다 (`git ls-files | grep -c '\.env$'` → 0)
- [ ] `JWT_SECRET` 이 기본값이 아니다

## 9. 개인정보

- [ ] 위젯에 개인정보 수집 동의 문구가 표시된다
- [ ] 수집 항목·목적·보관 기간이 문구에 들어 있다
- [ ] 보관 기간 정책이 코드 상수로 명시되어 있다 (`retention.ts`)
- [ ] 보관 작업이 스케줄에 등록되어 있다 (`retention_job_scheduled` 로그 확인)
- [ ] 수동 실행이 동작한다 (`npm run ops:retention -w apps/api`)

## 10. 운영 준비

- [ ] 관리자 초기 비밀번호를 변경했다
- [ ] DB 백업이 매일 실행된다 (`docker compose ... exec backup ls /backups`)
- [ ] **복구 리허설을 실제로 해봤다** (`deploy/backup/RESTORE-DRILL.md`)
- [ ] 장애 대응 매뉴얼(`deploy/RUNBOOK.md`)을 팀이 읽었다
- [ ] 운영 현황 대시보드에 접근할 수 있다
- [ ] 롤백 절차를 알고 있다
````

- [ ] **Step 2: `deploy/RUNBOOK.md` 작성**

````markdown
# 장애 대응 매뉴얼

각 항목은 "증상 → 확인 → 조치" 순서다. 당황하지 말고 위에서부터 따라간다.

## 0. 먼저 볼 곳

```bash
cd deploy
docker compose -f docker-compose.prod.yml ps            # 어떤 서비스가 죽었나
docker compose -f docker-compose.prod.yml logs --tail=100 api
curl -s https://$DOMAIN/api/health                       # API 살아 있나
```

관리자 화면 `/admin/chats/ops` 에서 지표를 본다.

---

## 1. 번역 API 장애

**증상**: 운영 현황의 `번역 실패 (24h)` 가 급증. 운영자 화면에 "번역 실패" 가 많다.

**확인**
```bash
docker compose -f docker-compose.prod.yml logs api | grep translate | tail -20
```

**조치**
1. **상담을 멈추지 않는다.** 원문 메시지는 계속 저장·전달되고 있다.
2. 운영자에게 "번역이 일시 중단됐으니 직접 작성해달라" 고 공지한다.
   - 운영자는 작성 언어를 `일본어` 로 바꿔 직접 쓸 수 있다.
3. OpenAI 상태 페이지를 확인한다.
4. 복구되면 실패한 메시지에서 `다시 시도` 를 누른다.

**공지 문구 (고객용, 필요 시)**
> 현재 자동 번역이 지연되고 있습니다. 상담은 정상적으로 접수되고 있으며 담당자가 확인 후 답변드립니다.

---

## 2. AI 응답 장애

**증상**: 상담을 시작해도 AI 인사 뒤 답변이 없다. 모든 상담이 `운영자 대기` 로 몰린다.

**확인**
```bash
docker compose -f docker-compose.prod.yml logs api | grep consultationBot | tail -20
```

**조치**
1. **이것은 설계된 동작이다.** AI가 실패하면 자동으로 운영자에게 넘어간다.
2. 운영자 인원을 늘려 대기 상담을 처리한다.
3. `OPENAI_API_KEY` 가 만료/한도 초과인지 확인한다.
4. 장기화되면 AI 없이도 운영이 가능하다. 서두르지 않는다.

---

## 3. 실시간 연결 장애

**증상**: 관리자 화면 헤더에 `재연결 중 · 주기 조회로 동작` 이 계속 뜬다.

**확인**
```bash
docker compose -f docker-compose.prod.yml logs nginx | grep -i "upgrade\|websocket" | tail
docker compose -f docker-compose.prod.yml logs api | grep socket | tail
```

**조치**
1. **채팅은 폴링으로 계속 동작한다.** 급하지 않다.
2. Nginx 의 `/socket.io/` 블록에 `proxy_set_header Upgrade` 와 `Connection "upgrade"` 가 있는지 확인한다.
3. `docker compose -f docker-compose.prod.yml restart nginx`
4. 그래도 안 되면 `restart api`

---

## 4. DB 장애

**증상**: `/api/health` 는 되는데 상담 시작이 500. 운영 현황의 `DB` 가 `장애`.

**확인**
```bash
docker compose -f docker-compose.prod.yml ps postgres
docker compose -f docker-compose.prod.yml logs postgres --tail=50
docker compose -f docker-compose.prod.yml exec postgres pg_isready -U $POSTGRES_USER
```

**조치**
1. 디스크 용량을 먼저 본다. `df -h` — 가득 차면 PostgreSQL 이 멈춘다.
2. `docker compose -f docker-compose.prod.yml restart postgres`
3. 복구가 안 되면 **홈페이지에 임시 안내를 띄운다.**
   - 상담 위젯 대신 전화/이메일 안내를 노출한다.
4. 데이터가 손상됐다면 백업에서 복구한다 (`deploy/backup/restore.sh`).

**고객 안내 문구**
> 현재 온라인 상담이 일시적으로 어렵습니다. 전화(000-0000-0000) 또는 이메일로 문의해주세요.

---

## 5. 응답 지연 (운영자 대기가 쌓임)

**증상**: 운영 현황의 `10분 이상 무응답` 이 0 이 아니다.

**조치**
1. `운영자 대기` 필터로 오래 기다린 순 정렬해서 순서대로 처리한다.
2. 반복되면 운영자 근무 시간을 조정하거나, AI FAQ 범위를 넓힌다 (Phase 11).

---

## 6. HTTPS 인증서 만료

**증상**: 브라우저에 보안 경고. 소켓 연결 실패.

**확인**
```bash
echo | openssl s_client -connect $DOMAIN:443 2>/dev/null | openssl x509 -noout -dates
```

**조치**
```bash
docker compose -f docker-compose.prod.yml run --rm certbot renew --force-renewal
docker compose -f docker-compose.prod.yml restart nginx
```

certbot 컨테이너가 12시간마다 자동 갱신하므로 보통은 일어나지 않는다.
일어났다면 certbot 컨테이너가 죽어 있었을 가능성이 크다. `ps` 로 확인한다.

---

## 7. 배포 직후 문제

**즉시 롤백한다.** 원인 분석은 롤백 후에 한다.

```bash
cd deploy
git log --oneline -5
git checkout <직전-정상-커밋>
docker compose -f docker-compose.prod.yml up -d --build
```

DB 마이그레이션이 포함된 배포였다면 롤백만으로 부족하다.
`deploy/DEPLOY.md` 의 롤백 주의사항을 참고한다.
````

- [ ] **Step 3: `README-chat.md` 에 운영 문서 링크 추가**

```markdown
## 운영 문서

| 문서 | 내용 |
|---|---|
| `CHECKLIST.md` | 배포 전 체크리스트 |
| `deploy/DEPLOY.md` | 배포 절차 |
| `deploy/RUNBOOK.md` | 장애 대응 매뉴얼 |
| `deploy/backup/RESTORE-DRILL.md` | 월 1회 복구 리허설 |
| `apps/api/API.md` | API 레퍼런스 |
```

- [ ] **Step 4: 체크리스트를 실제로 한 번 통과시켜 본다**

로컬 개발 환경에서 자동 테스트 항목(1번)과 수동 확인 항목(2~7번)을 실행한다. 8~10번은 운영 배포 후에 확인한다.

- [ ] **Step 5: 커밋**

```bash
git add CHECKLIST.md deploy/RUNBOOK.md README-chat.md
git commit -m "docs: add deployment checklist and incident runbook"
```

---

## Task 9: 상담 만족도 평가

**Files:**
- Modify: `apps/api/prisma/schema.prisma`
- Create: `apps/api/src/feedback/feedbackService.ts`
- Modify: `apps/api/src/chatRooms/publicChatRoutes.ts`
- Modify: `apps/api/src/ops/metricsRoutes.ts`
- Modify: `packages/shared/src/types.ts`
- Modify: `js/chat/api.js`, `js/chat/render.js`, `js/chat/widget.js`, `js/chat/i18n.js`
- Modify: `css/chat-widget.css`
- Test: `apps/api/tests/feedback.test.ts`

**Interfaces:**
- Consumes: `authorizeVisitor` (Phase 1 Task 6), `validateBody` (Phase 1 Task 3)
- Produces:
  - Prisma 모델 `ChatFeedback`
  - `packages/shared/src/types.ts` → `SubmitFeedbackRequest = { rating: 1|2|3|4|5; comment?: string }`
  - `submitFeedback(roomId: string, input: SubmitFeedbackRequest): Promise<{ id: string; rating: number }>`
  - `POST /api/public/chat/:roomId/feedback` → `201 { feedback }`
  - `OpsMetrics` 에 `avgRating30d: number | null`, `feedbackCount30d: number` 추가

---

### 왜 이 기능이 필요한가

스펙 6.1은 고객용 채팅 위젯의 마지막 항목으로 "상담 종료 후 만족도 평가"를 둔다.

운영 관점에서 이게 필요한 이유는, 상담 품질을 알 방법이 이것밖에 없기 때문이다.
상담 건수와 응답 시간은 지표로 잡히지만 "고객이 만족했는가"는 물어보지 않으면
알 수 없다. 특히 번역을 거친 상담은 운영자가 자기 답변이 고객에게 어떻게 읽혔는지
확인할 방법이 없으므로, 평점이 유일한 신호다.

MVP 마지막에 넣는 이유는 상담이 실제로 종료될 수 있게 된 뒤에야 의미가 있기 때문이다.

- [ ] **Step 1: Prisma 스키마에 모델 추가**

`apps/api/prisma/schema.prisma` 에 추가한다.

```prisma
model ChatFeedback {
  id         String   @id @default(uuid())
  chatRoomId String   @unique @map("chat_room_id")
  rating     Int
  comment    String?
  createdAt  DateTime @default(now()) @map("created_at")

  chatRoom ChatRoom @relation(fields: [chatRoomId], references: [id], onDelete: Cascade)

  @@index([createdAt])
  @@map("chat_feedbacks")
}
```

`ChatRoom` 모델에 역방향 관계를 추가한다.

```prisma
  feedback ChatFeedback?
```

`@unique` 를 `chatRoomId` 에 거는 이유는 **한 상담에 평가는 한 번만** 받기 위해서다.
DB 차원에서 막으면 프론트엔드 버그나 중복 클릭으로 평점이 여러 개 쌓이는 일이 없다.

마이그레이션을 만든다.

```bash
npm run db:migrate -w apps/api -- --name add_chat_feedback
```

- [ ] **Step 2: `packages/shared/src/types.ts` 에 타입 추가**

```ts
export interface SubmitFeedbackRequest {
  rating: 1 | 2 | 3 | 4 | 5;
  comment?: string;
}

export interface ChatFeedbackDTO {
  id: string;
  rating: number;
  comment: string | null;
  createdAt: string;
}
```

그리고 기존 `OpsMetrics` 에 두 필드를 추가한다.

```ts
  /** 최근 30일 평균 평점. 평가가 하나도 없으면 null */
  avgRating30d: number | null;
  feedbackCount30d: number;
```

- [ ] **Step 3: 실패하는 테스트 작성**

`apps/api/tests/feedback.test.ts`

```ts
import request from 'supertest';
import { afterAll, beforeEach, describe, expect, it } from 'vitest';
import { createApp } from '../src/app';
import { prisma } from '../src/db';
import { createCustomerWithRoom, createOperator } from './helpers/factories';
import { disconnectDatabase, resetDatabase } from './helpers/testDb';

const app = createApp();

beforeEach(async () => {
  await resetDatabase();
});

afterAll(async () => {
  await disconnectDatabase();
});

describe('POST /api/public/chat/:roomId/feedback', () => {
  it('종료된 상담에 평점을 남길 수 있다', async () => {
    const { room, visitorToken } = await createCustomerWithRoom({ status: 'closed' });

    const res = await request(app)
      .post(`/api/public/chat/${room.id}/feedback`)
      .set('X-Visitor-Token', visitorToken)
      .send({ rating: 5, comment: '친절하게 안내해주셔서 감사합니다.' });

    expect(res.status).toBe(201);
    expect(res.body.feedback.rating).toBe(5);

    const saved = await prisma.chatFeedback.findUniqueOrThrow({
      where: { chatRoomId: room.id }
    });
    expect(saved.comment).toBe('친절하게 안내해주셔서 감사합니다.');
  });

  it('의견 없이 평점만 남길 수 있다', async () => {
    const { room, visitorToken } = await createCustomerWithRoom({ status: 'closed' });

    const res = await request(app)
      .post(`/api/public/chat/${room.id}/feedback`)
      .set('X-Visitor-Token', visitorToken)
      .send({ rating: 4 });

    expect(res.status).toBe(201);
    expect(res.body.feedback.comment).toBeNull();
  });

  it('토큰이 없으면 401 이다', async () => {
    const { room } = await createCustomerWithRoom({ status: 'closed' });

    const res = await request(app).post(`/api/public/chat/${room.id}/feedback`).send({ rating: 5 });

    expect(res.status).toBe(401);
  });

  it('남의 상담방에는 평점을 남길 수 없다', async () => {
    const mine = await createCustomerWithRoom({ status: 'closed' });
    const other = await createCustomerWithRoom({ status: 'closed' });

    const res = await request(app)
      .post(`/api/public/chat/${other.room.id}/feedback`)
      .set('X-Visitor-Token', mine.visitorToken)
      .send({ rating: 1 });

    expect(res.status).toBe(403);
  });

  it('1~5 범위를 벗어난 평점은 400 이다', async () => {
    const { room, visitorToken } = await createCustomerWithRoom({ status: 'closed' });

    for (const rating of [0, 6, 3.5, -1]) {
      const res = await request(app)
        .post(`/api/public/chat/${room.id}/feedback`)
        .set('X-Visitor-Token', visitorToken)
        .send({ rating });

      expect(res.status).toBe(400);
    }
  });

  it('아직 진행 중인 상담에는 평가할 수 없다', async () => {
    const { room, visitorToken } = await createCustomerWithRoom({ status: 'active' });

    const res = await request(app)
      .post(`/api/public/chat/${room.id}/feedback`)
      .set('X-Visitor-Token', visitorToken)
      .send({ rating: 5 });

    expect(res.status).toBe(409);
    expect(res.body.error.code).toBe('ROOM_NOT_CLOSED');
  });

  it('같은 상담에 두 번 평가하면 409 다', async () => {
    const { room, visitorToken } = await createCustomerWithRoom({ status: 'closed' });

    await request(app)
      .post(`/api/public/chat/${room.id}/feedback`)
      .set('X-Visitor-Token', visitorToken)
      .send({ rating: 5 })
      .expect(201);

    const res = await request(app)
      .post(`/api/public/chat/${room.id}/feedback`)
      .set('X-Visitor-Token', visitorToken)
      .send({ rating: 1 });

    expect(res.status).toBe(409);
    expect(res.body.error.code).toBe('ALREADY_SUBMITTED');
    // 첫 평점이 덮어써지지 않아야 한다.
    const saved = await prisma.chatFeedback.findUniqueOrThrow({ where: { chatRoomId: room.id } });
    expect(saved.rating).toBe(5);
  });

  it('너무 긴 의견은 400 이다', async () => {
    const { room, visitorToken } = await createCustomerWithRoom({ status: 'closed' });

    const res = await request(app)
      .post(`/api/public/chat/${room.id}/feedback`)
      .set('X-Visitor-Token', visitorToken)
      .send({ rating: 5, comment: 'あ'.repeat(1001) });

    expect(res.status).toBe(400);
  });
});

describe('운영 지표의 평점 집계', () => {
  it('최근 30일 평균 평점과 건수를 준다', async () => {
    const { plainPassword } = await createOperator({ email: 'fb@stemcare.test' });
    const agent = request.agent(app);
    await agent
      .post('/api/admin/auth/login')
      .send({ email: 'fb@stemcare.test', password: plainPassword });

    for (const rating of [5, 3]) {
      const { room, visitorToken } = await createCustomerWithRoom({ status: 'closed' });
      await request(app)
        .post(`/api/public/chat/${room.id}/feedback`)
        .set('X-Visitor-Token', visitorToken)
        .send({ rating })
        .expect(201);
    }

    const res = await agent.get('/api/admin/ops/metrics');

    expect(res.body.metrics.feedbackCount30d).toBe(2);
    expect(res.body.metrics.avgRating30d).toBe(4);
  });

  it('평가가 없으면 평균은 null 이다', async () => {
    const { plainPassword } = await createOperator({ email: 'fb2@stemcare.test' });
    const agent = request.agent(app);
    await agent
      .post('/api/admin/auth/login')
      .send({ email: 'fb2@stemcare.test', password: plainPassword });

    const res = await agent.get('/api/admin/ops/metrics');

    expect(res.body.metrics.avgRating30d).toBeNull();
    expect(res.body.metrics.feedbackCount30d).toBe(0);
  });
});
```

- [ ] **Step 4: 실패 확인**

```bash
npm run build -w packages/shared
npx vitest run tests/feedback.test.ts --root apps/api
```

- [ ] **Step 5: `apps/api/src/feedback/feedbackService.ts` 작성**

```ts
import { Prisma } from '@prisma/client';
import type { ChatFeedbackDTO, SubmitFeedbackRequest } from '@stemcare/shared';
import { conflict } from '../common/errors';
import { prisma } from '../db';

export async function submitFeedback(
  roomId: string,
  input: SubmitFeedbackRequest
): Promise<ChatFeedbackDTO> {
  const room = await prisma.chatRoom.findUniqueOrThrow({ where: { id: roomId } });

  // 아직 상담 중인데 평가를 받으면 의미가 없다.
  if (room.status !== 'closed') {
    throw conflict('ROOM_NOT_CLOSED', '상담이 종료된 후에 평가할 수 있습니다.');
  }

  try {
    const created = await prisma.chatFeedback.create({
      data: {
        chatRoomId: roomId,
        rating: input.rating,
        comment: input.comment?.trim() || null
      }
    });

    return {
      id: created.id,
      rating: created.rating,
      comment: created.comment,
      createdAt: created.createdAt.toISOString()
    };
  } catch (err) {
    // P2002 = chatRoomId unique 위반 = 이미 평가함
    if (err instanceof Prisma.PrismaClientKnownRequestError && err.code === 'P2002') {
      throw conflict('ALREADY_SUBMITTED', '이미 평가를 남기셨습니다. 감사합니다.');
    }
    throw err;
  }
}

/** 최근 N일 평균 평점. 평가가 없으면 null */
export async function averageRating(days: number): Promise<{ avg: number | null; count: number }> {
  const since = new Date(Date.now() - days * 24 * 60 * 60 * 1000);

  const result = await prisma.chatFeedback.aggregate({
    where: { createdAt: { gte: since } },
    _avg: { rating: true },
    _count: { _all: true }
  });

  return {
    // 소수점 둘째 자리까지만 쓴다. 화면에 4.333333 이 뜨면 지저분하다.
    avg: result._avg.rating === null ? null : Math.round(result._avg.rating * 100) / 100,
    count: result._count._all
  };
}
```

- [ ] **Step 6: `publicChatRoutes.ts` 에 라우트 추가**

```ts
const FeedbackSchema = z.object({
  rating: z.number().int().min(1, '1~5 사이로 평가해주세요.').max(5, '1~5 사이로 평가해주세요.'),
  comment: z.string().max(1000).optional()
});

publicChatRoutes.post(
  '/:roomId/feedback',
  validateBody(FeedbackSchema),
  asyncHandler(async (req, res) => {
    const room = await authorizeVisitor(req.params.roomId, visitorTokenOf(req));
    const body = req.body as z.infer<typeof FeedbackSchema>;

    const feedback = await submitFeedback(room.id, {
      rating: body.rating as 1 | 2 | 3 | 4 | 5,
      comment: body.comment
    });

    res.status(201).json({ feedback });
  })
);
```

import를 추가한다.

```ts
import { submitFeedback } from '../feedback/feedbackService';
```

`assertRoomOpen` 을 쓰지 않는다는 점에 주의한다. 평가는 **종료된** 상담에만 하는 것이므로
반대 조건이다. 그 검사는 `submitFeedback` 안에 있다.

- [ ] **Step 7: `metricsRoutes.ts` 에 평점 추가**

`Promise.all` 배열에 한 항목을 더한다.

```ts
    const [
      waitingRooms,
      activeRooms,
      botRooms,
      todayStarted,
      unansweredOver10Min,
      translationFailures24h,
      ratingStats
    ] = await Promise.all([
      // ... 기존 6개 그대로 ...
      averageRating(30)
    ]);
```

`metrics` 객체에 두 필드를 추가한다.

```ts
      avgRating30d: ratingStats.avg,
      feedbackCount30d: ratingStats.count,
```

import를 추가한다.

```ts
import { averageRating } from '../feedback/feedbackService';
```

- [ ] **Step 8: 운영 현황 대시보드에 타일 추가**

`apps/admin/app/chats/ops/page.tsx` 의 타일 목록에 추가한다.

```tsx
          <Tile
            label="평균 만족도 (30일)"
            value={
              metrics.avgRating30d === null
                ? '평가 없음'
                : `${metrics.avgRating30d} / 5 (${metrics.feedbackCount30d}건)`
            }
            warn={metrics.avgRating30d !== null && metrics.avgRating30d < 3.5}
          />
```

- [ ] **Step 9: 위젯 i18n 문구 추가**

`js/chat/i18n.js` 의 두 사전에 각각 추가한다.

`ko`:
```js
    'feedback.title': '상담은 어떠셨나요?',
    'feedback.hint': '평가를 남겨주시면 서비스 개선에 활용하겠습니다.',
    'feedback.comment': '남기고 싶은 말씀 (선택)',
    'feedback.submit': '평가 보내기',
    'feedback.thanks': '소중한 의견 감사합니다.',
    'feedback.already': '이미 평가를 남기셨습니다. 감사합니다.',
    'feedback.skip': '건너뛰기',
```

`ja`:
```js
    'feedback.title': 'ご相談はいかがでしたか。',
    'feedback.hint': '評価をいただけますと、サービス改善に活用いたします。',
    'feedback.comment': 'ご意見（任意）',
    'feedback.submit': '評価を送信',
    'feedback.thanks': '貴重なご意見をありがとうございます。',
    'feedback.already': 'すでに評価をいただいております。ありがとうございます。',
    'feedback.skip': 'スキップ',
```

키 개수 검사를 다시 실행한다. (Phase 4 Task 7 이후이므로 `50 50` 이 나온다)

- [ ] **Step 10: `js/chat/api.js` 에 함수 추가**

```js
export function submitFeedback(roomId, visitorToken, { rating, comment }) {
  return request(`/api/public/chat/${roomId}/feedback`, {
    method: 'POST',
    visitorToken,
    body: { rating, comment: comment || undefined }
  });
}
```

- [ ] **Step 11: `js/chat/state.js` 에 상태 추가**

```js
  feedback: 'hidden',   // 'hidden' | 'prompt' | 'sending' | 'done'
```

setter를 추가한다.

```js
export function setFeedbackPhase(phase) {
  if (state.feedback === phase) return;
  state.feedback = phase;
  notify();
}
```

- [ ] **Step 12: `js/chat/render.js` 에 평가 UI 추가**

`mountWidget` 의 채팅 화면에서, 입력창(`consult-chat-composer`) **앞에** 넣는다.

```html
        <div class="consult-chat-feedback" data-role="feedback" hidden>
          <p class="fb-title">${escapeHtml(t('feedback.title'))}</p>
          <p class="fb-hint">${escapeHtml(t('feedback.hint'))}</p>
          <div class="fb-stars" data-role="fb-stars" role="radiogroup" aria-label="${escapeHtml(t('feedback.title'))}">
            ${[1, 2, 3, 4, 5]
              .map(
                (n) =>
                  `<button type="button" class="fb-star" data-rating="${n}" role="radio" aria-checked="false" aria-label="${n}">★</button>`
              )
              .join('')}
          </div>
          <textarea data-role="fb-comment" rows="2" placeholder="${escapeHtml(t('feedback.comment'))}"></textarea>
          <div class="fb-actions">
            <button type="button" data-role="fb-skip">${escapeHtml(t('feedback.skip'))}</button>
            <button type="button" data-role="fb-submit" disabled>${escapeHtml(t('feedback.submit'))}</button>
          </div>
          <p class="fb-done" data-role="fb-done" hidden>${escapeHtml(t('feedback.thanks'))}</p>
        </div>
```

`elements` 에 추가한다.

```js
    feedback: root.querySelector('[data-role="feedback"]'),
    fbStars: root.querySelector('[data-role="fb-stars"]'),
    fbComment: root.querySelector('[data-role="fb-comment"]'),
    fbSubmit: root.querySelector('[data-role="fb-submit"]'),
    fbSkip: root.querySelector('[data-role="fb-skip"]'),
    fbDone: root.querySelector('[data-role="fb-done"]'),
```

`renderState` 의 채팅 화면 부분에 표시 규칙을 추가한다.

```js
  // 상담이 종료되면 평가를 한 번 권한다.
  const showFeedback = status === 'closed' && state.feedback !== 'hidden';
  elements.feedback.hidden = !showFeedback;
  elements.fbDone.hidden = state.feedback !== 'done';
  elements.fbSubmit.disabled = state.feedback === 'sending' || !elements.fbStars.dataset.selected;
```

- [ ] **Step 13: `js/chat/widget.js` 에 동작 연결**

`bindThreadActions` 옆에 새 함수를 만들고 `init()` 에서 호출한다.

```js
function bindFeedback(elements) {
  // 별점 선택 (이벤트 위임)
  elements.fbStars.addEventListener('click', (event) => {
    const button = event.target.closest('.fb-star');
    if (!button) return;

    const rating = Number(button.dataset.rating);
    elements.fbStars.dataset.selected = String(rating);

    for (const star of elements.fbStars.querySelectorAll('.fb-star')) {
      const isOn = Number(star.dataset.rating) <= rating;
      star.classList.toggle('is-on', isOn);
      star.setAttribute('aria-checked', String(Number(star.dataset.rating) === rating));
    }

    elements.fbSubmit.disabled = false;
  });

  elements.fbSkip.addEventListener('click', () => setFeedbackPhase('hidden'));

  elements.fbSubmit.addEventListener('click', async () => {
    const session = getSession();
    const rating = Number(elements.fbStars.dataset.selected);
    if (!session || !rating) return;

    setFeedbackPhase('sending');

    try {
      await submitFeedbackApi(session.roomId, session.visitorToken, {
        rating,
        comment: elements.fbComment.value.trim()
      });
      setFeedbackPhase('done');
    } catch (error) {
      // 이미 평가했다면 감사 문구를 보여주고 끝낸다. 오류로 취급하지 않는다.
      if (error instanceof ChatApiError && error.code === 'ALREADY_SUBMITTED') {
        setFeedbackPhase('done');
        elements.fbDone.textContent = t('feedback.already');
        return;
      }
      setFeedbackPhase('prompt');
    }
  });
}
```

그리고 상담 상태가 `closed` 로 바뀌는 순간 평가를 띄운다. `subscribe` 콜백을 감싸 상태 변화를 감지한다.

```js
  let previousStatus = null;
  subscribe((state) => {
    const status = state.room?.status ?? null;

    // 방금 종료되었고 아직 평가를 안 물어봤다면 띄운다.
    if (status === 'closed' && previousStatus !== null && previousStatus !== 'closed') {
      if (state.feedback === 'hidden') setFeedbackPhase('prompt');
    }
    previousStatus = status;

    renderState(elements, state);
  });
```

import에 다음을 추가한다.

```js
import { ChatApiError, sendMessage, startChat, submitFeedback as submitFeedbackApi } from './api.js';
import { setFeedbackPhase } from './state.js';
import { t } from './i18n.js';
```

`previousStatus !== null` 조건이 중요하다. 이게 없으면 **이미 종료된 상담을 새로고침으로
다시 열었을 때** 평가창이 또 뜬다. 첫 렌더에서는 띄우지 않고, 실제로 `active → closed` 로
바뀌는 순간에만 띄운다.

- [ ] **Step 14: `css/chat-widget.css` 에 스타일 추가**

```css
.consult-chat-feedback {
  flex: none;
  padding: 12px 16px;
  border-top: 1px solid rgba(0, 0, 0, 0.08);
  background: #fbfaf7;
}

.consult-chat-feedback[hidden] { display: none; }

.consult-chat-feedback .fb-title {
  margin: 0 0 2px;
  font-size: 13px;
  font-weight: 600;
}

.consult-chat-feedback .fb-hint {
  margin: 0 0 8px;
  font-size: 11px;
  color: #7a8798;
}

.fb-stars { display: flex; gap: 2px; margin-bottom: 8px; }

.fb-star {
  border: none;
  background: none;
  padding: 0 2px;
  font-size: 22px;
  line-height: 1;
  color: #d8d2c4;
  cursor: pointer;
}

.fb-star.is-on { color: #b8922d; }

.consult-chat-feedback textarea {
  width: 100%;
  resize: none;
  border: 1px solid rgba(0, 0, 0, 0.15);
  border-radius: 6px;
  padding: 6px 8px;
  font: inherit;
  font-size: 13px;
  margin-bottom: 8px;
}

.fb-actions { display: flex; gap: 6px; justify-content: flex-end; }

.fb-actions button {
  border: 1px solid rgba(0, 0, 0, 0.15);
  background: #fff;
  border-radius: 6px;
  padding: 5px 12px;
  font-size: 12px;
  cursor: pointer;
}

.fb-actions button[data-role='fb-submit'] {
  background: #b8922d;
  border-color: #b8922d;
  color: #fff;
}

.fb-actions button:disabled { opacity: 0.5; cursor: not-allowed; }

.consult-chat-feedback .fb-done {
  margin: 8px 0 0;
  font-size: 12px;
  color: #1a7f37;
}

.consult-chat-feedback .fb-done[hidden] { display: none; }
```

- [ ] **Step 15: 테스트 통과 확인**

```bash
npm run build -w packages/shared
npm run test -w apps/api
```

기대: feedback 10 passed, 전체 통과.

- [ ] **Step 16: 브라우저에서 확인**

1. 고객으로 상담을 시작하고 메시지를 몇 개 주고받는다.
2. 운영자가 `상담 종료` 를 누른다.
3. **고객 화면 입력창 위에 평가 UI가 나타난다.**
4. 별 4개를 누르고 의견을 적은 뒤 `평가 보내기` → "소중한 의견 감사합니다."
5. 새로고침해도 평가창이 다시 뜨지 않는다.
6. 운영 현황(`/chats/ops`)에 `평균 만족도 (30일) — 4 / 5 (1건)` 이 보인다.
7. 이미 평가한 상담에서 API를 직접 한 번 더 호출하면 409 가 온다.

```bash
curl -s -X POST "http://localhost:4000/api/public/chat/<ROOM_ID>/feedback"   -H 'Content-Type: application/json' -H "X-Visitor-Token: <TOKEN>"   -d '{"rating":1}'
# → {"error":{"code":"ALREADY_SUBMITTED",...}}
```

- [ ] **Step 17: 보관 정책에 평가 포함**

`apps/api/src/ops/retention.ts` 의 `deleteOldMessages` 에서 요약·메모를 지우는 부분 옆에
평가도 함께 지운다. 의견란에 개인적인 내용이 들어갈 수 있다.

```ts
  await prisma.chatFeedback.deleteMany({ where: { chatRoomId: { in: roomIds } } });
```

- [ ] **Step 18: 커밋**

```bash
git add apps/api apps/admin packages/shared js/chat css/chat-widget.css
git commit -m "feat: add post-consultation satisfaction rating with ops metric"
```

---

## Task 10: CI 자동화 (선택)

**Files:**
- Create: `.github/workflows/ci.yml`

**Interfaces:**
- Consumes: 전체 테스트
- Produces: PR마다 자동 검증

---

- [ ] **Step 1: `.github/workflows/ci.yml` 작성**

GitHub를 쓰지 않는다면 이 Task는 건너뛴다.

```yaml
name: CI

on:
  push:
    branches: [master]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: stemcare
          POSTGRES_PASSWORD: stemcare
          POSTGRES_DB: stemcare_chat_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10

    env:
      DATABASE_URL: postgresql://stemcare:stemcare@localhost:5434/stemcare_chat_test
      TEST_DATABASE_URL: postgresql://stemcare:stemcare@localhost:5434/stemcare_chat_test
      JWT_SECRET: ci-only-secret-that-is-long-enough-for-tests-12345
      WEBSITE_ORIGIN: http://localhost:8080
      ADMIN_ORIGIN: http://localhost:3100
      # OPENAI_API_KEY 를 넣지 않는다.
      # 테스트가 실제 API 없이 통과하는지 확인하는 것이 목적이다.

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - run: npm run build -w packages/shared

      - run: npx prisma generate --schema apps/api/prisma/schema.prisma

      - run: npx prisma db push --schema apps/api/prisma/schema.prisma --skip-generate

      - name: API 테스트
        run: npm run test -w apps/api

      - name: 관리자 화면 테스트
        run: npm run test -w apps/admin

      - name: 빌드
        run: npm run build

      - name: 개인정보가 로그에 남지 않는지 확인
        run: |
          # maskPersonalData 테스트가 통과하는지 재확인
          npx vitest run tests/logger.test.ts --root apps/api
```

- [ ] **Step 2: 로컬에서 CI와 같은 조건으로 실행**

`OPENAI_API_KEY` 없이 테스트가 통과하는지 확인한다.

```bash
OPENAI_API_KEY= npm run test -w apps/api
```

기대: 전부 통과. 실패하면 어딘가에서 실제 API를 부르고 있다는 뜻이므로 `setModelCaller` 로 막아야 한다.

- [ ] **Step 3: 커밋**

```bash
git add .github
git commit -m "ci: add github actions workflow for tests and build"
```

---

## Phase 6 완료 기준 (Definition of Done)

- [ ] `CHECKLIST.md` 의 1~7번(자동 테스트 + 로컬 수동 확인)을 전부 통과한다.
- [ ] helmet 보안 헤더가 적용되고 `x-powered-by` 가 없다.
- [ ] 로그인과 상담 시작에 rate limit이 걸린다.
- [ ] 운영 환경에서 `JWT_SECRET` 이 기본값이면 서버가 시작되지 않는다.
- [ ] 서버 로그에 고객 이름·전화·이메일·상담 본문이 남지 않는다. (grep으로 0건 확인)
- [ ] 개인정보 보관 정책이 코드 상수로 명시되고 스케줄로 실행된다.
- [ ] `npm run ops:retention -w apps/api` 로 수동 실행이 된다.
- [ ] 운영 현황 대시보드에서 대기 상담·무응답·번역 실패·DB 상태를 볼 수 있다.
- [ ] E2E 테스트가 고객 접수 → 배정 → 번역 답변 → 종료 전 과정을 검증한다.
- [ ] E2E 테스트가 스크롤 품질 3가지(위치 유지·새 메시지 버튼·처음 여는 방은 최신/다시 여는 방은 읽던 위치 복원)를 검증한다.
- [ ] `deploy/` 로 HTTPS 배포 구성이 만들어졌고 이미지가 빌드된다.
- [ ] DB 백업이 매일 자동 실행되고 7일 보관된다.
- [ ] **복구 리허설을 실제로 실행해봤고 기록했다.**
- [ ] `deploy/RUNBOOK.md` 에 6가지 장애 시나리오 대응이 문서화되어 있다.
- [ ] 상담이 종료되면 고객 화면에 만족도 평가가 뜨고, 한 번만 제출된다.
- [ ] 운영 현황에 최근 30일 평균 만족도가 보인다.

## MVP 이후 확장 (별도 Phase)

- **Phase 7**: 음성 입력(STT), 음성 재생(TTS)
- **Phase 8**: 파일/이미지 첨부
- **Phase 9**: 상담 통계와 운영자 성과 분석
- **Phase 10**: LINE / 카카오톡 / 이메일 알림 연동
- **Phase 11**: AI 지식베이스(FAQ) 관리 화면

## 초보자가 자주 막히는 지점

**"Nginx 뒤에서 rate limit이 모든 요청을 같은 IP로 본다"**
`app.set('trust proxy', 1)` 이 없으면 Express가 보는 IP가 항상 Nginx 컨테이너 IP다. 그러면 한 사람이 한도를 채우면 모두가 막힌다.

**"WebSocket이 프로덕션에서만 연결이 안 된다"**
Nginx의 `/socket.io/` 블록에 `proxy_set_header Upgrade $http_upgrade;` 와 `proxy_set_header Connection "upgrade";` 가 빠졌다. 이 두 줄이 없으면 WebSocket 업그레이드가 일어나지 않는다.

**"관리자 화면이 API를 못 찾는다 (프로덕션)"**
`NEXT_PUBLIC_API_URL` 은 **빌드 시점**에 코드에 박힌다. 컨테이너 환경변수로 넣어도 소용없다. `docker build --build-arg` 로 넘겨야 한다.

**"Secure 쿠키가 저장되지 않는다"**
HTTPS가 아니다. 로컬에서 `NODE_ENV=production` 으로 테스트하면 `secure: true` 때문에 쿠키가 저장되지 않는다. 로컬 테스트는 `development` 로 한다.

**"백업 파일이 계속 쌓여 디스크가 찬다"**
`find ... -mtime +7 -delete` 가 동작하지 않았다. `docker compose exec backup ls -lt /backups` 로 파일 수를 주기적으로 확인한다.

**"마이그레이션을 롤백하려다 데이터를 잃었다"**
Prisma 마이그레이션은 되돌리는 기능이 없다. 컬럼 삭제는 반드시 두 번의 배포로 나눈다. 1차: 코드에서 사용 중단. 2차(며칠 뒤): 컬럼 삭제.

**"E2E 테스트가 로컬에서만 되고 CI에서 실패한다"**
CI에는 홈페이지(8080)와 관리자(3100) 서버가 없다. E2E는 별도 워크플로에서 `docker compose up` 후 실행하거나, 수동 실행 항목으로 남긴다. 이 계획서의 CI는 단위·통합 테스트만 돌린다.
