# 배포 전 체크리스트

**모든 항목을 통과해야 배포한다.** 하나라도 미확인이면 배포하지 않는다.

## 1. 자동 테스트

```bash
npm run test -w apps/api          # [ ] 통과
npm run test -w apps/admin        # [ ] 통과
npm run test:e2e                  # [ ] 통과
npm run build                     # [ ] 통과
```

## 2. 고객 위젯 수동 확인

- [ ] `/korea-travel/`, `/stemcell/`, `/` 세 페이지에서 위젯이 열린다
- [ ] 개인정보 동의 없이 시작하면 막힌다 (Network 탭에 요청이 안 나간다)
- [ ] 이름/연락처 누락 시 안내가 나온다
- [ ] 상담 시작 → AI 인사가 고객 언어로 온다
- [ ] 메시지 전송 → 즉시 화면에 보인다
- [ ] 새로고침 → 대화가 유지된다
- [ ] 오프라인 전송 → "전송 실패 · 다시 시도" 가 나오고 재시도가 된다
- [ ] 위로 스크롤 상태에서 새 메시지 → 화면이 안 움직이고 `새 메시지` 버튼만 뜬다
- [ ] 일본어 페이지에서 모든 문구가 일본어다 (한국어가 안 섞인다)

## 3. 운영자 화면 수동 확인

- [ ] 로그인 실패 시 안내가 나온다
- [ ] 로그인 없이 `/chats` 접근 → `/login` 으로 튕긴다
- [ ] 상담 목록에 unread 배지, 상태, 서비스, 언어가 보인다
- [ ] 필터와 정렬이 동작한다
- [ ] 상담 상세에서 원문과 번역이 구분되어 보인다
- [ ] `내가 상담하기` → 상태가 `진행 중` 이 되고 고객 화면에도 반영된다
- [ ] 메모 저장이 되고, 고객 화면에는 절대 안 보인다
- [ ] `상담 종료` → 양쪽 입력창이 잠긴다
- [ ] 운영 현황 대시보드가 열린다

## 4. 번역 확인

- [ ] 일본어 고객 메시지 → 운영자 화면에 한국어 번역이 붙는다
- [ ] 한국어 운영자 답변 → 일본어 미리보기가 뜬다
- [ ] 미리보기를 수정해서 보내면 `직접 수정` 표시가 된다
- [ ] 고객 화면에는 일본어만 보인다
- [ ] 번역 실패 시 원문은 그대로 저장·전달되고 `다시 시도` 버튼이 나온다

## 5. AI 확인

- [ ] 공항 픽업 등 기본 문의에 AI가 답한다
- [ ] 가격 문의 → AI가 답하지 않고 운영자 대기로 전환된다
- [ ] 예약 요청 → 운영자 대기로 전환된다
- [ ] 건강 상태 언급 → 운영자 대기로 전환된다
- [ ] 전환 시 사이드바에 AI 요약과 위험 태그가 보인다
- [ ] 운영자 배정 후에는 AI가 끼어들지 않는다

## 6. 실시간

- [ ] 고객 메시지가 운영자에게 즉시 도착한다
- [ ] 운영자 답변이 고객에게 즉시 도착한다
- [ ] 신규 상담이 운영자 목록에 즉시 나타난다
- [ ] 입력 중 표시가 보인다
- [ ] API 서버를 껐다 켜면 자동 재연결된다
- [ ] socket.io CDN 을 차단해도 폴링으로 동작한다

## 7. 모바일

- [ ] iOS Safari 에서 상담 시작이 된다
- [ ] Android Chrome 에서 상담 시작이 된다
- [ ] 가로 스크롤이 생기지 않는다
- [ ] 키보드가 올라와도 입력창이 가려지지 않는다

## 8. 보안

- [ ] HTTPS 로 접속된다 (자물쇠 아이콘)
- [ ] HTTP 로 접속하면 HTTPS 로 리다이렉트된다
- [ ] 관리자 쿠키가 `HttpOnly; Secure; SameSite=Lax` 다 (개발자도구 Application → Cookies)
- [ ] 로그인 없이 관리자 API 호출 → 401
- [ ] 남의 `visitorToken` 으로 상담방 접근 → 403
- [ ] 로그인 10회 연속 실패 → 429
- [ ] 서버 로그에 이름·전화·이메일·상담 본문이 없다
  ```bash
  docker compose -f deploy/docker-compose.prod.yml logs api | grep -c "010-\|@.*\.com" # → 0
  ```
- [ ] `.env` 가 Git 에 없다 (`git ls-files | grep -c '\.env$'` → 0)
- [ ] `JWT_SECRET` 이 기본값이 아니다

## 9. 개인정보

- [ ] 위젯에 개인정보 수집 동의 문구가 표시된다
- [ ] 수집 항목·목적·보관 기간이 문구에 들어 있다
- [ ] 보관 기간 정책이 코드 상수로 명시되어 있다 (`retention.ts`)
- [ ] 보관 작업이 스케줄에 등록되어 있다 (`retention_job_scheduled` 로그 확인)
- [ ] 수동 실행이 동작한다 (`npm run ops:retention -w apps/api`)

## 10. 운영 준비

- [ ] 관리자 초기 비밀번호를 변경했다
- [ ] DB 백업이 매일 실행된다 (`docker compose ... exec backup ls /backups`)
- [ ] **복구 리허설을 실제로 해봤다** (`deploy/backup/RESTORE-DRILL.md`)
- [ ] 장애 대응 매뉴얼(`deploy/RUNBOOK.md`)을 팀이 읽었다
- [ ] 운영 현황 대시보드에 접근할 수 있다
- [ ] 롤백 절차를 알고 있다
